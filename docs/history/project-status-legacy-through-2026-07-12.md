# Weapon Reinforce - PROJECT STATUS

## 현재 버전: 1.1.0 (조우·전투·전리품·탐사 준비소 기준선)

## 최신 작업 요약

### 2026-07-12 1.1 배포 마감 보강
- 혼합된 기존 작업 트리와 분리한 `origin/master` 기반 릴리스 worktree에 탐사 1.1 파일만 옮겼다. `archive/`, `tmp/`, 토끼·자라 모험, 이순신 후보·참고 생성물은 소스 커밋과 Pages 배포물에서 제외한다.
- 자동 저장 쓰기 실패를 더 이상 조용히 넘기지 않는다. 용량 부족과 저장 차단을 구분해 진행 유실 가능성과 JSON 파일 백업 방법을 화면에 안내하고, 저장 파일 교체 중 쓰기 실패는 실제 실패 저장소를 주입해 기존 진행 롤백을 검증한다.
- 퀴즈 런타임 검증은 카탈로그 9팩·출처 매니페스트·문항이 실제 참조하는 SVG만 허용한다. 현재 공개 대상 516개와 예상 집합이 정확히 같아야 하며 미참조 SVG나 임의 파일이 추가되면 배포를 중단한다.
- 탐사 모달에 초기 포커스, Tab 순환, 진행 중 Escape 차단, 정산 뒤 Escape 닫기와 원래 버튼으로의 포커스 복귀를 추가했다. 무기 도감의 해금 카드는 키보드 버튼으로 바꾸고 무기·괴작 도감을 접근 가능한 대화상자로 표시한다.
- 진행 중 탐사와 날짜 변경 일보가 동시에 열리던 경우에는 탐사를 먼저 보여 주고 귀환 뒤 일보가 열리도록 렌더 순서를 고쳤다. 390px 모바일에서 자동전투·귀환·정산, 가로 넘침 0개, 모달 내부 포커스와 브라우저 오류 0개를 프로덕션 미리보기로 다시 확인했다.
- 패키지는 Node 22와 npm 10.9.2를 릴리스 기준으로 명시하고 React·React DOM·Scheduler의 MIT 고지를 `THIRD_PARTY_NOTICES.md`에 기록했다. GitHub Pages 워크플로는 단순 빌드 대신 `npm run validate:all` 전체 게이트를 통과해야만 배포한다.

### 2026-07-11 탐사 시스템 1.1 완성
- 랜덤 조우를 `유형 우선 감독`으로 바꿨다. 2~6층에서 먼저 적 60%·NPC 23%·사건 17%를 결정한 뒤 해당 유형 안에서 개별 가중치를 뽑으므로 콘텐츠 수가 늘어도 유형 확률이 변하지 않는다. 첫 층과 마지막 층은 적으로 고정하고, 최근 ID 반복 방지와 적 2연속 뒤 도움 조우 안전장치를 진행 저장에 포함한다.
- 적 6종에 서로 다른 역할, 방어, 체력·공격 보정, 일반 공격, 강공 주기와 공격 예고를 부여했다. 전투는 `내 공격 → 상대 공격 예고 → 상대 공격`을 자동 반복하며 무기별 치명타·방어·회복, NPC가 남긴 다음 공격·방어 효과, 적 방어와 강공이 실제 계산에 반영된다.
- NPC 3종과 사건 3종은 같은 회복 복제가 아니라 회복·다음 공격·다음 방어·다음 전리품 조사 보정을 조합해 준다. 받은 효과와 현재 적/무기/준비물 특징을 탐사 화면에서 바로 확인할 수 있다.
- 명성·기록 조각과 별도의 가상 전리품 6종, 지역별 드롭 표 7종을 추가했다. 전리품은 탐사 중 `pendingLoot`에 담기며 안전 귀환 때만 창고로 옮겨지고 사망하면 전부 잃는다. 같은 정산 ID는 통계와 전리품 경제 모두 한 번만 반영된다.
- 기존 복원 무기고를 `탐사 준비소` 안으로 통합했다. 창고 전리품과 퀴즈 엽전으로 여행 붕대·공명 손질 천·균열 안전 주머니를 만들고 하나를 다음 탐사에 장착한다. 탐사 시작 때 한 개만 소비되어 최대 체력 +15, 공격력 +3, 패배 엽전 손실 50% 감소 중 하나가 적용된다. 전리품은 직접 판매하지 않아 퀴즈가 엽전의 주 수입원으로 유지된다.
- 강화 실패 괴작은 탐사 전리품과 분리했다. 마지막 보유품 판매로 보유 조건 칭호가 비활성화되면 확인 경고를 띄우고, 중복품은 한 개를 남긴 채 일괄 판매할 수 있다. 최초 발견과 이미 받은 칭호 보상은 유지된다.
- 탐사 진행 스키마를 버전 3으로 올려 물리 전리품, 최근 조우 유형, 적 연속 횟수, 양측 턴 수, 남은 도움 효과, 결정된 적 공격, 사용 준비물을 저장한다. 버전 2 진행은 명시적으로 이관하고 지원하지 않는 버전·깊이와 맞지 않는 조우·살아 있는 적의 결정 상태는 거부한다. 새 `weaponExpeditionEconomyV1`은 기본 로컬 자동 저장과 JSON 저장 데이터 파일에 모두 포함된다.
- `validate:economy`와 강화된 `validate:expedition`은 결정적 드롭, 거래·정산 중복 방지, 60/23/17 분포, 적 역할, 공격 예고, NPC/사건 효과, 준비물, 귀환/사망, v2→v3 이관과 손상 상태 거부를 검사한다. 개발 브라우저에서 데스크톱·768px 태블릿·390px 모바일의 준비소와 전투를 확인했고, 모바일에서 `귀환 → 전리품 보관 → 붕대 제작·장착 → 다음 탐사 HP 120→135 → 1개 소비` 흐름과 새로고침 저장 복구를 실제로 통과했다.

### 2026-07-11 탐사·로컬 저장·역사 학습 1.0 뼈대 통합
- 서버와 계정 없이 브라우저 `localStorage`를 자동 체크포인트로 사용한다. 강화, 엽전, 퀴즈, 도감뿐 아니라 진행 중인 탐사 전체를 저장하므로 새로고침 뒤에도 같은 HP, 깊이, 조우, 임시 전리품과 난수 상태에서 이어진다. 정산 직전 상태도 먼저 기록해 새로고침으로 보상이나 손실을 피하거나 중복 적용할 수 없게 했다.
- 기본 저장은 브라우저 `localStorage` 자동 저장이다. 더 확실한 보관이나 기기 이동을 원하는 경우에만 `저장·백업`에서 버전·생성 시각·체크섬이 포함된 1MB 이하 저장 데이터 파일을 기기에 내려받는다. 불러올 때 형식·버전·허용 키·크기·체크섬을 검증한 뒤 교체하며, 탐사 중에도 `파일 백업`으로 활성 체크포인트를 바로 내려받을 수 있다. 쓰기 도중 실패하면 이전 저장으로 되돌리고 서버 전송은 하지 않는다.
- 탐사를 3개 지역, 7개 역사층의 고정 경로로 정리했다. 첫 조우는 적, 마지막은 최종 적으로 고정하고 중간에는 깊이별 후보 중 적 60%, NPC 23%, 사건 17% 비율로 가중 추첨한다. 최근 조우 중복을 피하고 저장된 난수 상태를 사용해 새로고침이나 속도 변경으로 결과를 다시 뽑지 못한다.
- 콘텐츠 뼈대는 가상의 적 6종, NPC 3종, 비전투 사건 3종으로 확장했다. 적의 힘은 플레이어 단계가 아니라 탐사 깊이를 기준으로 정하고, NPC와 사건은 회복·명성·기록 조각을 제공한다. 사망하면 임시 보상을 잃고 최대 200냥 범위의 강탈 또는 즉시 수리비를 내지만 현재 강화 단계는 보존한다.
- 7단계 무기마다 공격명, 치명타, 방어, 타격 회복과 화면 배치를 따로 부여했다. 전장은 상반신 뒷모습과 현재 무기를 보여 주고, 내 공격 차례에는 무기 종류별 1인칭 휘두르기·찌르기·사격 연출로 전환한다. 턴 표시는 다음 차례가 아니라 방금 실행 중인 행동을 가리킨다.
- `시간 기록관`을 추가했다. 탐사에서 해당 역사층을 먼저 목격한 뒤 기록 조각을 써야 전체 카드가 열리며, 무기 시대·용도·고증 설명·공식 자료 링크·초등학생용 생각 질문을 함께 제공한다. 누적 명성은 탐사자 칭호를 올리고, +7 무기로 7층에서 안전 귀환하면 완주 기록을 해금한다.
- 1.0 최종 고증 재검수에서 7단계 문장을 방위사업청·국사편찬위원회 우리역사넷·국립중앙박물관 자료와 다시 대조했다. 쌍수도는 1598년 《무예제보》의 장도 명칭 관계를 분명히 했고, 칠지도의 의식용 성격은 추정으로 표시했으며, 비파형동검은 기원전 8~7세기경 이후라는 편년 표현으로 구체화했다.
- `npm run validate:all`은 적/NPC/사건/역사층 수, 가중 분포, 첫·마지막 조우, 난수 재현, 자동전투, 귀환·사망 정산, 기록 복원, 저장 파일 변조/용량/롤백, 무기 전투 개성과 런타임 자산을 모두 통과했다. 데스크톱·768px급 태블릿·390px급 모바일, 탐사 중/정산 후 새로고침, 실제 JSON 다운로드 파일, 프로덕션 `/weapon-reinforce/` 경로도 확인했고 콘솔 오류와 깨진 이미지는 0개였다. 인앱 검증 도구가 로컬 파일 주입을 지원하지 않아 OS 파일 선택 창을 통한 불러오기 클릭만 다음 실제 브라우저 배포 점검 항목으로 남긴다.

> 아래 0.13 이하 항목은 구현 변천 기록이다. 현재 출시 기준은 위 1.0 후보와 `CHECKLIST.md`를 따른다.

### 2026-07-11 연속 탐사·자동 교대전투 뼈대 완성
- 한 번의 확률 판정으로 끝나던 랜덤 조우를 `시간 균열 탐사`로 교체했다. 플레이어와 적이 실제 HP와 공격력을 가지고 `내 공격 → 상대 공격` 순서로 자동 전투하며, 전투 중에는 현재 무기를 든 플레이어의 뒷모습과 1인칭 무기 공격 연출이 함께 나타난다.
- 첫 확장 단위로 적 `시간 틈의 노상강도` 1종과 NPC `길 위의 훈장` 1종만 데이터화했다. 홀수 깊이에서는 깊이에 따라 강해지는 적, 짝수 깊이에서는 체력을 회복하고 기록 조각을 주는 NPC를 만나며, 이후에는 `src/data/expedition.js`에 항목과 선택 규칙을 추가하는 방식으로 확장할 수 있다.
- 승리나 NPC 조우로 얻는 명성·기록 조각은 임시 전리품이다. 매 조우 뒤 `안전하게 복귀`하면 모두 영구 기록으로 옮기고, `더 깊이 탐사`하면 현재 체력과 임시 보상을 유지한 채 다음 조우로 간다. 최대 깊이는 7이며 마지막 조우 뒤에는 안전 귀환만 선택할 수 있다.
- 전투에서 쓰러지면 임시 전리품을 전부 잃고 강제 귀환한다. 패널티는 무기 손상에 따른 긴급 수리비 또는 노상강도에게 빼앗기는 엽전으로 나뉘며, 보유액과 200냥 상한을 넘지 않는다. 현재 무기는 자동 수리되어 강화 단계가 내려가거나 파괴되지 않으므로 강화 핵심 루프는 보존한다.
- 탐사 중 강화·퀴즈·도감·복원 무기고 등 다른 조작을 잠그고, 타이머는 실행 ID·단계·스텝을 확인한 뒤 한 번만 진행한다. 정산 ID도 저장해 빠른 입력이나 재렌더링으로 보상·손실이 중복 반영되지 않게 했다. 구형 랜덤 조우의 명성/승리 기록은 새 탐사 통계로 한 번 이관한다.
- 뒷모습 플레이어는 `knol-jump`의 세종·이순신·허난설헌 스프라이트를 픽셀 밀도와 비율 참고 자료로만 사용해 새로 제작했다. 기존 노상강도·훈장 이미지는 투명 배경 파생본으로 분리했다. 세 이미지는 이번 시스템 뼈대의 런타임 자산으로 임시 채택했으며, 적/NPC 콘텐츠를 늘릴 때 최종 아트 검수를 다시 거친다.
- `npm run validate:expedition`이 교대 순서, 처치 뒤 반격 금지, HP 범위, NPC 회복, 임시 보상, 귀환/사망 정산, 손실 상한, 강화 단계 보존, 중복 정산 방지를 검사한다. `validate:balance`는 동일 상태 모델로 강화 단계별 최대 7깊이 원정을 반복해 전투가 유한하게 끝나는지 확인한다.

### 2026-07-11 위인점프 퀴즈 9종과 실제 플레이 루프 완성
- `pinky-ne-site/games/knol-jump/assets/quiz`의 검증된 스냅샷을 가져와 구구단·나눗셈·10 만들기·100 만들기·10/100/1000 곱하기·책상/의자·면 색칠·모서리 색칠·전개도 판별의 9팩 762문항을 `퀴즈 공방`에 연결했다. 텍스트와 SVG 보기형 문제를 같은 모달에서 선택해 풀 수 있고, 팩별 난도에 맞는 엽전, 정답/오답/연속 정답/평생 풀이 통계를 저장한다.
- 퀴즈는 원본 앱을 런타임 의존성으로 삼지 않는 내부 스냅샷이다. 출처 커밋과 복사 시점은 `public/quiz/weapon-reinforce-manifest.json`, 갱신 규칙은 `docs/quiz-source-boundary.md`에 기록했다. 검증기는 9팩 762문항, 정답-선택지 관계, 모든 SVG 경로와 위험 태그를 검사한다.
- 강화 비용과 복원 무기고 가격은 퀴즈 평균 보상 기준으로 맞췄으며, 시뮬레이션상 기본 입력으로 +7까지 약 235문항, 항상 PERFECT면 약 132문항이 필요하다. 당시 랜덤 조우의 무금전 보상 정책은 위의 0.13 연속 탐사 정산으로 대체되었다.
- 운영 화면의 시간 가속·무기 즉시 획득·연출 체험은 개발 서버의 `?debug=1`에서만 보인다. 강화 중 도감·괴작·복원·퀴즈·조우를 잠가 오래된 비동기 판정이 진행도를 덮어쓰지 못하게 했다.
- 자정 일보는 일일 최고 기록만 정산하고 현재 무기와 엽전을 보존한다. 손상되거나 구버전인 로컬 저장값은 범위와 자료형을 검사해 안전한 기본값으로 복구한다.
- 괴작 칭호 7종에 각각 한 번만 받을 수 있는 엽전 보상을 추가했다. 영구 발견 칭호는 괴작 판매 후에도 남고, 보유 조건 칭호는 조건이 깨지면 비활성화되지만 이미 받은 보상은 중복 회수되지 않는다.
- Vite가 원본 `public/` 전체를 복사하지 않고 허용된 모루·무기 7종·퀴즈·파비콘만 `.runtime-public/`로 준비하도록 바꿨다. 배포 결과물은 약 196MB에서 약 4.2MB로 줄었으며, 토끼·자라 장편 모험은 앱 실행 범위와 배포 자산에서 분리했다.
- 텍스트/그림형 퀴즈, 오답 5초 잠금, 새로고침 저장, 강화 중 조작 잠금, base path가 있는 프로덕션 빌드를 데스크톱·태블릿 표시 모드·390px 모바일 화면에서 직접 확인했다. 모바일/태블릿 헤더는 2열/3열로 재배치해 복원 무기고를 포함한 모든 조작이 화면 안에 들어온다.

> 2026-06의 토끼·자라 모험 기록은 과거 작업 이력이다. 현재 `weapon-reinforce`의 실행 기능과 검증 기준에는 포함되지 않는다.

### 2026-07-10 가벼운 랜덤 조우와 밸런스 검증 구현
- 메인 액션의 미연시형 `모험 시작`을 weapon-reinforce 전용 `랜덤 조우`로 교체했다. 한 번의 조우는 적 소개, 공개 승률, 한 번의 판정, 즉시 귀환 또는 다음 조우로 끝난다. 토끼·자라 장편 카탈로그 import도 제거해 실행 번들에 해당 이야기가 포함되지 않는다.
- 들개, 멧돼지, 풋내기 산적, 솥뚜껑 도깨비, 시간 틈의 병사 등 가벼운 독립 사건 12종을 추가했다. 적 상성은 사용하지 않고 현재 무기 강화 단계와 적 위협도만으로 25~95% 승률을 계산한다.
- 승리 시 적 위협도에 맞는 엽전을 얻고, 패배 시 보유 범위 안에서 소량의 엽전만 잃는다. 강화 무기는 조우 패배로 파괴되지 않는다. 누적 승·패, 현재 연승, 최고 연승, 획득·손실 엽전도 로컬 기록으로 보존한다.
- 강화 실패로 괴작이 떨어질 때 별도의 등급별 획득 카드와 신규 도감 등록 표시가 나타난다. 개발용 `괴작획득 연출`은 실제 도감 수량을 바꾸지 않고 화면만 확인한다.
- `npm run validate:balance`를 추가해 랜덤 조우 35만 회와 +1→+7 강화 완주 4만 회를 고정 시드로 시뮬레이션한다. 조우 승률과 기대 보상이 무기 단계에 따라 오르고, PERFECT 입력이 평균 완주 시도와 비용을 낮추는지 검사한다.
- 데스크톱과 390px급 모바일 화면에서 조우 준비·패배·다음 조우·괴작 획득 연출을 확인했고, 모바일 괴작 카드 너비 잘림과 0냥 패배 시 `+0냥`으로 보이던 표기를 수정했다.
- `npm run validate:weapons`, `npm run validate:balance`, `npm run validate:adventure`, `npm run lint`, `npm run build` 통과.

### 2026-07-10 시간역행 단일 강화 트리 1차 구현
- 기존 한손/두손 선택 강화 트리를 `K2 소총 → 정조대 월도 → 선조대 장도·쌍수도 → 조선 군영 환도 → 백제 칠지도 → 한국식 동검 → 비파형동검`의 7단계 단일 트리로 교체했다. 오래된 무기일수록 강하다는 설정은 실제 우열이 아닌 대장간의 `역사 공명`이라는 게임 규칙으로 분리했다.
- 강화 확률은 90%에서 시작해 20%까지 단계별로 낮아지고, 판정 직전 모바일 터치·마우스 클릭·키보드 Enter/Space로 누르는 타이밍 바를 추가했다. GOOD은 +3%p, PERFECT는 +7%p를 더한다.
- 실제 강화 실패 시 현재 무기는 +1 K2로 돌아간다. 실패할 때마다 항상 지급하지 않고 단계와 타이밍에 따라 확률적으로 12종의 비전투 괴작 중 하나가 나오며, 괴작은 도감 등록·중복 보유·판매가 가능하다.
- 칭호는 누적 발견 조건 5종과 현재 보유 조건 2종으로 분리했다. 누적 발견 칭호는 괴작을 팔아도 남고, 보유 조건 칭호는 판매로 수량 조건이 깨지면 비활성화된다.
- 복원 무기고에서는 평생 최고 성공 단계의 `-2`까지만 비싼 가격으로 무기를 다시 살 수 있다. 구매 즉시 해당 단계부터 강화하며, 현재 퀴즈 평균 보상 기준으로 필요한 문제 수를 함께 표시한다.
- 7종 무기 이미지를 새로 제작해 투명 배경 1024px PNG로 연결했고, 각 무기 카드에 시대·용도·고증 설명·게임 설정·공식 역사 자료 링크를 분리해 표시한다.
- `npm run validate:weapons`, `npm run lint`, `npm run build` 통과.

### 2026-06-10 자라 후보 배경/엔딩 연출 연결
- `review-main-002`의 새 자라 중심 대본에 맞춰 배경을 세 장 추가 연결했다. 어린 자라 회상은 `flashback-rescue`, 겨울 강가 방랑 결말은 `river-winter`, 빈손으로 돌아가 고백하는 결말은 `palace-confession` 배경을 사용한다.
- 새 배경은 모두 인물과 글상자 없는 래스터 PNG로 `public/images/adventure/backgrounds/`에 배치해, 글상자 좌우에 붙는 용왕/자라/토끼 상반신과 생김새가 충돌하지 않도록 했다.
- `docs/review-main-002-story-review-copy.md`를 현재 런타임 데이터 기준으로 다시 생성했고, `npm run validate:adventure`가 새 배경 키와 마지막 장면 배경 배치를 검사하도록 보강했다.
- `npm run validate:adventure`, `npm run lint`, `npm run build`, `ADVENTURE_EPISODE_ID=review-main-002 ADVENTURE_SMOKE_URL=http://127.0.0.1:5174/ npm run smoke:adventure` 통과. Playwright 캡처로 회상, 겨울 강가 결말, 용궁 고백 결말이 각각 새 배경 파일을 읽는 것도 확인했다.

### 2026-06-10 자라 후보 최신 대본 반영
- `review-main-002`를 사용자가 새로 준 개선본 흐름으로 대체했다. 제목은 `무거운 신뢰의 저울`로 유지하고, 주인공은 자라 시점으로 고정했다. 어린 자라가 용왕에게 `바다의 숨결(용궁 패)`을 받아 살아난 기억을 넣어, 용왕의 명령을 쉽게 거절하지 못하는 이유가 앞에서 보이게 했다.
- 분기는 `obey`/`hesitate`로 토끼를 데려오는 말투를 갈라 놓고, 용궁 대청의 함정 장면에서 `truth`/`shield`로 토끼의 거짓말을 끊을지 받아 줄지 고르게 했다. 토끼를 살려 육지로 돌아온 뒤에는 `exile`/`return`으로 바다의 숨결을 버리고 떠돌지, 용궁에 돌아가 사실대로 아뢸지 닫히게 했다.
- 새 결말은 `냉혹한 충성의 대가`, `징표를 버린 방랑`, `거두어진 숨결` 세 갈래로 정리했다. 원본 개선본의 강한 표현은 사건 의미는 유지하되, 실제 화면에서는 학생도 따라 읽기 쉬운 문장과 미연시식 마지막 글상자로 다듬어 연결했다.
- `docs/review-main-002-story-review-copy.md`를 현재 런타임 데이터 기준으로 다시 생성했고, `npm run validate:adventure`가 새 분기 키와 새 결말명을 검사하도록 갱신했다.

### 2026-06-10 자라 후보 전체 전개 명료도 재점검
- `review-main-002`의 전체 대본을 펼쳐 읽으며 독자가 한 번 더 해석해야 하는 표현과 전개를 정리했다. `낯선 육지 이야기`, `물속 숨길`, `약초 말부터`, `빈 바위를 보고도 모른 척`, `올가미 줄`, `함께 캐 줄 토끼`처럼 상황이 흐릿하거나 갑자기 나온 표현을 직접적인 행동과 조건으로 바꿨다.
- 토끼를 데려오는 설득은 `이 패를 가진 자라만 물속에서 숨 쉬게 도울 수 있다`는 이유로 정리했고, 정직 경로의 약속은 `호위가 다가오면 내가 먼저 막아서겠다`로 바꿔 용궁에서 실제로 자라가 멈춰 세우는 행동과 맞췄다.
- 용궁의 도라지 뿌리 대안은 `의관이 말린 뿌리도 달였지만 어떤 뿌리인지는 확인하지 못했다`는 근거를 붙여, 갑자기 약초가 해결책처럼 튀어나오지 않게 했다. 육지 선택 전 프롬프트도 토끼가 실제로 어디에 서 있고 무엇을 하려는지 보이게 다시 썼다.

### 2026-06-10 자라 후보 회상/명령/초반 개연성 재정리
- 회상 장면을 단순한 물건 전달이 아니라, 큰비 뒤 용왕이 어린 자라를 직접 돌봐 주고 안전한 물문을 알려 준 기억, 그리고 자라가 서쪽 물문에서 새끼 물고기 방 쪽으로 물이 새는 것을 보고하자 용왕이 그 말을 믿어 준 기억으로 바꿨다. 지금 자라가 용왕의 부탁을 쉽게 끊지 못하면서도 거짓말 앞에서 흔들리는 이유가 장면으로 보이게 했다.
- 용왕의 명령은 `물문을 지나면 곁에서 지켜라`처럼 애매한 말이 아니라, 병과 간 이야기를 꺼내지 말고 손님상과 숨 쉴 패만 말해 데려온 뒤 대청에서 호위에게 넘기라는 구체적인 지시로 바꿨다.
- 자라의 반문도 `간 이야기를 빼면 대답이 막힌다`가 아니라, 토끼가 이유를 모른 채 물문을 넘게 되고 자신은 잔칫상 이야기로 둘러대야 한다는 실제 상황으로 바꿨다. 첫 선택 프롬프트와 새벽 재출발 경로도 이 갈등에 맞춰 정리했다.

### 2026-06-10 자라 후보 인물별 말투 2차 정리
- `review-main-002`의 대사를 인물별 위치와 관계에 맞춰 다시 정리했다. 자라는 용왕 앞에서는 `제가/주십시오` 중심의 보고 말투를 쓰고, 토끼 앞에서는 `토끼 선생`, `내가`, `하겠소`로 낮추어 말한다.
- 토끼는 달콤한 말에 바로 넘어가지 않고 이유와 조건을 빠르게 묻도록 했고, 용왕은 병세가 깊어진 왕답게 짧은 명령과 의심으로 압박하게 했다. 의관은 감정 설명보다 증상과 처방 기록을 차례로 보고하는 말투로 맞췄다.
- 검증 기준도 새 표현에 맞춰 갱신해 마지막 장면과 대사 톤이 옛 토끼 시점 문구로 돌아가지 않게 했다.
- `npm run validate:adventure`, `npm run lint`, `npm run build`, `ADVENTURE_EPISODE_ID=review-main-002 ADVENTURE_SMOKE_URL=http://127.0.0.1:5174/ npm run smoke:adventure` 통과.

### 2026-06-10 자라 후보 문장/분기 개연성 정리
- `review-main-002`에서 `마음에 걸렸다`, `시간을 벌었지만`, `약속을 흔들었다`, `말을 만들지 못했다`처럼 감정이나 의미를 요약하던 문장을 줄이고, 빈 약사발, 용궁 패, 바위 밑 흙, 도라지 뿌리, 숲길을 막는 앞발처럼 보이는 행동과 물건으로 바꿨다.
- 토끼를 데려오는 경로에서 토끼가 왜 자라 등에 오르는지 더 분명히 했다. 손님상만 말하는 경로는 배고픔과 물가까지만 가 본다는 조건을 붙였고, 병세를 말하는 경로는 간 요구를 숨기지 않은 뒤 용왕 앞에서 한마디 하게 해 달라는 조건으로 이어지게 했다.
- 공통 용궁 장면은 `손님상만 말해 왔다`는 한 경로 기준 문장을 없애고, 병세를 말한 경로와 손님상을 앞세운 경로가 모두 자연스럽게 이어지도록 `병세를 말하거나 손님상과 물속에서 숨 쉬는 법을 말해 왔다`로 정리했다.
- 거짓말 뒤 육지 선택은 토끼가 이미 간 이야기를 고백한 뒤라는 점에 맞춰 `바위 밑을 다시 뒤지자고 하기`로 바꿨고, 선택 직전 프롬프트도 빈 바위를 다시 뒤질지 도라지 뿌리로 넘어갈지 바로 보이게 정리했다.
- 용궁 반전 직후에는 토끼가 처음 간 이야기를 듣는 듯한 대사를 없애고, `왜 벌써 줄이 들어오는 것이오?`처럼 정직한 경로와 손님상 경로 모두에 맞는 반응으로 바꿨다.
- `npm run validate:adventure`, `npm run lint`, `npm run build`, `ADVENTURE_EPISODE_ID=review-main-002 ADVENTURE_SMOKE_URL=http://127.0.0.1:5174/ npm run smoke:adventure` 통과.

### 2026-06-10 자라 어린 시절 회상 보강
- `review-main-002` 초반 회상을 한 글상자 요약에서 여러 글상자 흐름으로 나눴다. 어린 자라가 큰비 뒤 용궁 대청에 떠내려오고, 젊은 용왕이 쉬게 해 준 뒤, 안전한 물문을 알려 주고 서쪽 물문 누수 보고를 믿어 주는 장면으로 둘 사이의 신뢰가 짧게 보이도록 했다.
- 회상 장면은 `visualMode: flashback`, 젊은 용왕 상반신, 어린 자라 상반신을 유지하며, 배경과 글상자 전체가 낮은 채도/보랏빛 회상 톤으로 보이도록 CSS를 보강했다.
- `npm run validate:adventure`가 회상 글상자 수, 어린 자라/젊은 용왕 variant, 첫 심부름 장면, 회상 화면 스타일을 검사하도록 보강했다.
- `npm run validate:adventure`, `npm run lint`, `npm run build`, `ADVENTURE_EPISODE_ID=review-main-002 ADVENTURE_SMOKE_URL=http://127.0.0.1:5174/ npm run smoke:adventure` 통과.

### 2026-06-10 자라 관점 말투와 검증 기준 재정리
- `review-main-002`의 실제 대사를 `characterVoiceGuide`에 맞춰 다시 다듬었다. 용왕은 병세 때문에 짧지만 왕답게 신뢰와 명령을 섞어 말하고, 자라는 용왕 앞에서는 공손한 보고 말투, 토끼 앞에서는 `토끼 선생`과 하오체를 유지한다. 토끼는 의심을 빠르게 묻고 빈틈을 짚으며, 의관은 증상과 처방 기록을 차례로 보고한다.
- `sanitizeAdventurePreviewText`의 넓은 `대가` 치환이 `갈대가`를 `갈뒤탈`로 망가뜨리던 문제를 좁은 조사형 치환으로 고쳤다.
- `docs/adventure-vn-production-plan.md`와 `CHECKLIST.md`에 남아 있던 토끼 주인공 기준을 현재 자라 주인공 기준으로 정리했다.
- `npm run validate:adventure`, `npm run lint`, `npm run build`, `ADVENTURE_EPISODE_ID=review-main-002 ADVENTURE_SMOKE_URL=http://127.0.0.1:5174/ npm run smoke:adventure` 통과.

### 2026-06-10 용왕 악화 상태 이미지 보강
- `review-main-002`의 용왕 `critical` variant를 더 악화된 상반신 이미지 `adventure_dragonking_critical_worse_attached.png`로 교체했다. 기존 병든 용왕보다 피부색을 더 잿빛/푸른 회색으로 낮추고, 어두운 반점, 식은땀, 처진 눈꺼풀, 기침 연출을 강화해 병세가 급격히 나빠진 장면이 더 분명하게 보이도록 했다.
- 기존 `sick` 용왕과 젊은 용왕 이미지는 그대로 두고, 새 이미지는 새벽 악화 장면과 용궁 함정 장면의 `critical` variant에서 쓰인다.
- `npm run validate:adventure`, `npm run lint`, `npm run build`, `ADVENTURE_EPISODE_ID=review-main-002 ADVENTURE_SMOKE_URL=http://127.0.0.1:5174/ npm run smoke:adventure` 통과.

### 2026-06-09 토끼 후보 인물별 말투 정리
- `review-main-002`에 `characterVoiceGuide`를 추가해 토끼는 빠르게 되묻고 판단하는 말투, 자라는 `토끼 선생`과 하오체를 쓰는 공손한 말투, 용왕은 짧은 명령과 의심으로 압박하는 말투를 기준으로 남겼다.
- 용궁 환대/돌변, 간 거짓말, 약초 설득, 육지 마무리 대사 중 설명문처럼 길어지던 부분을 줄이고, 각 인물이 처한 위치에 맞게 말끝과 호칭을 정리했다.
- `npm run validate:adventure`가 두 번째 검토 후보의 `characterVoiceGuide` 유지 여부도 검사하도록 보강했다.

### 2026-06-09 토끼 후보 용궁 반전 화면 강화
- `review-main-002`의 용왕 상반신이 토끼보다 작아 보이지 않도록 인물 스프라이트 스케일 값을 런타임에서 받을 수 있게 하고, 용왕 기본 스케일을 1.42로 키웠다.
- 용왕 PNG는 원본의 아래 투명 여백 때문에 글상자 위에서 떠 보일 수 있어, 같은 그림을 유지하되 하단 기준점을 글상자 부착형으로 보정한 `adventure_dragonking_command_attached.png`를 기본 자산으로 연결했다. 커진 용왕이 모바일 화면 왼쪽에서 잘리지 않도록 인물별 `sideInset`도 지원한다.
- 용궁 대청 배경을 `palace-welcome`, `palace-trap` 두 단계로 나눴다. 환대 단계는 조개등, 방석, 작은 손님상, 연한 풀잎과 달콤한 뿌리가 보이고, 함정 단계는 어두워진 대청, 치워진 손님상, 바닥의 푸른 줄이 보이게 했다. 임시 도형 합성처럼 보이던 배경은 실제 래스터 PNG 픽셀아트 배경으로 교체했다.
- 용궁 줄거리는 손님 대접이 실제로 보이는 장면 뒤에 용왕의 기침과 의관의 말이 끼고, `그만. 환대는 여기까지다`로 돌변한 뒤 손님상이 치워지는 흐름으로 다시 조였다.
- `npm run smoke:adventure`가 `review-main-002`에서 용왕이 토끼보다 크게 렌더링되는지도 검사하도록 보강했다.

### 2026-06-09 토끼 후보 용궁 환대-돌변 흐름 보강
- `review-main-002`에서 자라 등을 타고 용궁에 도착한 직후를 곧바로 호위가 기다리는 장면이 아니라, 조개등, 시녀, 잎 수건, 방석, 작은 손님상으로 먼저 환대받는 흐름으로 바꿨다.
- 용왕도 처음에는 `육지에서 온 귀한 손님`처럼 맞이하다가, 기침과 의관의 말 뒤에 `환대는 여기까지다`라고 선을 긋고 손님상을 치우게 한다. 이 전환 뒤에 호위의 줄과 토끼 간 요구가 나와 함정이 더 선명하게 보인다.
- 용궁 선택 직전 문장도 방금 전까지 있던 손님상과 방석이 사라지고 줄이 다가오는 상황으로 바꿔, 사용자가 “방금 내가 환대받던 손님에서 묶일 처지가 됐다”는 압박을 바로 느끼게 했다.

### 2026-06-09 토끼 후보 인물 배치/화면 최적화
- `review-main-002`의 일반 대화와 선택 결과 글상자에서도 두 인물 상반신을 글상자 좌우에 유지하도록 렌더링을 보강했다. 말하는 인물은 밝게, 듣는 인물은 어둡게 표시하고, 해설이 같은 대화 흐름 안에 끼면 두 인물을 함께 어둡게 둔다.
- 초반 토끼 혼잣생각처럼 상대가 아직 등장하지 않은 장면은 토끼 단독 또는 배경 중심으로 남겨, 자라가 미리 화면에 붙는 오류를 막았다.
- 런타임은 통합 장면 컷보다 인물 없는 배경과 투명 인물 상반신을 조립하는 방식을 우선한다. 기존 통합 컷은 `runtime: false` 참고 컷으로 유지해 글상자 부착 인물과 장면 이미지의 생김새 불일치를 피한다.
- `npm run smoke:adventure`는 이제 `review-main-002`의 대화/결과/활성 장면 해설에서 두 인물 부착, 화자 강조, 청자 어둡게 표시가 유지되는지 검사한다.

### 2026-06-09 해설/대사/속생각 표시 구분 재정리
- 혼합 글상자는 상단 이름표 하나가 모든 줄을 덮지 않게 바꿨다. 실제 대사 줄에는 작은 인라인 화자 이름표를 붙이고, 행동 묘사와 속생각은 각각 다른 줄 스타일로 분리했다.
- 해설 이름표는 인물 대사 이름표처럼 보이지 않도록 투명 배경의 작은 서술 표식으로 낮췄고, 마지막 장면의 해설 beat에는 화자 이름표를 숨겼다.
- `review-main-002`에서 대사 뒤 행동 묘사가 같은 `나` 글상자에 붙던 거절 결과를 분리하고, 용궁 탈출 결과의 해설 안에 있던 생각 표현을 행동 묘사로 낮췄다.

### 2026-06-09 두 번째 후보 엔딩 글상자 흐름화
- 마지막 장면이 한 문단으로 빨리 종합되어 끝나지 않도록 `finalScene.beats`를 지원하게 했다. 엔딩 화면에서는 `이전`/`다음`으로 마지막 글상자를 넘겨 읽고, 마지막 beat 뒤에만 `대장간 정산`과 복귀 버튼이 보인다.
- `review-main-002`의 풀밭 거절, 거짓말 뒤 도망/약초/사실 고백, 설득 뒤 도망/약초/의논 결말을 모두 3개 이상의 엔딩 글상자로 나눴다.
- 초반 토끼 대사 중 `내 발이 닿을 땅인지, 물길인지`처럼 비유적으로 읽히는 표현은 `등에 오르기 전에 어디까지 가는 길인지`처럼 직접적인 질문으로 바꿨다.

### 2026-06-09 두 번째 후보 선택 압박 문장 직접화
- `review-main-002`의 첫 선택 직전 문장에서 `내 발로 디딘 땅`, `내 몸을 물길에 맡기는 일`처럼 상징이 강한 표현을 줄였다.
- 토끼가 아는 숨을 바위, 도라지꽃 언덕, 물속 길에서는 마음대로 내릴 수 없다는 조건을 직접 말해, 학생이 선택의 위험을 바로 이해할 수 있게 했다.
- 풀밭에 남는 결과 대사도 `내 발까지 맡기지는 않겠소` 대신 물속 길에서 내리고 싶을 때 못 내리는 것이 싫다는 말로 바꿨다.

### 2026-06-09 두 번째 후보 엔딩 장면성과 화자 라벨 보강
- 앱 화면에 보이는 주인공 이름표를 `주인공`이 아니라 `나`로 표시하도록 바꿨다. 내부 데이터 키와 이미지 연결은 계속 `주인공`을 사용해 토끼 상반신 에셋 매칭이 흔들리지 않게 했다.
- `review-main-002`의 마지막 장면 본문을 결과 요약처럼 읽히는 문장 묶음에서, 물가와 숲길, 도라지꽃 언덕 앞에서 마지막 말과 행동이 이어지는 미연시식 엔딩 장면으로 다시 썼다.
- 풀밭에 남는 결말은 조개껍데기 닫히는 소리와 `내일은 도라지꽃 언덕부터 올라가자`는 작은 말로 닫고, 육지 결말들은 자라의 마지막 대사, 발목 자국, 약초 꾸러미 매듭, 용왕에게 할 말 연습이 실제 장면 안에서 이어지게 했다.

### 2026-06-09 모험 마지막 장면 미연시 엔딩 화면화
- 모험 마지막 장면을 큰 결과 카드가 아니라 흐린 배경, 글상자에 붙은 인물 상반신, 하단 엔딩 대사창으로 보이는 VN형 마지막 화면으로 바꿨다.
- `review-main-002`의 선택별 마지막 장면에 배경 위치와 토끼/자라 상반신 배치를 명시해, 풀밭 거절 엔딩은 토끼 단독, 육지 마무리 엔딩은 토끼와 자라가 같은 화면 안에 남도록 했다.
- 엽전 정산과 대장간 복귀 버튼은 마지막 장면 본문과 섞지 않고 보조 하단 영역으로 낮췄으며, `npm run smoke:adventure`가 새 VN형 마지막 화면, 배경, 인물 상반신, 정산 분리를 검사하도록 갱신했다.

### 2026-06-09 두 번째 후보 선택 결과 묘사 보강
- `review-main-002`의 선택 직후 결과 글상자를 보강해, 각 결과가 선택 행동 확인, 상대 반응, 장소/물건 변화, 주인공 판단으로 자연스럽게 이어지도록 했다.
- 풀밭에 남기, 자라 등에 올라타기, 용궁에서 거짓말하기/설득하기, 육지에서 도망치기/약초 찾기/자라와 의논하기 결과가 모두 6개 안팎의 글상자로 충분히 읽히게 했다.
- 결과 본문에는 엽전, 손실, 정산 같은 게임식 표현을 넣지 않고, 사건 전개와 마지막 장면이 선택의 여파로 납득되도록 유지했다.

### 2026-06-09 지난 기록 대본형 화면 구성
- `지난 기록` 팝업을 항목 카드 목록이 아니라 연극대본처럼 읽히는 화자명/본문 2열 구성으로 바꿨다.
- 혼합 글상자도 기록 안에서는 지문, 실제 대사, 주인공 속생각이 줄 단위로 풀려 읽히며, 지문은 낮은 톤, 대사는 선명한 톤, 속생각은 괄호와 기울임을 유지한다.
- 문장 표시 기준을 `대사`, `description 지문`, `thought 속생각`, `해설` 네 갈래로 고정했다. 모호한 `type: 'text'` 지문은 `type: 'description'`으로 바꿨고, 검증 스크립트가 다시 들어오지 못하게 막는다.
- `npm run smoke:adventure`가 지난 기록의 대본형 줄 구조와 화자/본문 grid를 검사하도록 보강했다.

### 2026-06-09 모험 글상자 묘사/대사 분리 및 모바일 기준 보강
- 혼합 글상자 안에서 실제 대사가 아닌 행동 묘사나 장면 묘사가 대사처럼 붙어 보이지 않도록 `adventure-description-segment` 렌더링과 전용 스타일을 추가했다.
- 자라 첫 등장처럼 인물 이름표가 붙은 글상자에서도 묘사는 낮은 톤과 작은 크기, 실제 대사는 더 또렷한 본문으로 분리되어 읽히게 했다.
- 휴대폰 크기의 모바일 화면은 세로 플레이 친화를 우선하고, 태블릿은 가로 플레이가 유리하더라도 세로 플레이를 막지 않는 기준을 문서와 체크리스트에 반영했다.
- `npm run validate:adventure`, `npm run lint`, `npm run build`, `ADVENTURE_EPISODE_ID=review-main-002 ADVENTURE_SMOKE_URL=http://127.0.0.1:5174/ npm run smoke:adventure`가 통과했다.

### 2026-06-09 모험 미연시 기준 문서 정리
- 모험 미연시 제작 기준이 여러 문서에 늘어난 상태를 정리하기 위해 `docs/adventure-vn-guideline-index.md`를 새 입구 문서로 추가했다.
- 새 입구 문서는 현재 검토 후보 2편, 승인 완료 0편, 아카이브 초안 분리 원칙, 원작 기반 각색 기준, 미연시 화면 기준, 필수 산출물, 수정 순서, 검증 명령을 한눈에 정리한다.
- `docs/adventure-vn-story-standards.md`는 이야기 품질의 상세 기준, `docs/adventure-story-writing-workflow.md`는 작성 절차, `docs/adventure-vn-production-plan.md`는 현재 상태와 검증 경로, `CHECKLIST.md`는 QA 항목으로 역할을 나누었다.
- 과거 1편 후보 기준으로 남아 있던 검증 경로와 현재 런타임 풀 문구를 `review-main-001`, `review-main-002` 두 후보 체계에 맞게 정리했다.
- `npm run validate:adventure`는 이제 기준 정리본이 주요 문서 역할, 필수 산출물, 현재 후보 상태를 유지하는지도 검사한다.

### 2026-06-09 원작 기반 미연시 각색 기준 강화
- 앞으로 전래동화, 전설, 고전소설, 명작 소설 각색형은 원작을 읽지 않은 학생에게 원작으로 들어가는 첫 관문이 되도록 기준을 강화했다.
- 원작 전체를 요약하지 않고 대표 장면, 인물 관계, 핵심 갈등, 원작의 맛과 교훈을 플레이 가능한 한 사건으로 바꾸는 방식을 기준으로 삼았다.
- 원작 이름만 빌린 다른 장르나 보상 퀘스트로 흐르지 않게 하고, 교훈은 설명문이 아니라 선택 결과 속 행동, 물건, 관계 변화로 느끼게 한다.
- `review-main-002`에는 `literaryAdaptationPlan`을 추가해 토끼전/수궁가를 모르는 학생도 토끼와 자라, 용궁 초대, 간 요구, 말재주, 육지 귀환의 맛을 체험하도록 점검 기준을 남겼다.
- `npm run validate:adventure`는 이제 원작 기반 각색 기준과 두 번째 검토 후보의 `literaryAdaptationPlan`이 유지되는지 검사한다.

### 2026-06-08 모험 미연시 에셋 분리 기준 확립
- 앞으로 미연시형 모험 이미지는 `배경 이미지`, `투명 배경 인물 상반신`, `앱 글상자 UI`, `특수 장면 컷`으로 분리해 제작하도록 기준을 확립했다.
- 글상자는 이미지 생성 대상이 아니라 앱 UI/CSS 컴포넌트로 유지한다. 이름표, 본문, `지난 기록`, `다음` 버튼, 모바일 줄바꿈은 앱이 책임진다.
- 배경 이미지는 인물과 글상자가 빠진 장소 에셋으로 만들고, 인물 상반신은 투명 배경 독립 에셋으로 만들어 런타임에서 좌우, 밝기, 크기, 화자 상태를 조절한다.
- `review-main-002`에는 `assetProductionPlan`을 추가해 솔숲 풀밭/용궁 대청/육지 모래톱 배경, 토끼/자라/용왕 상반신, 표정 세트, 특수 장면 컷 사용 기준을 분리했다.
- `npm run validate:adventure`는 이제 에셋 분리 기준과 두 번째 검토 후보의 `assetProductionPlan`이 유지되는지 검사한다.

### 2026-06-08 모험 미연시 화면 구성/글상자 기준 강화
- 미연시 화면을 흐린 배경, 인물 상반신, 하단 글상자, 선택/기록 조작 레이어의 네 층으로 설계하도록 기준을 강화했다.
- 하단 글상자는 화면 아래 고정된 읽기 영역으로 두고, 이름표, 본문, `지난 기록`, `다음` 버튼 위치가 장면마다 흔들리지 않게 해야 한다는 기준을 추가했다.
- 인물 상반신은 글상자 위쪽 테두리에 붙되, 얼굴, 손, 중요한 소품이 본문 안전 영역을 가리지 않도록 했다.
- 선택 팝업은 중앙에 뜨더라도 글상자와 인물 얼굴을 동시에 가리지 않고, 선택 후에는 같은 하단 글상자 문법으로 돌아오도록 정리했다.
- `docs/adventure-vn-production-plan.md`, `docs/adventure-vn-story-standards.md`, `docs/adventure-story-writing-workflow.md`, `CHECKLIST.md`에 새 기준을 반영했다.
- `npm run validate:adventure`는 이제 네 층 화면 구조, 본문 안전 영역, 선택 팝업 겹침 방지, 모바일 겹침 방지 기준이 문서에 남아 있는지 검사한다.

### 2026-06-08 모험 대화 중심 스토리 기준 강화
- 앞으로 새 모험을 생성하거나 기존 후보를 고칠 때는 대화 중심 흐름을 먼저 설계하도록 기준을 추가했다.
- 주요 장면은 상대의 제안, 주인공의 되묻기나 반박, 상대의 회피나 인정, 선택 압박이 대사로 이어지게 하고, 해설은 장소, 물건, 표정, 거리 변화만 짧게 잇도록 정리했다.
- `review-main-002`에는 `dialogueFocus`와 `dialoguePlan`을 넣어 1막 토끼와 자라, 2막 토끼와 용왕, 3막 육지의 토끼와 자라 대화가 장면을 밀도록 했다.
- `npm run validate:adventure`는 이제 두 번째 검토 후보가 대화 중심 구조와 `dialoguePlan`을 유지하는지, 문서와 체크리스트에 새 생성 기준이 남아 있는지 검사한다.

### 2026-06-08 모험 미연시 인물 연출 기준 강화
- 앞으로 전래동화, 전설, 고전소설 각색형은 순수 1인칭 카메라가 아니라 사용자가 이야기 속 등장인물이 되어 체험하는 방향을 기본으로 삼도록 기준을 정리했다.
- 대화 장면은 `배경 + 글상자 부착형 인물 상반신 + 하단 글상자`를 우선하고, 해설/장소 진입/함정 공개/결말처럼 장면 묘사가 핵심일 때만 장면 컷을 크게 쓰는 방식으로 고정했다.
- 두 인물이 대화하면 좌우 위치를 안정적으로 유지하고, 말하는 쪽은 원래 밝기와 선명도, 듣는 쪽은 어둡게 또는 낮은 채도로 보여 화자 전환을 표현하도록 했다.
- 주인공도 토끼, 홍길동, 심청처럼 원전 인물로 정해진 경우에는 글상자에 붙은 큰 인물 상반신으로 등장할 수 있게 했다. 속생각은 같은 주인공이라도 입을 다문 표정, 낮은 시선, 어두운 톤으로 대사와 구분한다.
- `docs/adventure-v2-criteria.md`, `docs/adventure-vn-production-plan.md`, `docs/adventure-vn-story-standards.md`, `docs/adventure-story-writing-workflow.md`, `CHECKLIST.md`에 기준을 반영했다.
- `npm run validate:adventure`는 이제 글상자 부착형 인물 연출, 좌우 고정, 말하는 쪽/듣는 쪽 밝기 차이, 해설 장면의 배경 우선, `visualStaging` 작성 절차가 문서에 남아 있는지 검사한다.

### 2026-06-08 모험 두 번째 후보 상단 장소/시간 라벨 정리
- `review-main-002`의 모험 화면 왼쪽 상단 라벨을 더 직접적인 장소/시간 표현으로 정리했다.
- 첫 장면 시간은 `해 질 무렵` 대신 `저녁, 해가 지기 전`으로 바꿨다.
- 용궁 장면의 `푸른 물빛이 흔들리는 때`는 `용궁에 도착한 직후`로, 육지 장면의 `거짓말이 남은 새벽`과 `약속을 지켜야 하는 새벽`은 각각 `새벽, 육지에 돌아온 직후`, `새벽, 약초를 찾기 전`으로 바꿨다.
- 마지막 장면의 시간 라벨도 `숲길에 달빛이 든 뒤`, `새벽 물안개가 오른 뒤`, `햇살이 물가에 번진 뒤`, `숲그늘이 깊어진 뒤` 같은 비유적 표현 대신 `새벽, 숲길로 달아난 뒤`, `새벽, 도라지 뿌리를 건넨 뒤`, `아침, 물가에 앉은 뒤`처럼 실제 상황을 바로 보여 주는 말로 바꿨다.
- `npm run validate:adventure`는 이제 두 번째 후보의 상단 장소/시간 라벨과 마지막 장면 시간 라벨이 직접적인지 검사한다.

### 2026-06-08 모험 두 번째 후보 마지막 장면 구체화
- `review-main-002`의 선택별 마지막 장면에서 `쉬운 약속은 물길 너머로 사라졌고`, `설명하지 않은 거짓말만 남았다`, `내가 꺼낸 약초 말도 그곳에 그대로 남았다`, `약속은 빈말이 아니라`처럼 분위기나 추상 명사로 닫히는 문장을 제거했다.
- 거절 결말은 자라가 물가로 돌아간 뒤 남은 젖은 자국, 직접 찾은 풀잎, 바위 뒤 잠자리, 다음날 도라지꽃 언덕으로 올라갈 계획으로 닫히게 했다.
- 거짓말 뒤 도망 결말은 자라가 바위 밑에 간이 없다는 말을 듣지 못한 채 물가에 남고, 토끼가 다친 뒷발을 끌며 숲 안쪽으로 들어가는 장면으로 구체화했다.
- 거짓말 뒤 약초 결말은 간 이야기가 거짓말이었다는 고백, 도라지 뿌리 꾸러미 매듭, 자라가 용왕 앞에서 할 말을 외우는 장면으로 정리했다.
- 사실 고백/약속 이행/의논 결말도 자라가 실제로 붙잡지 않거나, 꾸러미를 등에 묶거나, 세 번 말을 연습한 뒤 도라지꽃 언덕에서 뿌리와 잎을 준비하는 행동으로 닫히게 했다.
- `npm run validate:adventure`는 이제 두 번째 후보 마지막 장면에 위 구체 장면들이 남아 있는지와 모호한 분위기식 결말 문구가 돌아오지 않는지 검사한다.

### 2026-06-08 모험 글상자 공통 레이아웃 고정
- 모험 하단 글상자는 해설, 인물 대사, 주인공 생각, 선택 결과 모두 같은 위치, 폭, 패딩, 상단 이름표 줄을 쓰도록 정리했다.
- 역할별 차이는 박스 전체 배경을 바꾸는 방식이 아니라 왼쪽 강조선과 이름표 색으로만 보이게 해, 읽는 중 박스가 흔들리거나 다른 UI처럼 느껴지지 않게 했다.
- `지난 기록` 버튼은 절대 위치로 떠 있지 않고 글상자 상단 오른쪽 칸에 고정되도록 바꿨다. 주인공 생각처럼 이름표가 없는 글상자에서도 같은 오른쪽 칸을 유지한다.
- `npm run validate:adventure`가 공통 글상자 grid, 왼쪽 강조선, 상단 이름표/지난 기록 행, 해설 이름표의 공통 규격을 검사한다.

### 2026-06-08 모험 두 번째 후보 결말 완성도 보강
- `review-main-002`의 일곱 결말을 다시 감사해, 선택지가 “있으니까 둔 경로”처럼 보이지 않도록 각 결말의 마지막 행동을 구체화했다.
- `사실을 말한 물가`, `자라와 함께 고른 물가`, `약초 꾸러미가 물길을 건넜다`, `거짓말이 물길을 되돌렸다`처럼 장소나 비유로 결과를 흐리는 제목을 제거하고, `토끼는 간이 바위 밑에 없다고 말했다`, `토끼와 자라는 용왕에게 할 말을 정했다`, `자라는 도라지 뿌리를 들고 돌아갔다`, `용왕은 토끼를 다시 육지로 보냈다`처럼 누가 무엇을 했는지 바로 보이는 제목으로 바꿨다.
- 중간 결과 제목도 `토끼는 차분히 말했다`, `토끼는 물가에 멈췄다` 같은 낮은 정보량 문구 대신 `토끼는 간 대신 도라지 뿌리를 말했다`, `토끼는 약초를 찾기 전에 멈췄다`처럼 장면 행동이 보이는 문장으로 정리했다.
- 초반 거절 결말은 단순히 풀밭에 남는 데서 멈추지 않고, 해가 뜨면 도라지꽃 언덕을 살피기로 하는 능동적인 마무리로 보강했다.
- 거짓말 뒤 도망 결말은 `바위 밑이 비어 있다는 사실을 말하지 않은 채` 떠나는 것으로 닫아, 선택의 찜찜함과 원인이 더 분명하게 남도록 했다.
- 거짓말 뒤 약초 결말은 자라가 `토끼의 간은 가져오지 않았습니다. 대신 토끼를 해치지 않는 약을 가져왔습니다`라고 연습하게 해, 거짓말에서 실제 대안으로 넘어가는 정리가 보이게 했다.
- 사실 고백 결말은 도라지 뿌리를 캐고 간 이야기는 지어낸 말이었다고 밝히기로 하는 다음 행동을 붙여, 물가에 앉는 분위기 결말로 흐르지 않게 했다.
- 설득 뒤 도망/약초/의논 결말은 각각 `약초 말을 남기고 떠남`, `먼저 이 약부터 보여 주겠다는 자라의 말`, `토끼의 간보다 먼저 약을 찾아보겠다는 자라의 첫 말`로 다르게 닫히도록 분리했다.

### 2026-06-08 모험 두 번째 후보 감정선/설득력 보강
- `review-main-002`의 도라지 약초 선택이 장면 안에서 설득되도록, 1막 풀밭에 `물가 위쪽 보랏빛 도라지꽃`을 먼저 배치했다.
- 자라의 달콤한 말에는 `패를 만지작거림`, `시선이 내려감`, `등껍질과 앞발의 떨림`을 붙여 단순 악역이 아니라 왕명에 눌린 심부름꾼으로 읽히게 했다.
- 용왕 앞 설득은 `병을 고치려다 토끼 몸이 망가지면 아픈 자리가 하나 더 생긴다`는 논리와, 이미 본 도라지꽃을 근거로 한 대안 제시로 정리했다.
- 육지 결말에서는 `자라를 버리고 달아났을 때 남는 찜찜함`, `거짓말 뒤에 실제 약초를 건네는 책임`, `왕명보다 자기 말로 생각하기 시작하는 자라`가 선택별로 분명히 남도록 다듬었다.
- 자동 검증은 `도라지 뿌리` 선택지, 자라의 감정 단서, 용왕 앞 설득 근거, 모호한 결말 문구 차단을 계속 확인한다.
- 자동 검증은 각 결말이 풀밭 계획, 설명하지 않은 거짓말, 도라지 뿌리 꾸러미, 사실 고백 뒤의 다음 행동, 약속을 버린 흔적, 자라의 첫 말 연습으로 서로 다르게 닫히는지도 확인한다.

### 2026-06-08 모험 인물별 대사 색 구분
- 모험 글상자의 등장인물 이름표에 인물별 색 구분을 추가했다.
- `주인공`, `자라`, `용왕`, `주모`, `검은 갓의 선비`, `동네 주민`, `훈장님`, `떠돌이 의원` 등 반복 인물은 안정적인 색 톤을 쓰고, 새 인물은 이름 기반 fallback 색을 받는다.
- 현재 글상자, 혼합 글상자 안의 인물명, `지난 기록` 팝업의 이름표가 같은 색 기준을 쓰도록 맞췄다.
- 해설은 계속 낮은 회색 톤으로 남겨 인물 대사처럼 보이지 않게 유지했다.
- `npm run validate:adventure`에서 인물별 색 클래스와 CSS 토큰이 유지되는지 검사한다.

### 2026-06-08 모험 두 번째 후보 3막 세트 재작성
- 두 번째 검토 후보 `review-main-002`를 `달콤한 유혹의 숨은 값`으로 재작성했다.
- 이 후보는 주인공이 토끼다. 초반 글상자에서 `나는 토끼의 몸으로 이 풀밭에 서 있었다`고 분명히 밝히고, 솔숲 아래 풀밭에서 허기를 달래던 토끼가 자라의 용궁 제안을 듣는 장면으로 시작한다.
- 1막 선택지는 `수상한 약속을 거절하기`, `자라 등에 올라타기`다. 거절하면 풀밭 결말로 닫히고, 따라가면 용궁 대청으로 이어진다.
- 2막 용궁 대청에서는 `간을 육지에 두고 왔다고 둘러대기`, `기침 약초를 찾아오겠다고 말하기`를 고른다. 거짓말을 고르면 `새벽, 육지에 돌아온 직후`, 약초 말을 꺼내면 `새벽, 약초를 찾기 전`의 육지 모래톱 장면으로 이어진다.
- 거짓말 뒤 육지 모래톱에서는 `바위 뒤로 가는 척하고 숲으로 뛰기`, `빈 바위 대신 약초 찾기`를 고른다. 설득 뒤 육지 모래톱에서는 `약속을 버리고 숲으로 달아나기`, `도라지 뿌리 캐서 맡기기`를 고른다. 선택지는 꼭 세 개를 맞추지 않고, 장면 안에서 실제로 의미 있는 행동만 남긴다.
- 육지 선택의 결말은 `토끼는 거짓말을 숨기고 도망쳤다`, `토끼는 도라지 뿌리를 건넸다`, `토끼는 약초 약속을 버리고 달아났다`, `자라는 도라지 뿌리를 들고 돌아갔다`로 각각 닫힌다.
- 교훈은 설명문으로 직접 말하지 않고 장면 결과로 느끼게 조정했다. 쉽게 얻는 약속은 `물속 길에서 마음대로 내릴 수 없는 상황`, 살기 위한 거짓말은 빈 바위 앞의 선택과 숲길의 발목 자국, 책임 있는 선택은 `도라지 뿌리 꾸러미`처럼 선택 뒤 남는 물건과 행동으로 드러난다.
- `잔치 말만 길게 남았다`, `강바람에 흩어졌다`, `처방전은 말보다 먼저 들켰다`처럼 모호하거나 예전 자라 시점에 묶인 문구를 새 후보 본문에서 제외했다.
- `npm run validate:adventure`에서 두 번째 후보가 토끼 시점, 1막-2막-3막 단계 선택, 선택별 결과 글상자, 한국 자료 근거, 이미지 필요 기록을 유지하는지 검사한다.

### 2026-06-08 모험 두 번째 검토 후보 추가
- 두 번째 검토 후보는 이솝식 경주가 아니라 한국 고전 `토끼전/수궁가`의 토끼와 자라 모티브를 쓴다.
- 새 이미지는 아직 생성하지 않았다. 대신 솔숲 아래 풀밭, 용궁 대청, 육지 모래톱, 도라지꽃, 약초 꾸러미, 물가 쉼터, 토끼와 자라의 연속성 기준과 중국풍/일본풍/이솝 경주 트랙 금지 기준을 기록했다.
- 앱은 모험 시작 때 `review-main-001`, `review-main-002` 중 한 후보를 랜덤으로 고를 수 있고, 스모크 검증용으로는 `?adventureEpisode=...` 쿼리로 특정 후보를 강제로 열 수 있다.

### 2026-06-07 모험 첫 후보 이미지 생성 프롬프트 구체화
- `review-main-001`은 계속 `review-candidate`로 유지하고, 사용자 승인 완료 에피소드는 0개로 유지했다.
- 새 이미지는 아직 생성하지 않았다. 대신 `imageScenes`의 6개 컷에 실제 생성용 `generationPrompt`와 `negativePrompt`를 추가해, 줄거리 요약이 아니라 구도, 인물 위치, 물건 위치, 표정, 손동작, 시간대, 금지 요소를 기준으로 이미지를 만들 수 있게 했다.
- 검은 갓의 선비, 주모, 낮은 상, 검은 사발, 빈 사발, 국밥, 검은 장부, 붓끝 먹물방울, 놋쇠 문고리, 젖은 짚신이 컷마다 같은 인물/장소/물건처럼 이어지도록 연속성 기준을 유지했다.
- 이미지 프롬프트는 중국풍 객잔, 일본풍 주점, 사무라이, 도사복, 술상, 현대 옷, 장면에 없는 무기, 예전 결말 흔적인 `오늘 밤은 아니다` 글씨와 `검은 갓끈`을 금지 요소로 명시한다.
- 검토용 읽기본, 미연시 이야기 기준, 승인 감사, 체크리스트, 카탈로그 검증을 이미지 생성 사양 기준에 맞게 동기화했다.
- 검증: `npm run validate:adventure`, `ADVENTURE_SMOKE_URL=http://127.0.0.1:5174/ npm run smoke:adventure`, `npm run lint`, `npm run build`, `git diff --check` 통과.

### 2026-06-07 모험 첫 후보 선택 C 표현 명확화
- `review-main-001`은 계속 `review-candidate`로 유지하고, 사용자 승인 완료 에피소드는 0개로 유지했다.
- 첫 후보 선택지는 현재 `선비의 검은 사발을 엎기`, `주모의 빈 사발에 국밥을 덜기`, `서리 낀 문고리를 잡고 밖으로 나서기`다.
- 선택 A는 먹물이 이름처럼 보이는 글씨에 닿지 못하고 엎어진 사발 바닥에 떨어져 장부가 닫히는 흐름으로 정리했다.
- 선택 B는 국밥을 받은 선비가 붓끝을 내리고, 이름처럼 보이던 글씨가 흐려져 맞은편 자리가 비는 흐름으로 정리했다.
- 선택 C는 `길은 되돌아왔다` 같은 모호한 표현을 제거했다. 이제 첫 굽이를 돌았을 때 눈앞에 다시 주막 처마가 보이고, 아침에 확인한 젖은 짚신 자국이 산길이 아니라 주막 마당 안에만 남았다는 사실로 닫힌다.
- 런타임 데이터, 검토용 읽기본, 승인 감사, 스모크 테스트, 카탈로그 검증의 기대 문구를 같은 기준으로 동기화했다.
- 검증: `npm run validate:adventure`, `ADVENTURE_SMOKE_URL=http://127.0.0.1:5174/ npm run smoke:adventure`, `npm run lint`, `npm run build`, `git diff --check` 통과.

## 주요 완성 기능

### 무기 강화 시스템
- [x] 엽전 벌기 구구단, 모험 게이지 나눗셈 퀴즈 분리
- [x] 무기 강화 (+1 ~ +7)
- [x] 강화 실패 시 +1 낡은 몽둥이 리셋
- [x] 강화 시각 효과 (긴 망치질, 판정 대기, 불꽃 파티클, 타격 텍스트, 성공/실패 플래시)
- [x] 자정 신문 보고 시스템 (getMidnightNews)

### 팔도 모험 시스템
- [x] 내부 최대 조우 수 기반 모험 진행
- [x] 단계 번호와 쪽수 대신 현재 장면 중심의 비주얼 노벨형 화면 구성
- [x] 제목/대표 이미지/하단 글상자 중심의 전체 화면 모험 장면
- [x] 선택지는 하단 글상자 안이 아니라 화면 중앙 팝업으로 표시
- [x] 선택 직후 결과는 중앙 팝업이 아니라 하단 결과 글상자로 이어지도록 변경
- [x] 선택지 버튼은 세로 1열 배열과 차분한 동일 계열 색상으로 정리
- [x] 짧은 사이드 장면은 매번 선택지를 강요하지 않고, 읽고 지나가는 장면으로도 진행
- [x] 지난 기록 확인을 작은 글상자 내부 목록이 아니라 전체 팝업형 이야기 다시 읽기로 변경
- [x] 상단 왼쪽은 사건명 대신 장소와 시각을 보여 주는 비스포일러 라벨로 변경
- [x] 모험 도중에도 선택지와 분리된 홈 버튼으로 대장간 화면 복귀 가능
- [x] 마지막 장면 카드와 별도 대장간 정산 패널
- [x] 현재 에피소드의 지난 기록 확인
- [x] 전투 로그 및 속보 스토리 다양화 (성공/실패별로 각 3가지 랜덤 기사 연동 완료)
- [x] 전투 장면 전장 레이어 강화 (단계별 배경, 아군 무기, 적 토큰, 충돌 상태, 성공 확률 게이지)
- [x] 랜덤 장면 적 픽셀 이미지 7종 추가 및 전장 렌더링 연동
- [x] 모험이 길어질수록 강한 적 등장 확률이 높아지는 가중치 기반 장면 시스템
- [x] 모험 초반에도 강한 적이 낮은 확률로 등장하는 완화형 랜덤 장면 시스템
- [x] 모험 중 동네 주민, 훈장님, 떠돌이 의원 같은 인연 장면 추가
- [x] 모험 중 보물상자, 상단 호위, 산신 제단, 통행세, 진흙 협곡 등 비전투 이벤트 추가
- [x] 현재 런타임 모험 선택 풀은 `src/data/adventureApprovedStories.js`의 승인본/검토 후보만 사용
- [x] 기존 80/200/50 V2 카탈로그, 시드 초안, 예전 전투 적 풀은 `archive/adventure-draft-pool/` 보관 자료로 분리하고 승인본으로 사용하지 않음
- [x] 검토 후보 `review-main-001`, `review-main-002` 두 편을 `review-candidate`로 연결하고, 사용자 승인 완료 작품은 아직 0개로 유지
- [x] 첫 검토 후보 `검은 갓의 사발`은 `first-complete-draft` 단계로 정리했고, 14개 글상자, 3개 실제 행동 선택지, 선택별 결과, 선택별 마지막 장면, 이미지 필요 컷과 연속성 기준을 갖춤
- [x] 두 번째 검토 후보 `달콤한 유혹의 숨은 값`은 `three-act-story-draft` 단계로 정리했고, 토끼 시점 1막 풀밭 선택, 2막 용궁 선택, 3막 육지 모래톱 선택과 선택별 마지막 장면, 계획 이미지 컷과 연속성 기준을 갖춤
- [x] 첫 검토 후보는 짧은 문장과 문장 사이 연결성, 인물 표정/감정 묘사, 같은 인물/배경/물건 이미지 연속성 기준을 문서화
- [x] 매 모험마다 독립 단편 사건이 랜덤으로 등장하도록 중복 방지형 장면 풀 추가
- [x] 서술, 등장인물 대사, 주인공 속생각, 선택지를 구분하는 비주얼 노벨식 글상자 추가
- [x] 주인공 속생각은 역할 배지 대신 괄호와 기울임으로 일반 대사와 구분
- [x] 장터/보부상/봉수/나루/서당/홍길동전 등 과거 대량 초안 소재는 승인본 후보가 아니라 아카이브 검토 자료로만 유지
- [x] `npm run validate:adventure`가 기존 초안 미사용, 첫 후보 1편 연결, 사용자 승인 전 `approved` 금지, 검토용 읽기본/승인 감사 동기화를 검사
- [x] `npm run smoke:adventure`가 첫 후보의 글상자, 지난 기록, 세 선택 결과 글상자, 선택별 마지막 장면, 데스크톱/모바일 선택지 배치를 검사
- [x] 첫 후보 최신 문장 수리: 해설의 직접 설명을 줄이고 `맞은편 자리는 분명 비어 있지 않았다`, `주모의 시선은 그 자리를 그냥 지나쳤다`, `주모는 선비를 보지 못했다`처럼 주인공이 본 순서로 선비의 기묘함을 확인하게 조정
- [x] 첫 후보 최신 문장 수리: `선비의 눈매는 얇은 얼음처럼 움직이지 않았다`, `사발 바닥에는 오래 마른 먹자국이 둥근 고리처럼 남아 있었다`, `꼭 내 이름은 아니었다. 그래도 낯선 이름이라고 넘기기 어려웠다`, `손을 뻗을 시간은 한 번뿐이었다`처럼 짧고 분명한 문장으로 긴장과 판단을 보강
- [x] 첫 후보 최신 결과 수리: 검은 사발 선택은 `선비의 눈매가 살짝 좁아졌다. 입가는 그대로였다`로 표정 반응을 보이고, 문고리 선택은 `처마 아래 젖은 짚신들도 아까 본 자리 그대로였다`로 같은 주막으로 돌아온 느낌을 더 구체화
- [x] 모험 난이도 상향 조정 (강화 보정 +5%, 성공 확률 5%~82% 캡)
- [x] 모험 패배/위험 사건 시 보상 또는 보유 엽전 손실 가능성 추가
- [x] 모험 패배/위험 사건 시 무기 손상 위험은 +1 리셋이 아니라 강화 단계 하락으로 변경
- [x] 모험 5분 쿨타임 및 퀴즈 5문제 정답 기반 모험 게이지 추가
- [x] 기존 시나리오/이미지/시드 초안 문서를 `archive/adventure-draft-pool/`로 분리
- [x] 모험 미연시 화면/시점/선택지/한 편씩 제작 기준 문서 추가 (`docs/adventure-v2-criteria.md`, `docs/adventure-vn-production-plan.md`)
- [x] 새 모험 에피소드는 장편식 연재가 아니라 한 문장 질문이 같은 편 마지막 장면에서 답나는 단편으로 제작하도록 기준 고정
- [x] 승인본 전용 런타임 파일 추가 (`src/data/adventureApprovedStories.js`)

### 효과음
- [x] Web Audio 기반 효과음 시스템 추가 (별도 음원 파일 없이 합성)
- [x] 퀴즈 보상/오답, 강화 망치질, 강화 성공/실패, 모험 시작, 전투 충돌, 페이지 넘김 효과음 연동
- [x] 헤더 효과음 켜기/끄기 토글 및 localStorage 저장 연동

### 무기 도감
- [x] 한손/양손 무기 탭 전환
- [x] 수집한 무기 잠금/해제 표시
- [x] 무기 상세 설명 패널

### 이순신 액션 스프라이트
- [x] `knol-jump` 이순신 캐릭터를 정체성 기준으로 한 한손/두손 액션 시트 생성
- [x] 대기, 걷기 2컷, 점프, 낙하, 베기 3컷, 찌르기 3컷의 11프레임 구성 정의
- [x] 4x3 생성 원본, 투명 contact sheet, 11컷 가로 시트, 개별 256px 프레임, 매니페스트 저장
- [x] 인물 키, 머리/얼굴/갓 크기, 몸통/사지 비율의 엄격 검수 통과
- [x] 동일 무기 유지, 손잡이/자루 파지, 칼날 파지 금지, 무기 궤적 기반 이펙트 검수 통과
- [x] 한손/두손 콤보 체인용 12프레임 시트 추가 (`베기 3컷`, `연결`, `찌르기 3컷`, `연결`, `베기 3컷`, `루프 복귀`)
- [x] 콤보 체인 이펙트 위치 엄격 검수 통과 (무기 일부 가림은 허용, 이펙트 시작점/프레임 경계/자루 발생 여부 기준 통과)
- [ ] 엄격 검수 통과 후 실제 전투/캐릭터 애니메이션에 연결

### 테스트 기능
- [x] 자정 초기화 테스트 버튼
- [x] 원하는 무기 즉시 획득 버튼 (테스트 전용)
- [x] 강화 연출 체험 버튼 6종 (테스트 전용, 실제 강화 단계/엽전 변화 없음)

### 반응형 및 스크롤 최적화
- [x] 모바일, 태블릿, PC 해상도별 레이아웃 최적화 (미디어 쿼리 연동)
- [x] 대장간 제목 우측 모드전환 및 전체화면 컨트롤 추가
- [x] 전투 로그 및 팔도 모험 스크롤 자동화 및 모바일 뷰 최적화 완료

### 배포
- [x] GitHub Pages 프로젝트 경로(`/weapon-reinforce/`)에 맞춘 Vite `base` 설정 추가
- [x] 앱 내부 이미지와 favicon 경로를 Vite base 기준으로 보정
- [x] GitHub Pages Actions 배포 workflow 추가

### 자정 초기화 및 일일 접속 정산
- [x] 일자 전환 감지 시스템 (마지막 접속 날짜와 오늘 날짜 비교)
- [x] 하루 지나 재접속 시 전날 최고 도달 무기 데이터 기반 결산 뉴스 자동 팝업 연동 완료

## 알려진 버그 / TODO
- [x] getMidnightNews에서 tier 1~3의 일부 body 텍스트가 잘려있을 수 있음 (복원 완료)
- [x] 무기 이미지 실제 PNG 파일 필요 (모든 1등급, 한손 2~7등급, 두손 2~7등급 픽셀 아트 에셋 생성 및 크로마키 투명화 연동 완료)
- [x] 전투 장면 연출 강화 필요 (전장 시각화 및 모바일 렌더링 검증 완료)

## 최근 작업 기록

### 2026-06-07 모험 해설/대사 UI 구분 보강
- `review-main-001`은 계속 `review-candidate`로 유지하고, 사용자 승인 완료 에피소드는 0개로 유지했다.
- 해설 글상자가 일반 인물 대사처럼 보이지 않도록 `role-narration`은 중립적인 배경, 더 낮은 글자 무게, 작은 `해설` 서술 표식으로 조정했다.
- 일반 대사 글상자는 `role-dialogue` 이름표가 파란 계열 이름표 배경과 테두리를 갖도록 해, 인물이 말하는 글상자와 해설 글상자가 한눈에 다르게 보이게 했다.
- 혼합 글상자 안의 인물 이름도 작은 이름표처럼 보이게 `adventure-inline-speaker` 스타일을 강화했고, `지난 기록`에서도 해설 항목과 대사 항목의 배경, 이름표, 글자 무게를 다르게 했다.
- `docs/adventure-vn-story-standards.md`, `docs/adventure-first-episode-approval-audit.md`, `scripts/validate-adventure-catalog.mjs`에 해설과 일반 대사의 시각 구분 기준을 추가했다.

### 2026-06-07 모험 첫 후보 검은 사발 등장 보강
- `review-main-001`은 계속 `review-candidate`로 유지하고, 사용자 승인 완료 에피소드는 0개로 유지했다.
- 검은 사발이 갑자기 선택 대상처럼 보이지 않도록 `선비 앞에는 어느새 작은 검은 사발이 놓여 있었다`를 추가했다.
- 이어서 `선비가 사발을 조용히 밀었다`, `검은 사발은 내 앞에서 멈췄고, 안쪽은 먹물을 담은 듯 어두웠다`, `사발 안쪽 바닥에는 마른 붓자국이 희미한 둥근 고리로 남아 있었다`로 이어지게 해, 핵심 물건이 보이고 움직이고 멈추는 순서를 분명히 했다.
- 검토용 읽기본, 승인 감사, 이야기 작성 절차, 스모크 테스트, 카탈로그 검증을 같은 문장 기준으로 동기화했다.

### 2026-06-07 모험 첫 후보 문장 연결 재점검
- `review-main-001`은 계속 `review-candidate`로 유지하고, 사용자 승인 완료 에피소드는 0개로 유지했다.
- 선택 직전 먹물방울 묘사는 `그러자 먹물 한 방울이 끝에 맺혔다. 검은 콩알처럼 둥글었다`처럼 비유가 따로 떨어지던 문장을 `붓끝에 검은 콩알처럼 둥근 먹물 한 방울이 맺혔다`로 붙였다.
- 선택 A 결과는 `먹물점 -> 선비의 눈매 변화 -> 검은 점을 한참 내려다보다 장부에 손끝을 댐 -> 장부가 닫힘` 순서로 다듬어, 각 문장이 다음 행동을 부르게 했다.
- 선택 B 마지막 장면은 `밥알 하나 없는 사발 때문에`를 `사발 안에는 밥알 하나 남지 않았다`, `방금 전의 말소리도 더 멀게 느껴졌다`로 나눠, 빈 사발과 남은 정적이 자연스럽게 이어지도록 했다.
- 런타임 데이터, 검토용 읽기본, 승인 감사, 스모크 테스트, 카탈로그 검증의 기대 문구를 같은 기준으로 동기화했다.

### 2026-06-07 모험 첫 후보 결과 글상자 호흡 압축
- `review-main-001`은 계속 `review-candidate`로 유지하고, 사용자 승인 완료 에피소드는 0개로 유지했다.
- 기준 문서의 `선택 결과 2-5문장`, `마지막 장면 2-5문장` 원칙과 실제 후보가 어긋나지 않도록, 선택 결과 글상자와 선택 A/B 마지막 장면을 더 짧은 문장 묶음으로 압축했다.
- 선택 A 결과는 먹물이 사발 바닥에 떨어져 검은 점으로 퍼지고, 선비가 장부 모서리에 손끝을 대자 장부가 닫히는 흐름으로 정리했다.
- 선택 B 결과는 국밥 김이 서리를 녹이고, 선비가 짧게 말한 뒤 `오늘 밤, 이 고개` 칸의 먹물이 번지며 맞은편 자리가 비는 흐름으로 줄였다.
- 선택 C 결과는 문고리를 잡고 나갔지만 첫 굽이에서 같은 처마 물소리를 듣고, 흙길이 주막 마당으로 바뀌는 되감김을 4문장으로 정리했다.
- 마지막 장면도 선택 A는 사라진 장부와 사발 바닥 글씨, 선택 B는 빈 사발과 날짜 없는 빈칸, 검은 갓끈의 여운이 한 화면에서 더 읽히게 줄였다.
- 런타임 데이터, 검토용 읽기본, 승인 감사, 스모크 테스트, 카탈로그 검증의 기대 문구를 같은 기준으로 동기화했다.

### 2026-06-07 모험 첫 후보 문장 연결성 추가 보강
- `review-main-001`은 계속 `review-candidate`로 유지하고, 사용자 승인 완료 에피소드는 0개로 유지했다.
- `주모는 그를 보지 못했다`처럼 대명사가 앞뒤 주체를 흐릴 수 있는 문장을 `주모는 선비를 보지 못했다`로 바꿔, 주인공은 보고 있고 주모만 보지 못한다는 장면 조건을 더 분명하게 했다.
- 검은 사발 장면은 `선비가 검은 사발을 밀었다 -> 내 앞에서 멈췄다 -> 고개를 숙여 보았다 -> 바닥의 붓자국을 보았다` 순서로 고쳐, 사발 안쪽과 바닥 묘사가 물건 목록처럼 따로 놀지 않게 했다.
- 저승사자를 떠올리는 속생각은 `검은 갓을 다시 보았다`, `주모는 여전히 맞은편 손님을 알아차리지 못했다`, `상판의 서리까지 보이자` 순서로 바꿨다. 이제 주인공이 본 것들이 차례로 쌓인 뒤 저승사자 이야기가 떠오른다.
- 선택 A 결과는 먹물점이 퍼진 뒤 선비가 그 점을 보고 눈매를 바꾸고, 그 점에서 시선을 떼지 않은 채 장부에 손을 대도록 고쳤다. 선택 B 결과는 장부 뒤쪽 종이가 내려앉은 뒤 종이가 조용해지고 나서 선비가 빈 사발을 보는 흐름으로 이어지게 했다.
- 선택별 마지막 장면도 한 번 더 조였다. 선택 A는 손끝이 풀린 뒤 낮은 상을 살피는 흐름으로, 선택 B는 빈 사발 때문에 방금 전의 말소리가 멀어지는 여운으로, 선택 C는 첫 굽이를 돌았지만 발자국이 멀리 가지 못했다는 흔적으로 이어지게 했다.
- `주모에게 보이지 않는 손님` 계열의 표현은 검토 문구와 이미지 필요 사항에서도 `주모 눈에는 보이지 않는 선비`, `주모의 눈에는 비어 있는 듯한 맞은편 자리`로 바꿔 시점 조건을 더 분명히 했다.
- 런타임 데이터, 검토용 읽기본, 승인 감사, 스모크 테스트, 카탈로그 검증의 기대 문구를 같은 기준으로 동기화했다.

### 2026-06-07 모험 첫 후보 마지막 장면 연결성 보강
- `review-main-001`은 계속 `review-candidate`로 유지하고, 사용자 승인 완료 에피소드는 0개로 유지했다.
- 선택 A 마지막 장면에서 `낮은 상`이 반복되어 시선 흐름이 끊기던 문장을 `냉기가 조금 가시자 먼저 낮은 상을 살폈다`, `그 자리에는 내가 엎어 놓았던 검은 사발만 바짝 말라 있었다`로 고쳤다.
- 선택 C 마지막 장면에서 `산 아래로 내려갔다고 믿었는데`는 선택 결과의 `첫 굽이를 돌자`와 거리감이 맞지 않아 `분명 내리막 첫 굽이를 돌았는데`로 고쳤다. 이제 도망 선택은 멀리 내려간 뒤 돌아온 요약이 아니라, 첫 굽이에서 바로 같은 주막 문 앞으로 되감기는 흐름으로 읽힌다.
- 검은 사발 묘사의 `달무리처럼`은 어린 학생이 장면을 바로 그리기 어려운 비유라서 `사발 안쪽 바닥에는 마른 붓자국이 희미한 둥근 고리로 남아 있었다`로 바꿨다.
- `보이지 않는 손님`은 주인공에게도 안 보이는 듯 읽힐 수 있어 `주모에게 보이지 않는 손님`으로 고쳤다. 이제 주인공은 보고 있고 주모만 보지 못한다는 장면 조건이 더 분명하다.
- 런타임 데이터, 검토용 읽기본, 승인 감사, 스모크 테스트, 카탈로그 검증의 기대 문구를 같은 기준으로 동기화했다.

### 2026-06-07 모험 첫 후보 선택 결과/마지막 장면 분리
- `review-main-001`은 계속 `review-candidate`로 유지하고, 사용자 승인 완료 에피소드는 0개로 유지했다.
- 선택 결과 글상자는 즉시 반응만 보여 주도록 다시 나눴다. 선택 A는 `검은 장부가 탁 닫혔다`, 선택 B는 `선비가 붓끝을 눕혔다`, 선택 C는 `빗길이 주막 처마로 이어졌다`로 정리했다.
- 마지막 장면은 시간이 지난 뒤 남은 증거만 남기게 했다. 선택 A는 사발 바닥의 `오늘 밤은 아니다` 글씨, 선택 B는 날짜 없는 빈칸과 검은 갓끈, 선택 C는 문 앞을 둥글게 돈 젖은 짚신 자국으로 닫힌다.
- `scripts/validate-adventure-catalog.mjs`는 선택 결과에 마지막 장면의 증거가 미리 섞이지 않는지 확인하고, 첫 후보의 자료 URL이 한국민족문화대백과사전/우리역사넷 같은 한국 기관 자료에 머무는지도 검사한다.

### 2026-06-07 모험 첫 후보 선택 C 되감김 단서 보강
- `review-main-001`은 계속 `review-candidate`로 유지하고, 사용자 승인 완료 에피소드는 0개로 유지했다.
- 선택 C 결과에서 문고리를 잡고 주막 밖으로 나간 뒤 바로 같은 문 앞으로 돌아오는 느낌이 갑작스럽지 않도록 `첫 굽이를 돌자 방금 떠난 주막 처마 물소리가 다시 앞에서 들렸다`와 `잠시 뒤 흙길은 주막 마당의 질척한 흙으로 바뀌었다`를 사용했다.
- 이 문장은 도망 선택의 결과가 단순 순간이동처럼 보이지 않고, 빗길이 같은 주막 앞으로 되감기는 모험 장면으로 읽히게 하는 전조다.
- 런타임 데이터, 검토용 읽기본, 승인 감사, 목표 완료 감사, 작성 절차, 스모크 테스트, 카탈로그 검증을 같은 문장 기준으로 동기화했다.

### 2026-06-07 모험 첫 후보 선택 B 전조 보강
- `review-main-001`은 계속 `review-candidate`로 유지하고, 사용자 승인 완료 에피소드는 0개로 유지했다.
- 선택 B 결과에서 오늘 밤 칸의 먹물이 번진 뒤 바로 선비가 사라지던 흐름을 `장부 맨 뒤쪽 빈 종이 한 장이 저절로 들렸다가 내려앉았고`, `그 순간 맞은편 자리가 비었다`로 나누었다.
- 이 전조 덕분에 마지막 장면에서 장부 맨 뒤 날짜 없는 빈칸에 이름처럼 보이는 글씨가 옮겨져 있는 장면이 갑자기 튀어나오지 않고, 선택 B의 대접과 장부 변화가 더 자연스럽게 이어진다.
- 런타임 데이터, 검토용 읽기본, 승인 감사, 목표 완료 감사, 작성 절차, 스모크 테스트, 카탈로그 검증을 같은 문장 기준으로 동기화했다.

### 2026-06-07 모험 첫 후보 문장 명확화
- `review-main-001`은 계속 `review-candidate`로 유지하고, 사용자 승인 완료 에피소드는 0개로 유지했다.
- 주모의 경고를 `이름을 먼저 알려 주면, 그 이름을 따라 사람이 끌려간다`에서 `이름을 먼저 알려 주면, 그 이름을 가진 사람이 밤길로 끌려간다`로 고쳐 위험의 주체와 결과를 더 바로 읽히게 했다.
- 주인공 속생각의 `그 글씨가 정말 내 이름이 되어 버릴 것 같았다`는 표현을 `그 글씨가 장부에서 내 이름으로 굳어 버릴 것 같았다`로 바꿔, 이름이 적힌 장부라는 장면 압박에 더 붙였다.
- 새 문구는 런타임 데이터, 검토용 읽기본, 승인 감사, 스모크 테스트, 카탈로그 검증에 동기화했고, 예전 모호한 문장은 회귀 금지 문구에 추가했다.

### 2026-06-07 모험 첫 후보 선택 직전 인과 보강
- `review-main-001`은 계속 `review-candidate`로 유지하고, 사용자 승인 완료 에피소드는 0개로 유지했다.
- 선택 직전 마지막 글상자에 `검은 사발은 장부 글씨 바로 옆에 바짝 붙어 있었다`를 추가해, 사용자가 `검은 사발을 엎어 장부 글씨를 덮기`를 골랐을 때 왜 먹물방울이 글씨 대신 사발 바닥에 떨어지는지 더 직접적으로 읽히게 했다.
- 런타임 데이터, 검토용 읽기본, 승인 감사, 목표 완료 감사, 이야기 작성 절차, 스모크 테스트, 카탈로그 검증을 같은 문장 기준으로 동기화했다.

### 2026-06-07 모험 첫 후보 자료 설명 정밀화
- `review-main-001`은 계속 `review-candidate`로 유지하고, 사용자 승인 완료 에피소드는 0개로 유지했다.
- 한국민족문화대백과사전 `연명설화`의 사용 설명을 `수명 숫자가 바뀐다`는 뭉툭한 표현에서, 저승사자를 대접하거나 착한 일을 해 정해진 목숨을 늘리는 이야기 구조로 정리했다.
- 이 자료 설명은 본문에 설명문처럼 추가하지 않고, 런타임 `sourceNotes`, 검토용 읽기본, 승인 감사의 참고 자료 설명에만 반영했다. 이야기 본문은 계속 검은 사발, 빈 사발, 국밥, 장부 글씨, 문고리 앞 선택으로 읽히게 둔다.
- 한국민족문화대백과사전 `주막` 자료를 `sourceNotes`에 추가해, 부엌이 마루나 방과 붙은 주막 구조와 주모, 국밥 솥, 낮은 상 배치가 생활사 근거를 갖도록 했다.
- 첫 후보 이미지 필요 사항의 `검은 도포`는 자료와 더 맞는 `검은 두루마기`로 고쳤고, 중국식 도사 복식과 일본 사무라이풍 검은 옷을 더 분명히 배제했다.
- `npm run validate:adventure`가 예전 `수명 숫자` 표현 회귀를 막고, `연명설화` 사용 설명이 현재 자료 근거와 맞는지 확인하도록 보강했다.

### 2026-06-07 모험 첫 후보 먹물방울 인과 수리
- `review-main-001`은 계속 `review-candidate`로 유지하고, 사용자 승인 완료 에피소드는 0개로 유지했다.
- Ren'Py, Writers.com, Inkett, Jane Friedman 등에서 참고한 미연시/단편/장면 작성 기준을 `사용자가 무엇을 보고, 무엇을 원하고, 무엇에 막히고, 무엇을 골라서, 무엇이 달라졌는가` 점검표로 좁혀 `docs/adventure-vn-story-standards.md`와 `docs/adventure-story-writing-workflow.md`에 반영했다.
- 두 번째 글상자는 이후 다시 고쳐, 주인공의 옷차림 위화감이 이야기를 방해하지 않게 했다. 현재 문장은 `이 고개에는 아는 얼굴이 하나도 없었다`, `낯선 밤길은 젖은 짚신처럼 발을 무겁게 했다`처럼 장소와 행동으로만 낯섦을 남긴다.
- 검은 장부 장면은 붓이 갑자기 나타나는 느낌을 줄이기 위해, 선비가 소매에서 검은 장부와 가느다란 붓을 함께 꺼내고 붓끝을 장부 한쪽 칸 위에 멈추는 장면을 먼저 보이게 했다.
- 선비의 선택 압박 문구는 `먹물이 마르기 전에`가 아니라 `먹물방울이 떨어지기 전에`로 고쳤다. 이제 위협이 먹물이 마르는 추상적인 시간보다, 이름처럼 보이는 글씨 위에 먹물방울이 떨어지는 순간으로 더 직접적으로 읽힌다.
- 이전의 `검은 물방울이 굴러온다`는 장치가 추상적으로 읽힐 수 있어, 선비가 장부 글씨 위에 붓끝을 세우고 먹물방울이 떨어지려는 장면으로 바꿨다.
- 선택 A는 `검은 사발을 엎어 장부 글씨를 덮기`로 고쳤다. 이제 사용자가 고른 행동 때문에 붓끝의 먹물이 글씨 대신 사발 바닥에 떨어지고, 장부가 닫히며 `오늘 밤은 아니다`라는 글씨가 남는 인과가 바로 읽힌다.
- 선택 A 결과와 마지막 장면은 `엎은 사발의 둥근 바닥`으로 표현을 통일했다. 먹물이 어디에 떨어졌는지 더 분명하게 하고, 결과와 덜 이어지는 `손등` 표현은 `차가운 손끝`으로 낮췄다.
- 선택 직전 문구는 `검은 갓의 선비가 붓끝을 세운 채 기다렸다. 먹물방울은 이름처럼 보이는 글씨 바로 위에서 떨어질 듯 매달려 있었다.`로 정리했다. `나는 먼저 무엇을 할까?` 같은 안내문보다, 글상자와 선택지가 이어지는 상황 압박을 먼저 보이게 했다.
- 마지막 선택 전 글상자에는 `내 시선과 손은 세 곳 사이에서 멈췄다`를 추가해, 검은 사발, 빈 사발과 국밥, 문고리 중 실제 행동을 고르는 순간이 더 분명하게 읽히도록 했다.
- 결과장면에 `참고 자료` 버튼을 추가했다. `sourceNotes`가 있는 이야기는 마지막 장면 카드에서 자료 팝업을 열어 전래동화/설화/역사기록/생활사 자료의 제목, 사용 방식, 링크를 확인할 수 있다.
- `docs/adventure-first-episode-review-copy.md`, `docs/adventure-first-episode-approval-audit.md`, `docs/adventure-goal-completion-audit.md`, `scripts/validate-adventure-catalog.mjs`, `scripts/smoke-adventure-first-episode.mjs`를 새 문구와 새 수리 기준에 맞췄다.
- 검증: `npm run validate:adventure`, `ADVENTURE_SMOKE_URL=http://127.0.0.1:5174/ npm run smoke:adventure`, `npm run lint`, `npm run build`, `git diff --check` 통과.

### 2026-06-07 모험 첫 후보 선택 A 직관화
- `review-main-001`은 계속 `review-candidate`로 유지하고, 사용자 승인 완료 에피소드는 0개로 유지했다.
- 선택 A를 `검은 사발의 가장자리를 받쳐 들기`에서 `검은 사발을 들어 글씨 앞을 가리기`로 바꿨다. 사용자가 고른 행동이 실제로 글씨 앞을 막는 장면으로 보이게 해, 물방울이 글씨 대신 손등에 떨어지고 장부가 닫히는 인과를 더 직관적으로 만들었다.
- 선택 직전 글상자도 `검은 사발이 장부 가까이 밀려왔다. 검은 물방울은 사발 가장자리를 따라 먹물이 번진 글씨 쪽으로 굴러왔다`로 정리해, 아직 나오지 않은 물건의 위치 설명이나 애매한 가장자리 묘사에 기대지 않게 했다.
- 선택 B의 당위성을 높이기 위해 빈 사발을 `그냥 이상하게 놓인 물건`이 아니라 `주모가 덜어 먹으라며 곁에 놓아 준 물건`으로 보이게 했다. 덕분에 국밥을 덜어 내미는 행동이 장면 안에서 더 자연스럽게 이어진다.
- 선비의 요구 대사는 `손이 먼저 닿는 것 하나를 움직이게`처럼 기계적으로 들리는 문장에서 `말 대신 손으로 답하게. 자네가 먼저 건드리는 것을 대답으로 삼겠네`로 바꿨다. 선택 직전 압박도 `손으로 대답하게` 반복 대신 `무엇에 먼저 손댈지 정하게`로 정리했다.
- `docs/adventure-vn-story-standards.md`와 `docs/adventure-story-writing-workflow.md`에 외부 글쓰기 기준을 현재 목표에 맞게 좁혀 반영했다. 플롯은 사건 나열이 아니라 원인과 결과의 배열이고, AI 초안은 한 문장 질문, 선택-결과 인과표, 글상자 역할표를 통과한 뒤 본문을 쓰도록 기준을 강화했다.
- `docs/adventure-first-episode-review-copy.md`, `docs/adventure-first-episode-approval-audit.md`, `docs/adventure-goal-completion-audit.md`, `docs/adventure-pilot-001-completion-audit.md`, `docs/adventure-vn-production-plan.md`, `scripts/validate-adventure-catalog.mjs`, `scripts/smoke-adventure-first-episode.mjs`를 새 선택명과 인과 기준에 맞췄다.

### 2026-06-07 모험 첫 후보 단편 인과 재개정
- `review-main-001`은 계속 `review-candidate`로 유지하고, 승인 완료 에피소드는 0개로 둔 채 첫 후보 한 편만 수정했다.
- 첫 후보의 중심 질문을 `검은 물방울이 이름처럼 보이는 장부 글씨에 닿기 전에 무엇을 할 것인가`로 더 선명하게 조였다. 선택 A는 `검은 사발의 가장자리를 받쳐 들기`, 선택 B는 `빈 사발에 국밥을 덜어 내밀기`, 선택 C는 `문고리를 잡고 주막 밖으로 물러서기`로 정리했다.
- 선택 A가 단순 위치 설명처럼 보이지 않도록 `검은 사발의 가장자리를 받쳐 들기`로 고정했다. 결과도 `검은 사발의 가장자리를 손으로 받쳐 들었다`로 시작해, 사용자가 고른 손동작 때문에 물방울이 글씨 대신 손등에 떨어지는 흐름이 바로 읽히게 했다.
- 선택 A 결과는 붓끝의 먹물이 글씨 대신 엎은 사발의 둥근 바닥에 떨어지고, 선비가 손끝을 장부 모서리에 살짝 대자 검은 장부가 탁 닫히는 즉시 반응으로 끝나게 했다. 사발 바닥의 `오늘 밤은 아니다`는 마지막 장면에서만 확인되도록 분리했다. 선택 B는 먹물이 번지고 장부 맨 뒤쪽 종이가 살짝 움직이는 즉시 반응 뒤, 마지막 장면에서 날짜 없는 빈칸에 이름처럼 보이는 글씨와 검은 갓끈이 남게 했으며, 선택 C는 젖은 짚신 자국이 같은 주막 문 앞에서 끝나게 했다.
- 장부 안의 위치 설명이 헷갈리지 않도록 `오늘 밤, 이 고개` 칸 옆에 먹물이 번진 글씨가 있고, 붓끝은 그 글씨 곁에서 멈춰 있다는 순서로 문장을 정리했다.
- `docs/adventure-first-episode-review-copy.md`, `docs/adventure-first-episode-approval-audit.md`, `docs/adventure-goal-completion-audit.md`, `docs/adventure-pilot-001-completion-audit.md`, `docs/adventure-vn-story-standards.md`, `docs/adventure-story-writing-workflow.md`, `scripts/validate-adventure-catalog.mjs`, `scripts/smoke-adventure-first-episode.mjs`를 새 문구와 인과 기준에 맞췄다.
- 검증: `npm run validate:adventure`, `ADVENTURE_SMOKE_URL=http://127.0.0.1:5174/ npm run smoke:adventure`, `npm run lint`, `npm run build`, `git diff --check` 통과. 인앱 브라우저에서도 첫 장면, 선택 전 문구, `검은 사발의 가장자리를 받쳐 들기` 선택지, 선택 A 결과, 선택 A 마지막 장면, 별도 `대장간 정산` 분리를 확인했다.

### 2026-06-07 모험 단편 기준 강화
- 새 모험 에피소드는 장편의 1화가 아니라 길 위에서 만난 한 사건으로 닫히는 단편이라는 기준을 `docs/adventure-vn-story-standards.md`, `docs/adventure-story-writing-workflow.md`, `docs/adventure-vn-production-plan.md`, `CHECKLIST.md`에 보강했다.
- 단편을 장편 챕터가 아니라 단편소설에 가까운 작은 완결 장면으로 다루도록, `한 사건, 한 선택 또는 판단, 한 결말`을 먼저 고정하는 절차를 `docs/adventure-story-writing-workflow.md`에 추가했다.
- 짧은 편이 사건 안내문, 짧은 퀘스트 설명, 보상 알림처럼 흐르지 않고 플롯 있는 단편으로 읽혀야 한다는 기준을 문서와 체크리스트에 추가했다.
- 단편이 짧은 줄거리 요약으로 흐르지 않도록, 중심 갈등/긴장 상승/전환점/결과가 있는 작은 완결 장면이어야 한다는 기준을 추가했다. OpenStax와 Texas Gateway의 단편·서사 기준은 갈등, 전환점, 해결 기준만 참고해 우리 제작 절차에 맞게 좁혀 반영했다.
- 새 후보마다 한 문장 질문을 먼저 정하고, 그 질문이 같은 에피소드 마지막 장면에서 답나는지 확인하도록 제작 절차와 `npm run validate:adventure` 검증을 강화했다.
- 단편은 분량만 짧은 장편 요약이 아니라 한 번의 플레이에서 시작과 끝이 닫히는 작은 완결 단위라는 기준을 보강했다.
- 사용자가 짚은 것처럼 앱의 모험은 장편보다 단편에 가깝다는 점을 기준 문서에 다시 못 박았다. 장편 플롯을 축약하지 않고, 사용자가 개입할 수 있는 한 순간과 그 전후의 원인·결과만 남기도록 정리했다.
- 첫 후보의 `storyCraft.shortStoryScope`와 검토용 읽기본에도 단편집의 한 꼭지처럼 한 번의 플레이 안에서 사건이 닫혀야 한다는 기준을 직접 남겼다.
- 단편 기준을 다시 좁혀, 한 편은 한 번에 읽고 바로 닫히는 사건이어야 하며 장기 복선, 다음 편 예고, 여러 단계의 여정은 본문에 넣지 않고 후보 메모로 분리하도록 정리했다.
- 장편으로 오해될 수 있는 `큰 단편` 표현은 현재 기준과 체크리스트에서 `메인 단편`으로 정리했다. 여기서 메인은 모험 중 중심 사건이라는 뜻일 뿐, 긴 챕터나 장편 에피소드가 아니다.
- 첫 후보 데이터에도 `storyCraft.singleQuestion`과 `storyCraft.shortStoryScope`를 추가해, `검은 갓의 사발`의 한 문장 질문과 장편이 아닌 짧은 단편 범위가 실제 승인본 구조에 남도록 했다.
- 첫 후보 데이터에 `storyCraft.studentClarity`와 `storyCraft.ambiguityGuard`를 추가해, 어린 학생도 따라갈 수 있는 장면 범위와 모호한 표현/갑작스러운 결과를 막는 기준이 승인본 구조에 남도록 했다.
- 장부 속 글씨는 `그 칸 옆에 번진 글씨는 내 이름처럼 보였다`처럼 이유가 덜 보이는 표현에서 `첫 글자와 끝 글자는 내 이름의 글자와 같아 보였다`로 바꿔, 주인공이 왜 긴장하는지 더 직접적으로 읽히게 했다.
- 장부 글씨는 선비가 소매에서 꺼낸 장부라는 흐름과 맞도록 `빗물에 번져`가 아니라 `먹물이 번져` 있는 글씨로 정리했다.
- 후보 설명과 감사 문서의 행동 표현을 실제 선택지와 맞췄다. `검은 사발을 누를까`, `버틸지`, `장부에 번진 이름`처럼 덜 정확한 표현은 `검은 사발을 장부에서 떼어 놓을까`, `검은 사발을 떼어 놓을지`, `이름처럼 보이는 글씨`로 정리했다.
- 선택 B 마지막 장면의 `국밥을 덜어 내밀었던 빈 사발`과 `국밥을 덜어 내밀었던 사발만 비어 있었다`는 앞뒤가 어색해, `국밥을 덜어 내밀었던 사발만 남아 있었다. 사발 안에는 밥알 하나 남지 않았다`로 고쳤다.
- 선택 B 마지막 장면에서 주인공이 저승사자 같은 손님이 남긴 갓끈을 곧바로 품에 넣는 행동은 이유가 충분하지 않아 제거했다. 갓끈은 가져가는 보상이 아니라, 국밥을 덜어 대접한 선택 뒤 장면에 남은 흔적으로만 보이게 했다.
- 첫 후보의 선택 대상 배치를 보강했다. 문고리는 첫 글상자에서 빗물 맺힌 놋쇠 문고리로 먼저 보이고, 빈 사발은 두 번째 글상자에서 주모가 곁에 둔 물건으로 먼저 보인 뒤 선비가 선택 대상으로 말하게 했다.
- `npm run validate:adventure`는 빈 사발과 문고리가 선비 대사보다 먼저 보이지 않으면 실패하도록 보강했고, 예전 `빈 사발이 깨끗하게 비어 있었다` 문구도 회귀 금지 문구로 추가했다.
- 선택 B 결과에서 예전 금지 문구였던 `선비는 자기 검은 사발을 그 옆에 나란히 두었다`를 제거하고, 선비가 `밥을 먼저 내민 이를 빈손으로 돌려보낼 수는 없지`라고 말한 뒤 이름 글씨가 ‘오늘 밤, 이 고개’라고 적힌 칸에서 흐려지고 장부 맨 뒤쪽 종이가 살짝 움직이는 흐름으로 인과를 보강했다. 마지막 장면에서는 그 변화가 날짜 없는 빈칸에 작게 옮겨진 글씨로 확인된다.
- 선택 B 결과 글상자와 마지막 장면이 어긋나지 않도록, 선비가 사라진 뒤 `한 장 펼쳐진 검은 장부와 검은 갓끈 하나`가 남는 흐름으로 연결했다. 마지막 장면도 상 위에 검은 장부가 한 장 펼쳐진 채 남아 있고 갓끈은 건드리지 않는 여운으로 정리했다.
- 선택 C 결과에서 선택 라벨과 결과가 어긋나지 않도록 `문고리를 잡고 주막 밖으로 물러서기`로 고치고, 첫 장면의 젖은 짚신과 마지막 발자국이 이어지도록 `젖은 짚신 자국은 번번이 주막 문 앞으로 되돌아왔다` 흐름으로 논리를 보강했다.
- 첫 후보 문구 검증을 현재 런타임 대사와 맞춰 `이름을 먼저 알려 주면, 그 이름을 가진 사람이 밤길로 끌려간다 하오`, `이름을 입에 올리지 말게`, `자네가 먼저 건드리는 것을 대답으로 삼겠네`, `무엇에 먼저 손댈지 정하게` 기준으로 정리했다.
- 빈 사발 설명이 설명문처럼 보이지 않도록 `주모는 덜어 먹으라며 빈 사발 하나를 곁에 놓아 주었다. 그 안쪽은 물기 없이 말라 있었다`로 다듬었다.
- 검증: `npm run validate:adventure`, `ADVENTURE_SMOKE_URL=http://127.0.0.1:5174/ npm run smoke:adventure`, `npm run lint`, `npm run build`, `git diff --check` 통과. 브라우저에서 `모험 시작` 후 선택지 세로 표시와 `빈 사발에 국밥을 덜어 내밀기` 문구를 확인했다.
- 최신 배치 기준: 첫 글상자에 `문가의 놋쇠 문고리에는 빗물이 맺혔고`, 두 번째 글상자에 `옷깃과 말끝이 이곳 사람들과 조금 달라 보일까 봐`와 `주모는 덜어 먹으라며 빈 사발 하나를 곁에 놓아 주었다`가 보인다. 검은 사발 가장자리의 물방울은 장부가 나오기 전에 `장부 쪽`으로 설명하지 않고, 검은 장부가 검은 사발 옆에 펼쳐진 뒤 `검은 사발이 장부 가까이 밀려왔다`는 압박으로 읽혀야 한다.
- 최신 문구 정리: 두 번째 글상자는 일반 해설 글상자로 두고, 실제 판단이나 불안이 드러나는 순간만 주인공 속생각으로 처리한다.
- 내부 선택 키 정리: 사발을 버티는 선택이 음주로 오해될 수 있는 `drink` 키를 쓰지 않도록 `brace`로 바꾸고, `npm run smoke:adventure`가 `brace`, `serve`, `flee` 세 흐름을 검증하도록 보강했다.
- 7번 글상자에서 검은 사발을 이미 민 것처럼 말하고 8번 글상자에서 다시 미는 중복 흐름을 제거했다. 이제 7번은 빈 사발/국밥/문고리와 소매 속 선비의 손만 보여 주고, 8번에서 검은 사발이 처음 이쪽으로 밀려온다.
- `이름은 입보다 손을 먼저 따라가니`처럼 뜻을 한 번 더 해석해야 하는 대사를 `먼저 손댄 것이 자네 대답이 될 걸세`로 고쳐 선택 규칙이 바로 읽히게 했다.
- 선택 A를 추상적인 `버티기`나 단순한 `물방울 막기`가 아니라 `검은 사발을 장부에서 떼어 놓기`로 고쳤다. 선택 전에는 검은 사발이 장부 모서리까지 바짝 밀려온 압박을 보여 주고, 결과에서는 물방울이 글씨 위로 떨어지지 않아 장부가 닫히는 흐름으로 인과를 보강했다.
- 인앱 브라우저에서 `지난 기록` 팝업, 선택 A 결과, 선택 A 마지막 장면을 직접 확인했다. 결과 글상자에는 `검은 사발의 가장자리를 붙들고 장부에서 조금 떼어 냈다`, `글씨 위로 떨어지지는 않았다`가 보이고, 마지막 장면에는 `장부에서 조금 떨어져 놓인 검은 사발`과 별도 `대장간 정산`이 표시된다.
- 물방울 압박의 등장 순서를 바로잡았다. 이제 검은 사발 가장자리에 물방울이 먼저 보이고, 그 뒤 검은 장부가 검은 사발 옆에 펼쳐진 다음 사발이 장부 모서리까지 바짝 밀려와 선택 A의 인과가 더 분명하게 읽힌다.
- 앞으로 같은 실수가 반복되지 않도록 `docs/adventure-story-writing-workflow.md`와 `CHECKLIST.md`에 아직 등장하지 않은 물건을 위치 기준으로 먼저 쓰지 않는다는 기준을 추가했다.
- `내 이름과 닮은 글씨`처럼 한 번 더 해석해야 하는 표현을 `내 이름처럼 보이는 글씨`로 바꿔, 주인공이 무엇을 보고 긴장하는지 더 바로 읽히게 했다.
- 주모의 경고는 `이름을 들은 밤손님이 그 이름을 따라 밤길로 데려간다`처럼 헷갈릴 수 있는 문장에서 `이름을 먼저 알려 주면, 그 이름을 가진 사람이 밤길로 끌려간다`로 고쳐, 이름을 말하면 왜 위험한지 학생도 바로 읽히게 했다.
- 옛말 어미는 완전히 없애지 않는 기준으로 정리했다. `하오`, `자네`, `말게`는 인물의 시대감과 분위기를 살릴 수 있으면 자연스럽게 남기되, 핵심 행동과 위험은 현대 학생도 바로 이해할 수 있는 단어로 쓴다.
- 두 번째 글상자의 빈 사발 묘사는 `주모는 덜어 먹으라며 빈 사발 하나를 곁에 놓아 주었다. 그 안쪽은 물기 없이 말라 있었다`로 고쳐, 빈 사발이 단순히 비어 있는 물건이 아니라 주막 안에서 어긋나 보이는 물건이라는 점을 더 쉽게 읽히게 했다.
- 선택 C 결과는 주막 밖으로 나가도 첫 굽이에서 방금 떠난 주막 처마 물소리를 다시 듣고, 발자국이 같은 문 앞으로 돌아오며, `문틈 사이로 보이는 낮은 상 위`에 검은 사발이 남아 있는 흐름으로 고쳐, 도망 선택의 결과가 갑작스러운 순간이동처럼 보이지 않게 했다.
- 선택 C 마지막 장면도 발자국을 따라보는 행동으로 다시 정리했다. 이제 주인공은 마당의 젖은 짚신 자국을 따라 고개를 숙이고, 자국은 주막 문 앞을 둥글게 돈 뒤 내가 잡고 나갔던 문고리 앞에서 멈춘다. 덕분에 `한참 내려갔다고 믿었다`는 설명보다 발자국, 문고리, 서리가 되감긴 결과를 직접 보여 준다.
- 시스템 로그에 노출될 수 있는 첫 후보 요약도 `주인공의 손` 같은 바깥말 대신 `내 손이 먼저 닿을 곳`으로 바꿔, 로그가 보여도 장면 안 말투를 해치지 않게 했다.
- `storyCraft.singleQuestion`, `endingSummary`, 검토용 읽기본의 한 문장 질문도 `주인공은`이 아니라 `나는`으로 맞췄다. 런타임 요약이나 검토 문구가 화면에 흘러가도 분석표 말투보다 실제 플레이 시점에 가깝게 읽히도록 정리했다.
- `endingSummary`는 선택을 다시 나열하고 `정했다`로 닫는 줄거리 요약에서, 검은 사발·빈 사발·국밥·검은 장부와 `내가 먼저 손댄 물건 하나가 흔적으로 남았다`는 짧은 회상문으로 정리했다.
- 첫 후보의 `storyCraft.shortStoryScope`, 검토용 읽기본, 승인 감사에도 단편소설형 단위를 직접 남겼다. 현재 후보는 사건 안내문이나 짧은 퀘스트 설명이 아니라, 한 사건, 사발과 문고리 앞의 한 선택 또는 판단, 그 행동 뒤에 남은 한 결말로 닫히는 작은 완결 장면이어야 한다.
- 검증: `npm run validate:adventure`, `ADVENTURE_SMOKE_URL=http://127.0.0.1:5174/ npm run smoke:adventure`, `npm run lint`, `npm run build`, `git diff --check` 재통과. 브라우저 스냅샷으로 로컬 앱 첫 화면과 `🧭 모험 시작` 버튼 표시를 확인했고, 버튼 클릭 검증은 Playwright 스모크 결과를 기준으로 삼았다.
- 선비의 대사도 `고를 것은 벌써 자네 곁에 있네`에서 `필요한 것은 이미 자네 손이 닿는 곳에 있네`로 고쳐, 선택 대상이 눈앞 물건이라는 점을 더 자연스럽게 드러냈다.
- 이 문구 정리 뒤 `npm run validate:adventure`, `ADVENTURE_SMOKE_URL=http://127.0.0.1:5174/ npm run smoke:adventure`, `npm run lint`, `npm run build`, `git diff --check`를 다시 통과했다.
- 선택 B 결과의 `빈 사발을 소매로 닦고`는 앞에서 이미 마른 사발이라고 설명한 장면과 맞지 않아 `빈 사발을 당겨 국밥을 덜었다`, `사발을 선비 앞에 밀어 놓자`, `사발 안쪽의 서리가 김에 녹아 물방울이 되었다` 흐름으로 고쳤다. 결과 제목도 현재는 `선비가 붓끝을 눕혔다`로 정리해 장면 반응 중심으로 읽히게 했다.
- 문장들이 따로 노는 느낌을 줄이기 위해 장부 등장 장면과 선택 직전 장면을 다시 다듬었다. `그는 장부를...`, `붓끝은...`, `장부에는...`처럼 목록으로 이어지던 구간은 이후 `선비가 내 앞에 멈춘 검은 사발 옆에 장부를 펼치자`, `붓끝이 한쪽 칸 위에 가만히 멈췄다`, `그 칸에는...`으로 더 좁혀, 검은 사발이 나타난 뒤 장부가 그 옆으로 펼쳐지는 시선 이동을 붙였다. 선택 직전도 `나는 낮은 상 위를 빠르게 훑었다`로 시작해 검은 사발, 빈 사발과 국밥, 문고리의 서리, 멈춘 시선과 손으로 이어지게 했다.
- 주인공 속생각의 `한 줄로 이어졌다` 같은 추상 표현은 이후 `검은 갓을 다시 보았다`, `주모는 여전히 맞은편 손님을 알아차리지 못했다`, `상판의 서리까지 보이자`로 고쳐, 주인공이 본 것에서 곧장 저승사자 이야기를 떠올리는 흐름으로 정리했다. 선택 C 결과도 `발밑` 반복을 줄이고 `진흙이 짚신에 무겁게 달라붙었다`, `잠시 뒤 흙길은 주막 마당의 질척한 흙으로 바뀌었다`로 바꿔 문장 연결을 더 자연스럽게 했다.
- 선택별 마지막 장면도 같은 기준으로 다듬었다. 선택 A는 보이지 않는 장부를 찾는 시선이 엎었던 검은 사발과 사발 바닥 글씨로 이어지게 했고, 선택 B는 맞은편 빈자리에서 사발, 사발 곁의 검은 장부, 비어 있는 오늘 밤 칸, 장부 맨 뒤 빈칸, 갓끈으로 시선이 이동하게 했다.
- 선택 B 제목과 행동 정리 뒤에도 `npm run validate:adventure`, `ADVENTURE_SMOKE_URL=http://127.0.0.1:5174/ npm run smoke:adventure`, `npm run lint`, `npm run build`, `git diff --check`를 통과했다.
- 문장 연결성 기준을 한 단계 더 엄격하게 했다. `처마가 낮게 흔들렸다`처럼 주체가 흐린 문장은 `처마 끝 물줄기가 낮게 흔들렸다`로 고치고, `낯선 밤길은 젖은 짚신처럼 발끝에 달라붙었다`는 다음 행동으로 더 자연스럽게 이어지도록 `낯선 밤길은 젖은 짚신처럼 발을 무겁게 했다`로 바꿨다. `storyCraft.sentenceConnection`, 검토용 읽기본, 이야기 기준, 작업 절차, 제작 계획, 체크리스트에는 `한 문장 한 문장이 따로 노는 느낌`이면 실패로 보고 앞 문장이 다음 문장을 부르는지 다시 확인한다는 기준을 추가했다.
- 이 문장 연결성 보강 뒤에도 `npm run validate:adventure`, `ADVENTURE_SMOKE_URL=http://127.0.0.1:5174/ npm run smoke:adventure`, `npm run lint`, `npm run build`, `git diff --check`를 통과했다.
- Playwright 최신 화면 확인도 통과했다. `http://127.0.0.1:5174/`에서 첫 화면은 `비 내리는 고갯길 주막`과 밤비/처마/문고리/젖은 짚신으로 시작하고, 선택지는 세로로 표시되며, `지난 기록` 팝업은 힌트가 아니라 앞부분을 다시 읽는 이야기 흐름으로 열린다. 선택 B 결과는 `선비가 붓끝을 눕혔다`, `밥을 먼저 내민 이를 빈손으로 돌려보낼 수는 없지`, `장부 맨 뒤쪽 빈 종이 한 장이 저절로 들렸다가 내려앉았고`, `그 순간 맞은편 자리가 비었다`로 표시되고, 마지막 장면 `검은 갓끈이 낮은 상에 남았다` 뒤 `대장간 정산`은 별도 패널로 분리되어 있다.
- 검토용 읽기본에 `빠른 승인 판단`을 추가했다. 사용자가 실제 앱에서 읽은 뒤 첫 화면, 긴장 이해, 선택 행동성, 선택-결과 인과, 단편소설형 완결감 다섯 항목을 모두 자연스럽다고 판단해야만 `approved`로 올릴 수 있고, 하나라도 애매하면 같은 한 편만 다시 고친다.
- 첫 후보에 쓰인 다섯 자료 링크도 현재 접근 가능함을 확인했다. 한국민족문화대백과사전 `주막`, 한국민족문화대백과사전 `저승사자`, 우리역사넷 `한국인의 죽음과 저승`, 한국민족문화대백과사전 `저승설화`, 한국민족문화대백과사전 `연명설화`의 확인 내용은 설명문으로 노출하지 않고, 주막 구조, 검은 갓, 검은 두루마기, 장부, 짚신, 빈 사발, 국밥 대접, 이름이 적힌 칸 같은 장면 요소로만 유지한다.

### 2026-06-06 모험 첫 검토 후보 교체
- 사용자가 제시한 더 강한 에피소드 예시를 기준으로, 첫 검토 후보 `review-main-001`을 `검은 갓의 사발`로 교체했다. 현재 후보는 비 내리는 고갯길 주막에서 주모가 보지 못하는 검은 갓의 선비가 검은 사발과 검은 장부를 내미는 인연 만남형 단편이다.
- 첫 후보는 14개 글상자와 3개 선택지 구조를 쓴다. `검은 사발을 엎어 장부 글씨를 덮기`, `빈 사발에 국밥을 덜어 내밀기`, `문고리를 잡고 주막 밖으로 물러서기`가 각각 `검은 장부가 탁 닫혔다`, `선비가 붓끝을 눕혔다`, `빗길이 주막 처마로 이어졌다`로 이어지도록 데이터와 검증 기준을 맞췄다.
- 상단 표시 기준은 이후 다시 고쳐, 이야기 제목이 아니라 장소와 시각을 보여 주게 했다. 첫 후보는 `고갯마루 주막 안`, `비 내리는 늦은 밤`을 상단 왼쪽에 표시하고, 저승사자 같은 정체는 문도 열지 않고 앉은 빈자리, 주모가 보지 못하는 손님, 서리 번진 상판, 검은 장부와 서리 낀 문고리로 드러나게 했다.
- 이 후보는 사용자가 제시한 `저승사자와의 주도` 방향에서 출발하되, 어린 학생도 읽을 수 있도록 앱의 미연시 글상자 흐름에서는 검은 사발, 빈 사발, 장부, 문고리 앞의 선택으로 새로 썼다. 이야기 안에는 정산 수치나 스탯 보상을 직접 쓰지 않고, 마지막 장면에는 닫힌 장부, ‘오늘 밤, 이 고개’라고 적힌 칸에서 지워져 장부 맨 뒤, 날짜가 적히지 않은 빈칸으로 옮겨진 이름 글씨, 검은 갓끈, 같은 문고리 앞으로 돌아온 발자국처럼 장면 안에서 실제로 달라진 흔적만 남긴다.
- 어린 학생도 체험할 수 있다는 조건을 반영해 `한 번에 비웠다`, `단숨에 마셨다`, `술맛`처럼 음주를 미화할 수 있는 표현은 제거하고, 어두운 사발은 저승사자 앞의 예의 시험 장치로만 보이도록 검증 기준을 보강했다.
- 말하는 사람 구분 방식은 과한 역할 배지를 쓰지 않도록 다시 정리했다. 서술은 별도 역할 배지 없이 읽히고, 등장인물 말은 이름표로 구분하며, 주인공 속생각은 이름표 없이 괄호와 기울임으로 일반 대사와 구분되게 했다. 주인공이 실제로 말하는 장면에만 화자명 `주인공`을 쓴다.
- `scripts/validate-adventure-catalog.mjs`, `scripts/smoke-adventure-first-episode.mjs`, `docs/adventure-first-episode-review-copy.md`, `docs/adventure-first-episode-approval-audit.md`, `docs/adventure-pilot-001-completion-audit.md`, `docs/adventure-vn-production-plan.md`, `docs/adventure-vn-story-standards.md`, `CHECKLIST.md`를 새 후보 기준으로 갱신했다.

### 2026-06-06 모험 첫 검토 후보 단편 보강 (이전 팥죽 후보, 이후 폐기)
- 첫 검토 후보 `review-main-001`을 여러 이야기로 늘리지 않고 한 편만 보강. `팥죽 할머니와 호랑이` 각색 장면에서 시간 어긋남을 품은 주인공 관점, 호랑이의 `한 입, 아니, 큰절` 말실수, 빈 그릇/부지깽이로 미는 뜨거운 밤톨 선택, 선택별 결과가 원인과 결과로 이어지도록 12개 글상자 흐름으로 재작성.
- `첫 숨을 헛디디게`, `배고픈 척`, `빈 그릇에서 팥죽 김`처럼 행동 대상과 원인 관계가 어색하게 섞인 표현을 제거하고, 선택 결과가 `큰절처럼 마당에 코를 박는 호랑이`, `한 발로 뛰며 숲으로 물러나는 울음소리`처럼 짧은 해학과 장면 반응으로 끝나도록 보강.
- 외부 플롯/미연시 선택지 기준을 `docs/adventure-vn-story-standards.md`에 보강하고, `docs/adventure-first-episode-review-copy.md`, `docs/adventure-first-episode-approval-audit.md`, `docs/adventure-pilot-001-completion-audit.md`, `docs/adventure-vn-production-plan.md`를 현재 후보 문구와 동기화.
- 선택 결과 글상자에서 방금 누른 선택지 라벨을 다시 반복하지 않고 결과 제목과 장면 문장만 보여 주도록 정리. 첫 에피소드 스모크 검증에 선택지 라벨 반복 방지 검사를 추가.
- 두 선택이 공통 요약으로 뭉개지지 않도록 선택별 마지막 장면을 추가했다. 빈 그릇 선택은 금 간 그릇과 그릇 바닥의 팥죽 냄새, 밤톨 선택은 손등의 재와 문턱을 막은 맷돌이 남도록 정리하고, 검증 스크립트에 선택별 마지막 장면 문구 누출 방지 검사를 추가.
- 주인공의 시간 어긋남 관점은 `시간을 건너온 몸`, `내가 온 시대`처럼 설명문에 가까운 표현 대신 낯선 옷깃과 말끝 같은 장면 단서로 자연화하고, 검증 스크립트가 직접 시간여행 설명이 사용자 이야기 글상자에 돌아오지 않도록 보강.
- 모험 시작 로그에 남아 있던 `시간을 건너 조선 팔도...` 직접 설명도 `낯선 조선 팔도의 길목에 섭니다`로 바꾸고, `npm run validate:adventure`가 App.jsx의 사용자 노출 모험 문구에 직접 시간여행 설명이 돌아오면 실패하도록 보강.
- `npm run smoke:adventure`도 실제 글상자, 지난 기록, 선택, 결과, 마지막 장면 화면에 `시간여행`, `시간을 건너`, `내가 온 시대` 같은 직접 설명이 보이면 실패하도록 보강해, 시간 어긋남이 장면 단서로만 남는 기준을 브라우저 흐름까지 고정.
- 첫 후보가 플레이어의 현재 무기와 충돌하지 않도록 `칼`, `베지 않고` 같은 특정 날붙이 전제를 제거하고, `무기를 먼저 앞세우지 않는다`는 무기 중립 판단으로 정리. `npm run validate:adventure`와 `npm run smoke:adventure`가 첫 후보 사용자 노출 문구에 날붙이 전제가 돌아오면 실패하도록 보강.
- 해설 글상자에 섞여 있던 `내 손 닿는 곳` 표현을 주인공 글상자로 옮기고, 두 번째 글상자도 `나는 익숙하지 않은 옷깃을...`로 정리해 주인공 관점과 해설 역할을 분리. `npm run validate:adventure`가 해설 글상자에 주인공 1인칭 감각이 들어오면 실패하도록 보강.
- 선택 결과에 필요한 빈 그릇/밤톨/부지깽이/멍석과 문밖 지게/마당 구석 맷돌을 서로 다른 글상자에 나누어 배치해, 초반 장면이 물건 목록처럼 보이지 않게 다시 다듬었다. `npm run validate:adventure`가 한 글상자에 모든 물건이 몰리면 실패하도록 보강.
- 빈 그릇 선택은 코끝/그릇 금/넘어지는 문밖 지게 반응으로, 밤톨 선택은 앞발/손등 재/들썩인 발목을 감는 멍석/천천히 굴러와 문턱을 막는 맷돌 반응으로 갈라지게 유지하고, `npm run validate:adventure`와 `npm run smoke:adventure`가 한 선택만의 결과가 다른 선택 결과에 섞이면 실패하도록 보강.
- 선택 직후 결과 글상자 본문은 장면 반응만 보여 주고 엽전 수, 획득/손실, `대장간 정산` 라벨이 보이면 실패하도록 `npm run smoke:adventure`를 보강. 조작 버튼은 본문과 분리된 진행 장치로 두며, `docs/adventure-vn-story-standards.md`와 `docs/adventure-vn-production-plan.md`에도 결과 글상자/마지막 장면/정산 패널의 역할 분리를 고정.
- 첫 후보의 이미지 필요 사항을 검토용 읽기본에 함께 기록하고, `npm run validate:adventure`가 초가/팥죽/빈 그릇/밤톨/부지깽이/멍석/지게/맷돌/호랑이/할머니 및 중국풍/일본풍 방지 기준을 확인하도록 보강. 이미지는 아직 생성하지 않고 승인 전 검토 기준으로만 둔다.
- 첫 장면의 `붙어 있었다`, `저녁바람이 낮아지자`처럼 어색하게 걸리는 표현을 `초가 한 채가 서 있었다`, `저녁바람이 잦아들자`로 정리하고, 문턱의 빈 그릇/밤톨/부지깽이/멍석이 결말에서 갑자기 움직이지 않도록 `그냥 물건으로만 보이지 않았다`, `문턱의 물건들이 차례를 얻는다`는 주인공 판단을 추가.
- 할머니가 구해 준 호랑이에게 위협받는 사정을 `산밑 구덩이에서 끙끙 울길래 줄을 내려 꺼내 줬소`로 장면화하고, 재 속 밤톨이 소리를 내고 멍석 끝이 조금 말리는 전조 글상자를 추가해 전래동화식 작은 존재들의 움직임이 결과에서 갑자기 튀지 않게 보강.
- 선택 결과는 두 갈래 모두 앞 장면의 멍석/지게/맷돌 반응으로 이어지되, 단순히 `기다렸다는 듯` 처리하지 않고 발목이 비는 순간, 지게가 넘어지는 순간, 맷돌이 천천히 굴러오는 순간이 보이도록 정리.
- 당시 첫 후보만 연결되어 있던 단독 후보 단계에서는 `npm run validate:adventure`가 승인 완료 0편과 단독 검토 후보만 허용하도록 강화했다. 현재 기준은 최신 작업 요약의 `review-main-001`, `review-main-002` 두 검토 후보 체계를 따른다.
- `npm run smoke:adventure`가 두 선택 모두 결과 글상자 뒤 선택별 마지막 장면까지 확인하도록 보강. 첫 후보 전용으로 `그릇 안쪽에 남은 달큰한 냄새`, `큰절은커녕 한 발로 뛰던 울음소리...` 같은 핵심 문구와 예전 어색한 표현 회귀 여부를 함께 검사한다.
- `npm run smoke:adventure`가 선택지 표시 시점에 `지난 기록` 팝업을 열어 첫 글상자, 주인공 속생각, 등장인물 라벨, 전체 글상자 수가 다시 읽기 흐름으로 보이는지 검사하도록 보강. `해설`, `대사`, `속생각` 같은 메타 분류명은 반복되지 않아야 한다.
- 마지막 장면 카드에서 `결과: +OO냥` 정산 블록과 귀환 버튼을 분리해, 이야기 카드는 끝맺음만 담고 엽전 변화는 별도 `대장간 정산` 패널에서 보이도록 변경. `npm run smoke:adventure`가 마지막 카드 안에 정산 라벨과 이동 버튼이 섞이지 않는지도 검사한다.
- `docs/adventure-vn-story-standards.md`와 `docs/adventure-vn-production-plan.md`에도 마지막 장면은 이야기의 여운만, 엽전/손실/귀환은 별도 `대장간 정산` 패널이라는 원칙을 고정. `npm run validate:adventure`가 이 기준 문구가 사라지면 실패하도록 보강.
- 첫 후보의 마지막 카드는 예전 `한성 일보 속보`, `조선 16XX년` 틀을 쓰지 않고 `마지막 장면`, `초가 문이 다시 닫힌 뒤`로 표시되도록 변경. 앱 렌더링도 `newspaper-card`가 아닌 `adventure-final-scene-card`로 분리했고, `npm run validate:adventure`와 `npm run smoke:adventure`가 임시 신문 메타나 신문 카드 껍데기가 돌아오면 실패하도록 보강.
- 마지막 장면 본문에도 게임식 정산 문구, 엽전 수, 직접 시간여행 설명, 특정 무기 전제가 들어오지 않도록 `npm run validate:adventure`와 `npm run smoke:adventure`를 보강. 끝맺음은 선택 행동의 흔적과 장면의 여운만 맡고, 엽전 변화는 별도 `대장간 정산` 패널에서만 보인다.
- 같은 첫 후보 안에서 지게와 맷돌이 결과에 갑자기 등장하지 않도록 주인공의 생각과 물건 배치 해설을 분리하고, 다섯 번째 글상자에 문밖 지게와 마당 구석 맷돌을 배치했다. 일곱 번째 글상자에는 밤톨/멍석/지게/맷돌의 작은 전조를 추가하고, 선택 직전 주인공 판단도 `필요한 건 이기는 힘이 아니라 멈칫하게 할 한순간`으로 정리해 선택의 이유가 더 직접적으로 읽히게 보강.
- 학생이 읽기 쉬운 문장을 위해 `둔한 그림자를 눌렀다`, `낮게 깔렸다` 같은 추상적 표현을 `흙자국을 남겼다`, `낮게 들렸다`처럼 눈에 보이는 행동/감각으로 교체. `npm run validate:adventure`가 이 어색한 표현들이 돌아오면 실패하도록 보강했다.
- 첫 후보의 자료 근거를 한국문학번역원 한국문학 디지털도서관, KCI 등재 논문, KCI/한국고전연구 자료 중심으로 정리하고, 불안정한 예전 `ciSereArticleSearch` 주소와 끊긴 공연 안내 URL을 제거. 검토용 읽기본에도 자료 링크와 사용 범위를 표시하고, `npm run validate:adventure`가 현재 국내 자료 묶음과 링크 회귀를 검사하도록 보강.
- `docs/adventure-first-episode-approval-audit.md`에 `승인 전 플레이 판정표`와 `승인 보류 기준`을 추가해, 자동 검증으로 통과한 항목과 사용자가 실제 읽고 판단해야 하는 재미/인과/몰입 기준을 분리. `npm run validate:adventure`가 이 승인 전 판정표가 남아 있는지도 검사한다.
- `docs/adventure-first-episode-approval-audit.md`에 `승인 직전 증거 묶음`을 추가해, 런타임 데이터, 검토용 읽기본, 이야기 기준 문서, 자동 검증, Playwright 앱 스모크가 승인 전 최신이어야 한다는 기준을 고정. 최종 재미와 완성도는 사용자의 실제 플레이 판단으로 남긴다.
- `docs/adventure-first-episode-approval-audit.md`에 `사용자 승인 기록`을 추가하고, `npm run validate:adventure`가 사용자 승인 근거 없이 `approvalStatus`만 `approved`로 바뀌면 실패하도록 보강. 현재 검토 후보는 `firstEpisodeUserApprovalEvidence: false`가 정상이다.
- `docs/adventure-first-episode-review-copy.md`의 마지막 장면 섹션에서 실제 첫 후보 플레이에 보이지 않는 공통 예비 문구를 제거하고, 사용자가 검토할 선택 A/B 마지막 장면만 중심으로 정리. `npm run validate:adventure`도 검토용 읽기본 동기화를 선택별 마지막 장면 기준으로 검사하도록 조정.
- 사용자가 지적한 속보식 어색함을 줄이기 위해 첫 후보의 마지막 장면을 다시 다듬었다. 제목은 신문 헤드라인처럼 과장하지 않고 `문턱에는 금 간 그릇이 남았다`, `재가 식기 전, 맷돌이 문턱을 막고 있었다`처럼 장면 안의 남은 물건으로 정리하고, 본문은 호랑이가 물러난 뒤의 침묵, 손등의 재, 문고리 확인, 멍석과 맷돌의 위치처럼 직접 본 행동과 소리 위주로 끝나게 수정.
- 선택 결과 문장도 `옆구리를 받쳤다`, `비어 있던 발목`처럼 동작이 덜 선명한 표현을 `옆구리를 툭 쳤다`, `들썩인 발목`으로 바꾸고, 검토용 읽기본의 줄 단독 `결과:` 라벨을 제거했다. `npm run validate:adventure`가 이 메타 라벨 회귀를 막도록 보강.
- 승인본 전용 데이터 구조도 `newsSummary`/`finalNews...`가 아니라 `endingSummary`/`finalScene` 기준으로 정리했다. 앱 어댑터는 새 필드를 우선 사용하고, `npm run validate:adventure`는 모든 승인본 런타임 항목에 옛 속보 필드가 다시 들어오거나 `finalScene` 구조가 빠지면 실패한다.
- 모험 종료 예비 카드와 직접 귀환 카드의 `한성 일보 속보`, `조선 16XX년`, 엽전 정산 본문을 제거하고, `모험이 끝난 뒤`, `대장간 문턱으로 돌아온 뒤` 같은 마지막 장면형 문구로 낮췄다. 엽전 변화는 계속 별도 `대장간 정산` 패널이 맡는다.
- 적 조우로 모험이 끝나는 경우도 예전 `getCombatNews` 기사 템플릿을 쓰지 않고 `getCombatFinalScene` 후일담 문장으로 분리했다. `긴급 철수`, `모험 보고`, 과장된 신문 헤드라인 대신 골목 흙먼지, 산비탈 발자국, 바닷바람, 강물 소리처럼 장소에 남은 흔적으로 끝나며, 옛 `COMBAT_STORIES` 기사 데이터도 런타임에서 제거했다. 검증 스크립트가 옛 전투 뉴스 함수와 `한성 일보 속보` 예비값의 재유입을 막는다.
- `npm run validate:adventure`, `npm run lint`, `npm run build`, `git diff --check`, `ADVENTURE_SMOKE_URL=http://127.0.0.1:5174/ npm run smoke:adventure` 통과. 인앱 브라우저에서도 첫 글상자, 선택지, 빈 그릇 선택 결과, 선택별 마지막 장면의 새 문구 반영을 확인.

### 2026-06-05 강화 연출 문구/체험 정리
- 강화 판정/결과 문구가 무기 이미지 위를 가리지 않도록 무기 화면 밖 이름판으로 고정.
- 망치질 타격 텍스트와 효과음은 기존처럼 무기 근처에서 겹쳐 보일 수 있게 유지.
- 개발 편의 `강화 연출 체험`에 `+1강화v1`, `+1강화v2`, `강화실패v1`, `강화실패v2`, `+2대강화`, `+3대강화` 버튼 추가.
- 연출 체험은 임시 무기 표시 상태만 사용하고 실제 강화 단계, 계열, 엽전, 복구 모달에는 영향을 주지 않도록 분리.
- 강화 성공/대성공 확정 순간에 새 무기 단계와 이미지를 즉시 적용하도록 변경.
- 새 무기 전환 시 승격 플래시, 이미지 확대/등장, 강화 단계 배지 팝 애니메이션을 추가해 전환 체감을 강화.
- 기존 복구 제안용 대장장이 이미지를 기존 인연 NPC와 비슷한 전신 chibi 픽셀 AI 이미지로 교체.
- 복구 제안용 대장장이 이미지를 `blacksmith_apology.png`, `blacksmith_lookaway.png`, `blacksmith_smirk.png`, `blacksmith_suspicious.png` 4종으로 확장.
- 강화 실패 후 복구 모달이 진심 사죄, 모른 척, 능청웃음, 일부러 그런 듯한 수상함 톤의 이미지와 문구를 랜덤으로 표시하도록 변경.
- 대장장이 대사에 손이 미끄러짐, 재를 밟음, 깨진 조각을 숨기는 듯한 수상함 등 개그성 변명을 추가하고 복구 퀴즈 안내도 선택된 말투에 맞춰 표시.
- 무기 이미지 크로마키 처리 전 원본 RGB PNG가 잠깐 렌더링되어 흰 바탕이 스치는 현상을 막고, 성공/대성공 전환 플래시의 흰 면광을 낮춰 금색/청색 빛 위주로 조정.
- 모험 장면 상세와 로그에서 `맥락`, `단서` 같은 개발자식 분류 라벨을 제거하고, 생활사 설명과 현장 흔적이 자연스럽게 읽히도록 정리.
- 엽전 벌기/모험 게이지 퀴즈 오답 시 정답을 보여주고 선택지를 5초간 잠가, 보상 없이 잠시 기다리는 패널티를 추가.
- 실제 강화 실패는 복구 모달 없이 +1 `낡은 몽둥이`로 돌아가도록 변경.
- 모험 전투/인연/보물/사건 기본 보상을 상향하고, 긍정 장면에는 낮은 확률로 `뜻밖의 후한 사례` 추가 보상이 붙도록 변경.
- 강화 성공 후 무기 계열 경로를 정규화해, 한손/두손 선택과 이어지는 성공/대성공 이름판, 이미지, 저장값이 서로 어긋나지 않도록 수정.
- 최고 단계(+7)에서는 강화/대강화 불가 상태를 명확히 표시하고, 개발 편의 강화 연출 체험도 남은 강화 단계 수보다 큰 흐름은 비활성화.
- 모험 장면 화면을 `제목 → 대표 이미지 → 하단 글상자 → 선택지/결과` 흐름으로 재정리하고, 진행 로그는 기본 화면에서 제거해 이야기 몰입을 우선하도록 변경.
- 위험 사건처럼 손실 로그가 있는 길목은 지나온 길로 돌아봐도 빨강 상태가 우선 표시되도록 모험 결과 상태 계산을 정리.
- 모험 장면 본문을 주인공이 현장에서 보고 겪는 1인칭 기록처럼 다시 작성해, 적/인연/보물/위험 사건이 설명문이 아니라 이야기 속 사건처럼 읽히도록 변경.
- 적 패배 속보가 기존의 단계별 코믹 기사 후보를 실제로 사용하도록 수정하고, 인연/보물/위험/자진 귀환 속보도 사건 전개, 주변 반응, 정산이 들어간 기사형 문장으로 보강.
- `모험 시작` / `모험 게이지` / `팔도 모험`으로 기능명을 정리하고, 주인공을 시간 어긋남을 품은 관점으로 설정.
- 모험 글상자를 해설, 등장인물 대사, 주인공 생각, 선택지 역할로 나누고 `지난 기록` 버튼으로 현재 에피소드 대화 기록을 펼쳐 볼 수 있게 변경.
- 화면에서는 1~7 단계 구조를 드러내지 않고, 내부 최대 조우 수 안에서 여러 길목과 하나의 메인 단편 에피소드가 랜덤으로 지나가도록 변경.
- 메인 단편 에피소드가 끝나면 다음 선택 없이 자동으로 대장간 속보 귀환으로 넘어가고, 그 전에 작은 도적전이나 위험 사건에서 패배하면 더 깊은 이야기를 만나지 못한 채 복귀하도록 정리.
- 장터 소매치기, 나루터 주먹꾼, 봉수대 습격꾼, 왜구 척후, 여진 기마 척후, 이무기 그림자 등 적 변형을 추가.
- 오일장 엿장수, 두레꾼 새참, 서당 학동, 역참 파발, 봉수대 연기, 보부상 장부, 나루터 쌀자루, 장터 할머니 등 단편형 사건/인연을 추가.
- 수상한 훈장님, 주먹밥 속 쪽지, 수상한 약초 꾸러미 등 함정/반전 에피소드를 추가하고, 홍길동 장면에는 `홍길동전`의 서얼 차별과 호부호형 모티프를 반영.
- 해와 달 모티프의 하늘 밧줄, 혹부리 노인 장단, 선녀못 비단 조각, 콩쥐의 깨진 콩자루 등 전래동화 모티프 단편 사건을 추가.
- 새 반전/생활사 시나리오용 이미지 생성 계획을 `docs/adventure-scenario-image-plan.md`로 정리.
- 비슷한 도장/장부/장인 사건도 선택 후 다른 의미로 갈라지도록 `SE-037`, `SE-039`, `SE-079`, `SE-101`, `SE-103`, `SE-111`, `SE-133`, `SE-135`, `SE-185`의 글상자, 선택지, 선택 결과 후보를 수작업 보강.
- `SE-021`, `SE-031`, `SE-038`, `SE-057`, `SE-102`, `SE-104`, `SE-141`, `SE-142`를 추가 보강해 장부·쌀자루·길막·북소리·관문·초소 사건이 같은 겉상황이어도 선택 후 다른 의미로 갈라지도록 정리.
- `SE-022`, `SE-027`, `SE-087`, `SE-091`, `SE-143`, `SE-163`을 추가 보강해 보부상 봇짐, 파발 봉서, 봉인 끈, 군량 자루, 호패 끈 사건이 물건 단서와 사람 반응에 따라 다르게 읽히도록 정리.
- `SE-164`~`SE-168`을 추가 보강해 상투, 꽃신, 혼례 가마, 빨래터 물빛, 우물 바가지 사건이 마을 생활 장면처럼 비슷해 보여도 가짜 권세, 아이의 길표, 신부에게 벌어 준 시간, 염료·녹물 혼동, 우물물 경고로 다르게 전개되도록 정리.
- `SE-043`, `SE-044`, `SE-117`, `SE-118`, `SE-121`~`SE-125`, `SE-169`, `SE-170`을 추가 보강해 종각, 돌탑, 의례 행렬, 탈춤/광대패, 붉은 실, 장승, 성황당 사건이 비슷한 민속 장면처럼 보여도 아이의 경고, 길막 표시, 마지막 인사, 풍자, 안전선, 도둑 신호, 가족의 소원처럼 선택 후 다르게 갈라지도록 정리.
- `ME-027` 관아 앞 북소리는 신문고·격쟁·지방 관아 청원을 섞지 않고 조선 지방 관아의 원정/소지 문서 접수 장면으로 고정해, 접힌 문서·아전 소매 먼지·문턱 절차 압박이 선택 결과로 이어지도록 개별 보강.
- 짤막한 사이드 이벤트 확장을 위해 시장, 서당, 관아, 집안일, 역참·군영, 강·바다·산길, 전래 모티프, 시간여행 단서 계열의 미래 이미지 후보 48개를 `docs/adventure-scenario-image-plan.md`에 제작 필요 백로그로 기록. 현 단계에서는 이미지 생성이나 런타임 연결 없이, 한 사건 한 시대상황 기준과 한국적 소품·장소 기준만 남김.
- `SE-171`~`SE-178`을 추가 보강해 장기판, 바둑돌, 가야금, 문지방 붉은 실, 물레, 베틀, 목화밭 손수건, 물레방아 사건이 조용한 생활 장면에서 시작하더라도 아이의 체면, 방향 표시, 낮은 음악 신호, 병든 아이 보호, 실끝 단서, 숨긴 쪽지, 밭고랑 도움 요청, 물길 다툼으로 다르게 전개되도록 정리.
- `SE-179`~`SE-184`를 추가 보강해 염전 발자국, 갯벌 삿대, 조개 바구니, 포구 창고 문, 산비탈 낮은 연기, 나무꾼 도끼 사건이 소금 노동자의 누명, 물때 경고, 굶은 집에 나눈 조개, 밤배를 피해 숨은 사람, 아이의 낮은 구조 신호, 도적 길목 경고로 갈라지도록 정리.
- `SE-186`~`SE-191`을 추가 보강해 산중 약수터 물그릇, 기와 조각, 놋숟가락, 식은 아궁이, 두 자물쇠 쌀뒤주, 효자비 사건이 약수터 경고, 숨긴 물건 표시, 굶은 집의 도움 요청, 숨은 사람을 지키는 연기 숨김, 아이 이름과 곡식을 지키는 장치, 평판 과시로 갈라지도록 정리.
- 위 사건들에 필요한 새 이미지는 생성하지 않고, `docs/adventure-scenario-image-plan.md`에 표정/자세/소품 차이 중심의 이미지 큐로만 기록.
- 모험 런타임에서 `스쳐가는 사건` 같은 사건 유형 설명 배너를 제거해, 독자가 장면의 역할을 메타 정보로 먼저 알지 않도록 정리.
- 사이드 이벤트 선택 후 바로 다음 길목으로 넘어가지 않고, 선택 결과와 실제 정산을 확인한 뒤 `발걸음 이어가기`로 이어가는 흐름으로 변경.
- 진행 기록, 단계 도트, 쪽수 표시를 기본 모험 화면에서 제거하고 미연시/시뮬레이션 게임처럼 대표 이미지와 하단 글상자, 선택지가 중심이 되도록 UI를 정리.
- 테스트 단계에서는 모험 게이지/쿨타임 패널을 숨기고 `모험 시작` 버튼으로 바로 모험을 열 수 있도록 임시 플래그를 적용.

### 2026-06-04
- GitHub 저장소 `LUCKYBRIDGE/weapon-reinforce`를 `apps/weapon-reinforce`로 클론.
- 소문자 `agents.md`를 프로젝트 단위 `AGENTS.md` 최소 계약으로 정리.
- 팔도 모험 단계 페이지에 전장 시각화 레이어 추가.
- 실패 후 뉴스 페이지가 다음 스테이지 도트와 겹쳐 보일 수 있는 탐색 조건 정리.
- ESLint 설정에서 브라우저 게임 이벤트 랜덤성에 맞지 않는 React Compiler 순수성 규칙 완화.
- `npm run lint`, `npm run build`, Playwright 데스크톱/모바일 렌더링 검증 완료.

### 2026-06-04 추가 작업
- 팔도 모험 적 픽셀 이미지 7종(`enemy_stage_1.png` ~ `enemy_stage_7.png`) 생성 및 연동.
- 왜구 이미지는 해안 약탈 도적단 기록을 바탕으로 남루한 약탈꾼 톤으로 재생성.
- Web Audio 기반 효과음과 효과음 토글 추가.
- `npm run lint`, `npm run build`, Playwright 1단계 적 이미지 렌더링 검증 완료.

### 2026-06-04 밸런스 조정
- 팔도 모험 성공 공식 변경: `기본 성공률 + (강화 수치 - 1) * 7%`, 5%~90% 캡.
- 팔도 모험 패배 시 +2 이상 무기는 8% 확률로 파손되어 낡은 몽둥이 +1로 복귀.
- 각 단계 성공 후 자동 다음 단계 진입 대신 `계속 모험` 또는 `대장간 귀환`을 선택하도록 변경.
- 모험 시작 후 5분이 지나야 재모험 가능하며, 쿨타임 중 풀무질 퀴즈 5문제를 맞히면 모험 게이지가 차서 즉시 재모험 가능.
- 보상 정산 문구를 "입금" 대신 "보상을 얻었습니다" 계열로 정리.
- 기능명을 `모험 시작`, `모험 게이지`, `팔도 모험`으로 통일.

### 2026-06-04 강화 연출 조정
- 강화 연출 시간을 약 3.9초로 늘리고 망치질 타격을 여러 차례 연속 발생하도록 변경.
- 결과 직전 판정 대기는 중앙 배너가 아니라 무기 화면 밖 이름판으로 표시하고, 고조된 화로/무기 발광 애니메이션 추가.
- 결과 이름판과 로그 문구를 `강화 성공!`, `강화 실패...`, `대성공!` 톤으로 정리.
- `신비한 대장간` 제목 우측에 `모드전환`과 `전체화면` 버튼 추가.
- 팔도 모험의 적을 단계 고정형에서 랜덤 장면형으로 변경하고, 후반 장면일수록 위험도 높은 적의 등장 가중치가 커지도록 조정.
- 대장간 무기 표시 크기와 후광/드롭섀도를 강화해 배경에서 더 잘 보이도록 조정.

### 2026-06-04 퀴즈/모험 리스크 밸런스 조정
- 엽전 벌기는 구구단만, 깨진 무기 복구와 모험 게이지 충전은 나눗셈만 출제되도록 퀴즈 목적을 분리.
- 강화 성공률을 85/70/50/35/20/8%로 낮추고, 복구 성공률을 10~50%로 하향.
- 복구 실패 또는 복구 포기 시 무기가 항상 1강으로 리셋되지 않고 정답 수/상황에 따라 몇 단계 하락하도록 변경.
- 모험 적 조우 가중치를 완화해 초반 길목에서도 고위험 적이 낮은 확률로 등장할 수 있게 변경.
- 모험 장면 풀에 보물/도움 이벤트와 부정 이벤트를 추가하고, 부정 이벤트 또는 패배 시 보상 손실, 보유 엽전 손실, 무기 단계 하락이 발생할 수 있게 변경.
- 모험 장면 설명, 로그, 정산 문구를 짧은 판단형 문장으로 정리해 성공률/보상/손실을 빠르게 읽을 수 있게 개선.
- 모험 성공/실패 상태의 시각 구분을 강화해 성공은 초록, 실패/손실은 빨강 배경·테두리·로그 색으로 명확히 표시.
- 유명 위인 카메오 대신 동네 주민, 훈장님, 떠돌이 의원 등 생활형 인연 장면을 추가하고, 현재는 각 장면이 독립 단편으로 랜덤 등장하도록 조정.
- 향후 적/인연/사건 이미지는 우리역사넷, 국가유산포털, 한국민족문화대백과, 국립민속박물관, 국립중앙박물관/e뮤지엄 등 대한민국 자료를 우선 근거로 생성하고, 중국풍·일본풍 복식/무기/건축 실루엣은 검수 탈락 기준으로 명시.
- 이미지 재생성 1차 후보를 `public/images/candidates/`에 저장. 홍길동, 여진족 기병, 뒷골목 깡패 후보와 동네 주민, 훈장님, 떠돌이 의원 인연 후보를 생성하고, 후보 몽타주를 `candidate_montage_v1.png`로 정리.
- `knol-jump`의 세종, 이순신, 허난설헌 스프라이트와 비교해 새 후보들이 과하게 RPG 일러스트풍인 문제를 확인하고, 큰 머리/짧은 몸/굵은 윤곽선/단순 색면 기준의 `knol_style` 후보 6종과 비교 몽타주를 추가 생성.
- 앞으로 인물형 적/인연/사건 이미지를 생성할 때 `knol-jump` 세종, 이순신, 허난설헌 정면 스프라이트 파일을 필수 스타일 참조로 쓰도록 `AGENTS.md`, `docs/image-regeneration-brief.md`, `docs/walkthrough.md`, `CHECKLIST.md`에 기준을 고정.
- 상단 헤더에서 `Weapon Reinforce` 텍스트를 제거하고 `신비한 대장간`, `무기 도감`, `효과음`, `모드전환`, `전체화면`이 한 줄에 놓이도록 헤더를 압축 정리.
- 강화 성공 후 낮은 확률로 대성공이 발생해 일반 성공(+1)이 +2 또는 +3 강화로 이어질 수 있게 추가. +3 대성공의 두 번째 추가 강화 전에는 `어어? 설마...한번 더?` 문구를 표시하며, 최대 +7을 넘지 않도록 제한.
- 대성공 연출을 일반 성공 파티클 재사용에서 분리해 배경 암전, 청금색 오라, 확산 링, 광선/보너스 파티클, 강조된 대성공 이름판, 더 긴 대기 타이밍을 추가.
- 개발 편의 기능으로 강화 성공/실패/대성공 연출 체험 버튼을 추가해 실제 강화 결과 없이 여러 연출만 재생할 수 있게 구성.
- 강화 판정 직전 긴장 효과음을 추가하고, 실패 연출을 빛이 붙은 듯한 고조 후 깨지는 실패와 예고 없이 바로 깨지는 실패로 랜덤 분기. 실패 확정 시 금 가는 소리와 파손음을 별도 합성 효과음으로 재생.
- 강화 성공/실패/대성공 결과가 뜨기 전 대기 시간을 고정값이 아니라 작은 랜덤 범위로 흔들리게 조정하고, 대성공처럼 보였다가 추가 강화 없이 +1에서 끝나는 추가 강화 착시 흐름을 추가.
- 대성공 결과 문구를 `대성공!`으로 압축하고, `...오잉?` 이후 실제 대성공 결과가 뜨기까지의 대기 시간을 더 길게 조정.
- 대성공 대기 문구는 무기 화면 밖 이름판으로 옮겨 중앙 중복 배너를 제거하고, 성공/실패/대성공/추가 강화 착시 결과 문구는 `강화 성공! 환두대도`처럼 무기명과 이어져 표시되도록 정리.
- 대성공, 성공처럼 보이다 실패하는 흐름, 대성공처럼 보이다 멈추는 흐름의 중간 대기 시간을 늘리고 `안쪽의 결`, `숨은 힘`, `남은 잠재력` 계열 문구로 결과 확정 전 애간장 타는 구간을 강화.
- +3 대성공의 두 번째 추가 강화는 첫 대성공 직후 바로 이어지지 않고, 끝난 듯한 정적과 `조용히 빛을 머금는다...` 전조를 거친 뒤 `어어? 설마...한번 더?`로 다시 고조되도록 타이밍을 늘림.
- 대성공 대기 구간이 비어 보이지 않도록 `깡!`, `챙!`, `카앙!`, `쾅!` 망치질 타격을 중간중간 추가하고, +3 두 번째 추가 강화 전 정적 구간에도 재점화 타격을 배치.
- 개발 편의 강화 연출 체험은 대성공 구간으로 바로 진입하지 않고, 일반 강화 망치질과 판정, `강화 성공!` 이후 끝난 듯한 정적을 거쳐 무기의 숨은 결과 잠재력을 끌어내는 추가 대성공 전체 흐름으로 변경.
- 성공/대성공 결과 이름판이 다음 강화 흐름으로 바로 사라지지 않도록 유지 시간을 늘리고, 성공/대성공/실패 확정 순간에 모루 화면 진동 애니메이션을 추가.
- 대성공 중간 문구를 단순히 상태가 이상하거나 빛난다는 표현에서 `안쪽의 결`, `숨은 힘`, `남은 잠재력`을 대장장이가 끌어내는 표현으로 조정.
- 실제 대성공 확률을 +2 대성공 3%, +3 대성공 1%로 낮추고, 추가 강화 착시도 4%로 낮춰 대성공 계열 사건이 매우 드물게 발생하도록 조정.
- 모험의 인연/보물/위험/사건 장면은 아군 무기와 대상이 마주 보는 전투 구도를 제거하고, 사건 이미지나 아이콘을 중앙에 크게 보여주는 단독 장면으로 변경.
- 모험 단계 성공 후 로그의 선택 중복 설명을 제거하고, 선택 패널은 현재 보상과 두 행동만 짧게 보이도록 압축.
- 모험 장면별 설명에 한국사/민속/생활사 분위기와 현장 흔적을 추가해, 중복 없이 읽을거리를 되살림.
- 팔도 모험을 시뮬레이션 게임식 글상자 흐름으로 변경하고, 해설/등장인물/주인공/선택 글상자를 역할별로 구분하며 `지난 기록`에서 역할명이 중복 노출되지 않게 정리.
- 모험은 7장면 고정 진행처럼 보이지 않도록, 여러 자잘한 사건 사이에 숨은 메인 단편 에피소드를 하나 배치하고 해당 이야기가 끝나면 마지막 글상자를 읽은 뒤 자연스럽게 속보/정산으로 돌아가도록 조정.
- 전래동화 모티프 단편과 함정/반전/딜레마 사건을 추가하고, 새 이미지가 필요한 28개 사건/적/인연/위기 파일명을 `docs/adventure-scenario-image-plan.md`에 관리.
- 새 이미지가 아직 없는 사건은 기존 이미지를 임시 fallback으로만 사용하고, `enemy_market_pickpocket.png` 등 대상 PNG가 추가되면 코드 수정 없이 새 이미지가 우선 표시되도록 런타임 이미지 매핑을 변경.
- 모험 선택지와 장면 요약에서 보상/손실/무기 손상 같은 결과 정보를 미리 보여주지 않도록 변경하고, 정산 숫자는 돌아온 뒤 속보/정산 화면에서만 확인하도록 조정.
- `docs/adventure-story-catalog.md`에 시간여행자 관점의 메인 에피소드 80개와 자잘한 이벤트 200개를 기획 형태로 작성. 비슷한 겉상황이라도 숨은 원인, 선택 이유, 분기 논리가 달라지도록 설계 기준을 고정.
- 이미지 생성은 이야기 승인 뒤로 미루고, `docs/adventure-scenario-image-plan.md`에 할머니/훈장님/주민/의원/사공 등 반복 인물의 일관된 표정·동작 세트와 에피소드별 필요 이미지 큐 기준을 기록.
- 80/200 카탈로그를 `src/data/adventureCatalog.js`로 변환하고 모험 후보 풀에 연결. 메인 80개와 사이드 200개 모두 런타임 글상자와 선택별 결과 후보를 가진 분기형 데이터로 보강.
- `src/data/adventureBranchVariants.js`에 장터 할머니, 훈장님, 주먹밥 주민, 나룻배, 젖은 장부, 봉수대 연기처럼 비슷해 보이지만 다르게 전개되는 초기 런타임 변주 묶음을 추가.
- 떠돌이 의원, 혼례 꽃신, 장승/성황나무, 호패/마패 길목 사건을 추가해 런타임 변주 사건을 1차 32개로 확장. 같은 겉상황이라도 선의, 착각, 장난, 함정, 공무, 도난으로 전개가 갈라지도록 구성.
- `docs/adventure-source-validation.md`에 장시/보부상, 홍길동전/서얼, 봉수/마패, 나룻배, 혼례, 장승/성황나무, 호패, 한지/옹기, 전래동화 모티프의 한국 자료 기준과 이미지 생성 가드레일을 기록.
- 사건형 장면은 결과를 먼저 계산하지 않고, 글상자를 끝까지 읽은 뒤 `주인공의 선택` 버튼을 눌렀을 때 선택별 결과 후보가 뽑히도록 변경.
- 선택 질문이 글상자와 선택 패널에 중복 표시되지 않도록, 이야기 본문은 장면 묘사로 끝나고 실제 선택은 선택 패널에서 한 번만 표시되게 정리.
- 1차 변주 사건 32개의 필요한 이미지는 생성하지 않고 `docs/adventure-story-catalog.md`와 `docs/adventure-scenario-image-plan.md`에 이미지 키와 표정/동작 차이 기준만 기록.
- 메인 에피소드 `ME-001`~`ME-080`은 공통 설명문이 아니라 한국어 해설/대사/주인공 글상자와 선택별 결과 후보가 있는 런타임 분기형 이야기로 보강.
- 사이드 이벤트 `SE-001`~`SE-200`은 기존 `길 위에서...` 공통 문장 셸 대신 해설/대사/주인공 글상자와 실제 선택 라벨, 선택별 결과 후보를 가진 런타임 분기형 데이터로 보강.
- 사이드 이벤트 결과 로그도 `작은 흔적들이 한 줄로 이어졌다` 같은 공통 생성 문구 대신, 각 이벤트의 숨은 원인과 선택 내용에 묶인 결과 문장으로 보강.
- 카탈로그 사이드 이벤트 선택은 곧바로 귀환으로 끝나지 않고, 사건 선택 결과를 본 뒤 다음 길목으로 이어질 수 있도록 `choiceEndsAdventure: false`를 연결.
- `scripts/validate-adventure-catalog.mjs`와 `npm run validate:adventure`를 추가해 메인 80개, 사이드 200개, 현재 변주 50개, 결과 미리보기 금지, 이미지 큐 존재, 메인/사이드 분기형 진행률을 검사.
- 현재 검증 기준으로 메인 에피소드 80개와 사이드 이벤트 200개 모두 런타임 분기형 보강 완료, 공통 런타임 문장 셸은 0개로 정리. 사이드 이벤트 단서 문장 최대 반복은 4회로 제한.
- `npm run validate:adventure`가 `ME-001`~`ME-080`, `SE-001`~`SE-200`, `BV-001`~`BV-050` source ID 연속성과 중복 source ID 0개를 검사하도록 강화. `enrichedMainEpisodes`도 최소값이 아니라 80개 전부를 요구하도록 조정.
- 사이드 이벤트 결과 로그의 `A, B, 아니면 C`식 기획 메모형 문장을 짧은 반전 문장으로 변환하고, `주변에는 말투와 손끝`, `우물 바가지 주변에는`, `산길에 소금 주변에는` 같은 생성 문장 오류를 검증 기준에 추가.
- 메인 에피소드 80개는 각 선택 분기마다 성공/실패 결과 후보가 모두 섞이도록 정리하고, `mainBranchesWithoutMixedOutcomes` 검증값이 0인지 검사하도록 추가.
- 사이드 이벤트 200개의 첫 문장이 `팔도 길목에서 ... 나는 지나치려다 발을 늦췄다` 단일 틀로 반복되지 않도록 장소/리듬별 시작 문장 변주를 추가하고, 선택 질문 최대 반복 수를 1회 이하로 검사.
- 사이드 이벤트 결과 로그의 따옴표형 숨은 원인 설명과 `~이라는 말 뒤에` 같은 메모형 요약을 제거하고, `noteLikeOutcomeSideEventsRemaining` 검증값이 0인지 검사하도록 추가.
- 모든 메인/사이드/변주 사건이 해설·대사·주인공 글상자, 말하는 이, 충분한 본문, 서로 다른 선택 라벨을 갖추는지 `storyStructureIssues`로 검사하고, 전체 선택 분기에 성공/실패 후보가 모두 섞이는지 `runtimeBranchesWithoutMixedOutcomes`로 검사.
- 변주 사건 50개는 export 단계에서 대사 바깥 따옴표를 제거하고 `sceneTerrain`을 추론해 붙이도록 정규화. 전체 런타임 대사의 `quotedDialogueEntries`와 변주 장소의 `branchVariantsMissingSceneTerrain`이 0인지 검사한다.
- 31자 미만으로 짧던 변주 사건 결과 로그 27개를 보강해 선택 행동 뒤의 단서, 지연, 놓침, 인물 반응이 보이도록 수정. `minRuntimeOutcomeLogLength`와 `shortRuntimeOutcomeLogsRemaining`를 추가해 메인/사이드/변주 전체 결과 로그가 짧은 반응문으로 퇴행하지 않도록 검사한다.
- 전부 성공으로만 끝나던 변주 사건 5개의 안전 선택 후보를 조정해, 기다리기/믿기/돌아가기 선택도 사건 안의 놓침이나 지연으로 손해가 날 수 있게 수정.
- 마지막 사이드 이벤트 `SE-192`~`SE-200`은 짚신 길표/증거, 가마꾼 붉은 천, 빈 고삐 신고, 매사냥 장갑, 빈 화살통, 낯선 말투, 소매 실밥, 늦은 그림자, 내일 비 냄새처럼 비슷해 보이는 단서가 선택에 따라 다르게 흘러가도록 수작업 분기와 이미지 큐를 보강.
- 모험의 메인 단편 에피소드 풀은 예전 전투 적이 아니라 `ME-001`~`ME-080` 카탈로그 메인 에피소드만 뽑도록 정리하고, `npm run validate:adventure`가 이 조건을 검사하도록 추가. 예전 적 조우는 깊은 이야기 전의 작은 위험/전투로만 남긴다.
- 예전 보조 조우의 `fieldStory` 문장에 남아 있던 보상/손실/정산 예고 표현이 선택 전 글상자에 그대로 나오지 않도록 `sanitizeAdventurePreviewText`를 추가. 결과 뉴스와 정산 로그는 유지하되, 사전 이야기 글상자는 짐/흔적/곤란처럼 결과를 숨기는 표현으로 렌더링하고 검증기가 이 필터 적용을 확인한다.
- 한 번의 모험 안에서는 같은 조우 ID뿐 아니라 같은 `variantGroup`이나 같은 제목의 장면도 다시 뽑히지 않도록 `getDeployEncounterRunKeys`를 추가. 비슷한 사건 묶음은 여러 모험에 걸쳐 다르게 보이되, 한 모험 안에서 같은 표면 사건이 반복되는 느낌을 줄인다.
- 위험 사건 선택으로 모험이 끝날 때 누적 보상 손실과 보유 엽전 손실을 분리 계산하되, 마지막 장면/정산에는 합쳐진 순손익을 표시하도록 보강. 선택 전에는 손실을 예고하지 않고, 끝난 뒤에는 실제로 손해를 본 결과가 숨지 않도록 `eventHazardNetSettlementConfigured` 검증을 추가.
- 비슷한 겉상황의 변주 사건도 숨은 원인, 선택 질문, 선택 라벨, 이미지 키가 서로 갈라지는지 `underdiverseVariantGroups`로 검사하고, 한국 맥락 소재가 80/200 런타임 카탈로그에서 빠지지 않도록 `coverageSummary` 기준을 추가.
- 시간여행자 관점이 설명문처럼 드러나지 않도록 `ME-038`, `ME-044`, `ME-074`에 소매·실밥·말투·바느질의 작은 어긋남을 추가하고, `coverageSummary.timeHint.main` 최소 기준을 6개로 강화.
- 메인 80개와 사이드 200개의 화면 장소 라벨이 `팔도 길목` 기본값으로 남지 않도록 장소 추론 사전을 확장. 서원/향약/광대패/역관/못가/봉서/연기 색 같은 단서도 세책방 골목, 장터 모퉁이, 관아 앞길, 산길, 역참 길목 등으로 연결하고 `genericSceneTerrainEntries` 0개를 검사한다.
- 메인 에피소드 보조 기록에서 반복되던 `시간여행자라는 사실을 숨긴 채` 직접 설명을 `옷깃과 말끝을 낮춘 주인공`식 현장 묘사로 교체하고, `directTimeTravelerExposition` 검증값을 추가해 사용자 표시 텍스트에 직접 설명이 다시 나오지 않도록 차단.
- 메인 에피소드 보조 기록 생성 방식을 6개 묶음 반복에서 줄별 20개 템플릿 조합으로 확장하고, 제목을 걷어낸 뒤 같은 문장 구조가 반복되는 정도를 `maxRepeatedMainLogSkeleton` 4회 이하로 검사하도록 추가.
- 사이드 이벤트 선택 질문을 사건 초점, 장소, 선택 행동을 조합하는 방식으로 바꿔, 같은 선택 질문이 11회 반복되던 흐름을 정확 반복 0개로 낮추고 `maxRepeatedSideChoicePrompt` 기준을 1회 이하로 강화.
- 사이드 이벤트 첫 해설 생성식을 제목 재진술 방식에서 인물·장소·소품별 장면 묘사 방식으로 교체하고, `sideNarrationTitleEchoes` 검증값을 추가해 제목이 해설 문장에 그대로 반복되지 않도록 차단.
- 군영/피란/초소 계열 첫 해설 묘사를 추가하고, 메인/사이드 카탈로그에는 `sceneTerrain` 장소 라벨을 저장. 변주 사건은 제목·단서에서 장소를 추론해 화면 라벨에 사용하며 `missingSceneTerrainEntries` 0개를 검사.
- 메인/사이드/변주 대사는 글상자 역할 라벨과 중복되지 않도록 생성 또는 정규화 단계에서 바깥 따옴표를 제거한다. `ME-036`의 짧은 대사도 새 산적이 두목을 말리는 맥락이 읽히도록 보강했고, `quotedDialogueEntries` 0개를 검사한다.
- `docs/adventure-scenario-image-plan.md`에 선택별로 달라지는 비슷한 사건의 이미지 단서 기록을 추가. 종이문 쪽지, 금 간 사발, 우물 바가지, 장터 북, 짚신길, 꽃신, 비 오는 보따리 사건은 아직 이미지를 만들지 않고 나중에 일관된 한국 픽셀풍 세트로 생성하도록 큐에 남김.
- 메인 에피소드 결과 로그 1차 수작업 보정: `ME-001`, `ME-004`, `ME-006`, `ME-007`, `ME-012`, `ME-013`, `ME-023`, `ME-059`의 선택 결과가 더 구체적인 행동, 단서, 사회적 맥락, 실패 원인으로 이어지도록 수정하고 `src/data/adventureCatalog.js`를 재생성.
- 사이드 이벤트 결과 로그 생성 템플릿을 선택 유형별로 확장하고, 살피기/확인 계열은 성공·실패 각각 24갈래까지 늘려 반복감을 낮춤. `maxRepeatedSideOutcomePhrase` 검증 기준을 16회 이하로 강화했고 현재 최대 반복은 14회. `같 주변`, `다로 이어졌다`, `시간 낯선`, `산길은 평소처럼 분주`, `다는 쪽`, `확인를`, `설명를` 같은 생성 한국어 오류를 추가 차단.
- 전래동화/시간여행 사이드 이벤트 생성 보정: 하늘 밧줄은 사공 대사 대신 작은 목소리로 처리하고, 도깨비 장단/활빈당 표식/늦은 그림자/낯선 말투/낡은 짚신/연기 색 같은 초점 문구가 자연스럽게 나오도록 수정.
- 이미지 생성은 계속 보류하고, 비슷한 사건 이미지도 표정·자세·소품 각도·단서 위치가 달라져야 함을 `docs/adventure-scenario-image-plan.md`의 `Similar-Incident Image Rules`에 기록.
- 처마 밑 젖은 보따리, 우물가 바가지, 장터 북소리, 산길 짚신처럼 같은 겉상황에서 약 꾸러미/가짜 장부/장난, 쪽지/염료/낡은 줄, 장난/도둑 신호/공무 신호, 어머니 표시/도적 미끼/사냥꾼 경고로 갈라지는 변주 사건 12개를 추가해 첫 44개 런타임 변주 묶음을 구성.
- 새 변주 사건에 필요한 이미지는 생성하지 않고 `branch_rain_bundle_*`, `branch_well_bucket_*`, `branch_market_drum_*`, `branch_straw_shoe_*` 이미지 키와 표정/동작/단서 위치 차이를 `docs/adventure-story-catalog.md`와 `docs/adventure-scenario-image-plan.md`에 기록.
- 한지 문틈 쪽지와 가마 앞 금 간 사발처럼 같은 겉상황에서 도움 요청/가짜 문서 미끼/견습 장난, 견습 실수/밤길 표식/도공의 불길 읽기로 전개가 달라지는 변주 사건 6개를 추가해 런타임 변주 사건을 50개로 확장.
- 새 변주 사건에 필요한 이미지는 생성하지 않고 `branch_paper_window_*`, `branch_kiln_bowl_*` 이미지 키와 한지 문살, 접힌 종이, 붉은 도장, 견습 아이, 가마, 금 간 사발, 숯바구니 단서 차이를 `docs/adventure-story-catalog.md`와 `docs/adventure-scenario-image-plan.md`에 기록.
- 예전 모험 이벤트의 선택 전 문구에서 보상/손실/안전 예고와 `🏠 ... 귀환`식 시스템 라벨을 제거하고, `npm run validate:adventure`가 앱 내부 `choicePrompt`/선택 라벨과 카탈로그 숨은 원인 누출까지 검사하도록 보강.
- 사이드 이벤트의 범용 대사를 시간여행 힌트, 전래동화, 홍길동/신분, 생활사, 관아/공무, 장인, 물길, 장터, 아이 목격자 소재별로 나누고 사건 초점과 장소를 섞어 생성하도록 보강. `maxRepeatedSideDialogue` 검증 기준을 5회 이하로 강화했고 현재 최대 반복은 4회.
- 전래·민속 사이드 이벤트 4차 수작업 보강: `SE-033`, `SE-034`, `SE-036`, `SE-126`, `SE-127`, `SE-128`, `SE-130`, `SE-131`, `SE-132`를 비슷한 전래동화 표면 속에서도 구조 신호, 안전 표시, 미끼, 누명, 아이 증언, 숨긴 물건으로 다르게 전개되도록 교체.
- 새 전래·민속 사건 이미지는 생성하지 않고 `side_se_033_sky_rope_rescue_signal.png`, `side_se_127_two_rope_shadow_safety_mark.png`, `side_se_128_cut_bean_sack_child_frame.png` 등 필요한 컷과 한국식 산길/마을/장터 단서 차이를 `docs/adventure-scenario-image-plan.md`에 기록.
- 사이드 이벤트 결과/보조 문장의 조사 오류를 추가 보정. `큰길 선택는`, `큰 무기를 보고 수레를 부탁한다를`, `생선 냄새가 없 주변`, `의원 뒤따르자`, `처마 찾자`처럼 몰입을 깨는 생성형 표현을 검증 금지 패턴에 추가하고, 행동형 선택 문장을 자연스러운 서술로 변환하도록 카탈로그 생성기를 보강.
- 변주 사건 선택 버튼의 `🏠` 반복 라벨을 제거하고 듣기, 살피기, 기다리기, 알리기, 돕기 같은 실제 장면 행동 아이콘으로 교체. `homeIconChoiceLabelsRemaining` 검증값을 추가해 모험 선택지가 시스템 귀환 버튼처럼 보이지 않도록 검사.
- 사이드 이벤트 선택지 표시 라벨을 보정해 `보기`/`묻기`/`살피기` 어미 쏠림을 줄이고, `흔적 읽기`, `차분히 짚기`, `대답 기다리기`, `표정 읽기`, `말끝 붙잡기`, `한 박자 기다리기` 같은 장면 행동으로 더 다양화. `maxRepeatedChoiceLastWord` 검증 기준을 65회 이하로 강화하고 `systemNavigationChoiceLabelsRemaining` 검증값을 추가.
- 사이드/변주 선택지와 결과 로그의 생성형 반복을 추가 정리. `말 듣기`, `말없이 보기`, `끝까지 따라보기`, `준비한 거짓말만 더 길어졌다` 같은 기계적 문구를 제거하고, `awkwardChoiceLabels`와 `awkwardOutcomeLogs` 0개 및 `maxRepeatedChoiceLastWord` 65회 이하를 검사한다.
- 비슷한 겉상황의 런타임 변주 사건은 같은 그룹 안에서 숨은 원인, 선택 질문, 두 선택 라벨, 이미지 키가 정확히 반복되지 않도록 `duplicateVariantFieldGroups` 검증값을 추가. 이미지는 생성하지 않고 필요한 표정·자세·소품 차이는 이미지 계획 문서의 큐로 남긴다.
- 사이드 이벤트 보조 기록에서 `OO 장면에서 두 선택의 흐름을 가늠했다`, `사건은 작아 보였지만...`처럼 개발자식 공통 문구가 반복되던 흐름을 제거. 각 보조 기록은 사건 초점, 장소, 선택 행동, 현장 단서를 섞어 생성하고 `genericSideSupportLogsRemaining` 0개 및 `maxRepeatedSideSupportLog` 4회 이하를 검사한다.
- 메인 에피소드 결과 로그 중 28자 미만으로 짧아 인과가 약하던 10개 결과를 수작업 보강하고, `minMainOutcomeLogLength`와 `shortMainOutcomeLogsRemaining` 검증값을 추가해 메인 결과가 지나치게 짧은 반응문으로 퇴행하지 않도록 검사.
- 메인 에피소드 결과 로그 2차 수작업 보강: `ME-002`, `ME-008`, `ME-009`, `ME-010`, `ME-011`, `ME-015`, `ME-016`, `ME-017`, `ME-018`, `ME-027`, `ME-029`, `ME-030`, `ME-031`, `ME-034`, `ME-035`, `ME-036`, `ME-037`, `ME-039`, `ME-040`, `ME-044`, `ME-049`, `ME-055`, `ME-056`, `ME-057`, `ME-058`, `ME-071`, `ME-073`의 28~30자급 짧은 결과를 단서와 인물 반응이 보이는 문장으로 늘리고, 메인 결과 로그 최소 길이 검증 기준을 31자로 강화.
- 메인 에피소드 보조 로그가 `사건의 앞뒤를 차분히 살폈다` 같은 공통 fallback 문장으로 반복되던 흐름을 제목별 한국어 기록으로 교체. `maxRepeatedMainLogLine` 4회 이하, `genericMainFallbackLogsRemaining` 0개, `englishRuntimeTextLeaks` 0개를 검사해 영어 메타데이터나 반복 보조 로그가 사용자 글상자/기록에 섞이지 않도록 보강.
- 메인 에피소드 3차 수작업 보강: `ME-025`, `ME-030`, `ME-043`, `ME-047`, `ME-052`, `ME-053`, `ME-064`, `ME-069`, `ME-070`, `ME-071`의 곡식 말, 금난전권, 염전, 나전, 하인/부엌 노동, 효자비, 과부의 뒤주, 목민관 장부, 소금장수 빚 문서, 빨래터 붉은 물 사건을 다시 써 선택의 성공/실패가 현장 단서와 사회적 압박으로 이어지도록 보강.
- 생성 카탈로그의 fallback 표시 문장은 `surfaceHook`, `era`, `people`, `hiddenCause`, `branchLogic` 같은 기획 메타데이터를 직접 읽지 않고, 제목과 한국어 장소 라벨만 쓰도록 제한. `catalogMetadataFieldLeaks` 0개를 검사해 비슷한 사건의 숨은 원인이나 영어 기획 메모가 화면에 노출되지 않게 했다.
- 메인 에피소드별 소재 근거 검증을 추가. 동아줄, 변방 수비, 산적, 세책방, 광대패, 화전민, 효자비, 목화/무명, 꽃신/혼례, 빨래터, 청계천/석공, 장군당/마을 신앙처럼 한국사·전래·생활사 축에 속하는 소재를 인식하고 `mainEpisodesWithoutGrounding` 0개를 검사한다.
- 사이드 이벤트 해설 시작 템플릿을 20갈래로 확장하고 `내 눈에는` 같은 어색한 생성 표현을 제거. `maxRepeatedSideNarrationPrefix` 16회 이하와 `sideCornerOpeningCount` 12회 이하를 검사해 첫 문장 리듬이 몇 가지 틀로 몰리지 않도록 보강.
- 동네 주민, 훈장님, 떠돌이 의원 인연 이미지는 `knol_style` 후보를 정식 자산으로 연결하고, 보물상자/상단 호위/산신 제단/군량 꾸러미/통행세/진흙 협곡 사건 이미지를 512px 런타임 자산으로 추가.
- 생성 이미지 검수 중 서양식 보물상자 느낌이 강한 1차 보물상자와 일본 신사 장식처럼 보일 수 있는 1차 산신 제단은 런타임 연결에서 제외하고 재생성본으로 대체.
- 새 장면 이미지가 흰 배경 박스로 뜨지 않도록 가장자리 배경색만 투명 처리하는 방식으로 이미지 렌더링을 보강.
- GitHub Pages 배포에서 이미지가 깨지지 않도록 Vite production base를 `/weapon-reinforce/`로 설정하고, 런타임 이미지 경로를 `import.meta.env.BASE_URL` 기준으로 변경.
- 비슷한 겉상황의 변주 사건이 같은 결과문으로 흐르지 않도록 `duplicateVariantOutcomeLogGroups` 검증값을 추가하고, 선택 라벨을 그대로 붙인 `몸을 숙인 사이`, `시선을 고정한 사이`류 결과 문장을 생성기와 검증에서 차단.
- 이미지 생성은 계속 보류하고, 선택에 따라 달라지는 사건 이미지는 정답/함정 경로를 드러내지 않으면서 표정, 자세, 소품 각도, 단서 위치만 큐에 남기도록 `docs/adventure-scenario-image-plan.md` 기준을 보강.
- 사이드 이벤트 결과 로그가 `물어보기를`, `비춰보기를`, `따라가기에`처럼 선택 버튼 문구를 그대로 문장에 붙이지 않도록 결과용 단서 문구 변환을 확장.
- `처음에는 ...처럼 보였지만`, `마지막 흔적은 다른 쪽`, `장면의 앞뒤가 조금씩`, `겉보기 쉬운 일이 아니었다` 같은 공식형 결과문을 검증에서 차단하고, 짧아진 실패 결과 템플릿은 31자 이상 인과 문장으로 보강.
- 메인 에피소드 선택 라벨 반복을 줄이기 위해 `ME-001`, `ME-007`, `ME-052`, `ME-056`, `ME-073`의 `하인에게 먼저 묻기`, `아이에게 묻기`류 라벨을 담장 하인 눈길, 인주 색, 부엌 사정, 도랑 아래 아이, 심부름꾼 행선지처럼 사건별 행동으로 교체.
- `duplicateMainChoiceLabels` 검증값을 추가해 메인 80개 중심 사건 선택 라벨이 정확히 같은 문구로 반복되지 않도록 차단. 현재 `duplicateMainChoiceLabels` 0개, `maxRepeatedChoiceLastWord` 65회.
- 사이드 이벤트 선택지 반복을 따로 조인다. `처마 찾기` 같은 어색한 행동 라벨은 장면 행동으로 바꾸고, 찾기/살피기/보기/묻기 계열을 기다리기, 읽기, 짚기, 확인하기, 따라가기 등으로 분산해 현재 `maxRepeatedSideChoiceLastWord` 34회, `maxRepeatedSideExactChoiceLabel` 2회로 통과한다.
- 선택 라벨을 선택 질문 문장에 붙일 때 `더 듣기를 택하면`, `길을 함께 걷기가 빠른 길`, `정직하게 쪽 눈길 읽기`처럼 조사가 어색하게 붙지 않도록 `awkwardChoicePrompts` 검증값을 추가. 현재 `awkwardChoicePrompts` 0개, `maxRepeatedSideChoicePrompt` 1회로 통과한다.
- 사이드 이벤트 결과 로그의 원인 문장 생성 규칙을 보정해 `다는 쪽`, `로 넘기기`, `다로 이어졌다` 같은 어색한 조사 조합을 줄이고, 비슷한 선택 실패도 사건 초점이 결과문에 들어가도록 실패 템플릿을 다양화.
- `maxRepeatedSideFinalSentence` 검증값을 추가해 사이드 이벤트 선택 결과의 마지막 문장이 4회 넘게 반복되지 않도록 차단. 현재 `maxRepeatedSideFinalSentence` 4회, `maxRepeatedSideOutcomePhrase` 10회.
- 메인 에피소드 결과 로그 4차 수작업 보강: `ME-002`, `ME-003`, `ME-008`, `ME-009`, `ME-010`, `ME-011`, `ME-014`, `ME-015`, `ME-016`, `ME-017`, `ME-019`, `ME-020`, `ME-021`, `ME-022`, `ME-026`, `ME-027`, `ME-029`, `ME-031`, `ME-034`, `ME-035`, `ME-036`, `ME-037`, `ME-038`, `ME-039`, `ME-040`, `ME-041`, `ME-042`, `ME-044`, `ME-045`, `ME-048`, `ME-049`, `ME-050`, `ME-051`, `ME-054`, `ME-057`, `ME-058`, `ME-060`, `ME-061`, `ME-072`, `ME-074`, `ME-078`의 짧거나 단서 발견만으로 끝나는 결과를 선택 행동, 인물 반응, 체면/신분/생활사 압박이 보이는 인과 문장으로 보강.
- 메인 결과 로그 최소 길이 검증 기준을 35자로 상향. 현재 `minMainOutcomeLogLength` 35, `shortMainOutcomeLogsRemaining` 0개로 통과.
- 사이드 이벤트 결과 로그 생성 템플릿을 보강해 35자 미만으로 짧던 결과 후보를 0개로 줄이고, `minSideOutcomeLogLength`와 `shortSideOutcomeLogsRemaining` 검증값을 추가. 현재 `minSideOutcomeLogLength` 35, `shortSideOutcomeLogsRemaining` 0개로 통과.
- `지켜보기` 계열 선택이 `지켜의 흔적`으로 바뀌거나, `창고의 흔적로`처럼 조사가 잘못 붙는 생성 오류를 보정하고 검증 금지 패턴에 추가. 사이드 결과 마지막 문장 반복도 다시 `maxRepeatedSideFinalSentence` 4회로 낮춰 유지.
- 메인 선택지 중 `병사 소매 묻기`, `마을 경고 듣기`, `진흙이 어디서 묻었는지 물을지`처럼 개발자식이거나 어색한 표현을 장면 행동 중심으로 수정. `awkwardChoiceLabels` 0개, `awkwardChoicePrompts` 0개, 메인 선택지 수동 감사 0건으로 통과한다.
- 비슷한 사건이 선택 후 다르게 흘러가는 방향은 유지하되, 이번 단계에서는 새 이미지를 생성하지 않고 필요한 이미지 키, 반복 인물 세트, 표정·자세·소품 각도·단서 위치만 `docs/adventure-scenario-image-plan.md`에 기록한다.
- 봉수·역참 사이드 이벤트 9차 수작업 보강: `SE-025`, `SE-026`, `SE-028`, `SE-083`, `SE-084`, `SE-085`, `SE-086`, `SE-088`, `SE-089`, `SE-090`, `SE-092`를 낮게 깔린 봉수 연기, 숫자를 틀리는 봉수군, 울지 않는 역참 말, 봉수대 따뜻한 돌, 물집 없는 봉수군, 누런 연기, 봉수대 아래 말발굽, 빈 말안장, 엎어진 물통, 길 이름을 틀리는 파발, 말에게 속삭이는 역참 아이처럼 각각 다른 단서와 선택 인과로 교체. 공통 생성문이던 `잘못 읽으면 한 사람의 길이 막힐 수 있었다`류 흐름을 제거하고, `npm run validate:adventure` 기준 `awkward generated Korean remains` 0개로 통과한다.
- 신분·문서·길표식 사이드 이벤트 10차 수작업 보강: `SE-040`, `SE-105`, `SE-106`을 노비 아이의 이름표 봇짐, 번지지 않는 먹빛의 문질러진 한지, 삐뚤게 세운 고갯길 표지판 사건으로 교체. `SE-105`의 생성형 서당/포졸 문구와 `SE-106`의 먹빛 문서 단서 오배치를 제거하고, 이미지 생성 없이 `docs/adventure-scenario-image-plan.md`에 필요한 컷 차이만 기록한다.
- 생활사·신분 압박 사이드 이벤트 11차 수작업 보강: `SE-107`, `SE-108`, `SE-109`, `SE-110`을 두 켤레 짚신과 아픈 동생, 부러진 낫과 머슴의 책임 두려움, 우물 줄과 나무패, 안에서 잠긴 집과 병든 사람의 단서로 교체. 기존 생성형 마을/아이/흔적 문구를 제거하고, 이미지 생성 없이 신발 크기·약초 잎·낫날 금·물집·우물 줄·흰 천·죽 냄새 단서만 이미지 큐에 기록한다.
- 장인·산길 위험 사이드 이벤트 12차 수작업 보강: `SE-112`, `SE-113`, `SE-114`, `SE-115`, `SE-116`을 덮인 완성품과 문밖 별감, 한쪽 길 빗물과 막힌 도랑, 끊긴 새소리와 비 냄새, 호랑이처럼 들리는 북소리 울림, 멧돼지 발자국 옆 짚신 덫 흔적으로 교체. 이미지 생성 없이 덮인 공예품, 막힌 도랑, 뒤집힌 잎, 작은 북/바위 울림, 짚신 뒤꿈치와 덫줄 단서를 큐에 추가했다.
- 산길 표식·계곡 물길 사이드 이벤트 13차 수작업 보강: `SE-119`, `SE-120`을 귀한 소금 줄과 그늘진 비탈, 소금 장수의 말, 계곡 흙물과 볏짚, 작은 다리와 위쪽 둑 터짐 전조로 교체. 비슷한 산길 사건처럼 보여도 하나는 믿음/안전 표시/추적 흔적 사이에서, 다른 하나는 지금 건너기/위쪽 확인 사이에서 다르게 전개되도록 하고, 이미지는 생성하지 않은 채 소금 줄·짚신 자국·흙물·볏짚·둑 단서만 큐에 기록했다.
- 장인·무기 재료 사이드 이벤트 14차 수작업 보강: `SE-129`, `SE-134`, `SE-136`~`SE-140`을 진흙 없는 비단 가지 표식, 담금질 물과 칼날 얼룩, 찢어진 한지 틀과 빠진 젖은 종이, 강한 옻칠 냄새와 탄 종이 냄새, 길 위의 나전 조각 길표식, 느슨한 각궁 활줄, 붉은 실 달린 다른 색 화살깃 사건으로 교체. 이미지 생성 없이 비단 매듭, 물통 기름막, 닥나무 섬유, 옻칠 받침대 그을음, 나전 뒷면 옻, 활줄 매듭 물기, 화살깃 붉은 실 단서만 큐에 기록했다.
- 해안·포구 사이드 이벤트 15차 수작업 보강: `SE-147`, `SE-148`, `SE-149`, `SE-150`을 낮은 배 자국과 창고 문턱 젖은 모래, 깨끗하게 잘린 밧줄과 칼집 손때, 검은 조개 줄과 물때 표시, 홀로 뜬 해녀 물왁과 같은 바위를 향한 발끝 사건으로 교체. 이미지 생성 없이 얕은 배 자국, 밧줄 섬유 결, 젖은 발자국, 검은 조개 줄, 물왁 끈 손자국, 해녀들 몸짓 단서만 큐에 기록했다.
- 변방·이야기·홍길동·신분 사이드 이벤트 16차 수작업 보강: `SE-151`~`SE-162`를 북방 풀밭 발자국과 바람막이 천, 말방울 한 번과 끊어진 방울끈, 국경 장터 오역과 중개인 손짓, 털모자 안감과 되돌아선 발길, 얼음 썰매 홈과 갈대 밑 작은 자국, 산성 돌문 숨소리와 성벽 흙자국, 세책방 젖은 책갈피, 이야기꾼이 바꾼 결말, 홍길동 이름 밑 쌀독 그림, 활빈당 표식을 베낀 아이 종이, 문밖 서자 소년의 지운 편지, 양반 아이가 지운 친구 이름 사건으로 교체. 이미지 생성 없이 변방 풀밭·역참 방울·국경 장터·세책방·이야기판·홍길동 표식·신분 압박 단서만 큐에 기록했다.

### 2026-06-04 이순신 액션 스프라이트 생성
- `knol-jump/assets/characters/leesunsin`의 기존 이순신 캐릭터를 기준으로 한손/두손 액션 시트를 생성.
- 각 무기군은 `idle`, `walk_a`, `walk_b`, `jump`, `fall`, `slash_start`, `slash_middle`, `slash_end`, `stab_start`, `stab_middle`, `stab_end`의 11프레임으로 정리.
- 산출물은 `public/images/yisun_actions/`에 저장하고, 후처리 스크립트는 `scripts/process-yisun-action-sprites.py`에 추가.
- 통과본은 `strict-review-pass-v1` 상태로 표시. 이전 실패 후보는 public 자산에서 제거하고, 통과본만 유지.
- 검수 기준에 동일 인물/비율뿐 아니라 동일 무기 유지, 손잡이/자루 파지, 칼날 파지 금지, 무기 궤적에 붙은 휘두르기/찌르기 이펙트 조건을 포함.
- 찌르기 시작/중간/끝 프레임에 무기 끝에서 시작하는 짧은 thrust 잔광과 밝은 코어를 후처리로 보강. 캐릭터와 무기 본체는 재생성하지 않음.
- 콤보 체인 전용 한손/두손 12프레임 시트를 별도로 추가. 첫 공격은 칼/월도를 위로 치켜든 상단 베기 예비동작으로 시작하고, 베기-연결-찌르기-연결-베기-복귀 구조로 검수.

### 2026-06-05 이순신 콤보 체인 재생성
- 게임 공격 애니메이션의 예비동작, 타격, 팔로스루 기준을 반영해 한손/두손 콤보 체인 시트를 12프레임으로 재생성.
- 한손 콤보는 상단 베기 예비동작으로 시작하고, 손이 칼날을 잡지 않으며, 베기/찌르기 이펙트가 무기 궤적에 붙도록 검수.
- 두손 콤보는 한손보다 긴 사정거리를 유지하고, 찌르기 접촉 프레임에서 월도 날이 보이며 이펙트가 날 앞쪽에서 시작하도록 검수.
- 12프레임 전체 시트를 한 번에 생성하면 일부 이펙트가 프레임 경계에 닿고 모션 연결이 흔들리는 문제가 있어, 한손/두손 각각 `베기A`, `찌르기B`, `베기C` 4프레임 스트립 단위로 재생성.
- 타격 컷에서 무기 일부가 이펙트에 가려지는 것은 자연스러운 것으로 허용하되, 이펙트가 무기 궤적/끝에서 시작하고 손·자루·몸통에서 발생하지 않으며 프레임 경계에서 잘리지 않는지 기준으로 재검수.
- `scripts/process-yisun-combo-chain.py`에 4프레임 스트립 조립, 가장자리 잔여 픽셀 제거, 안전 여백 확보 후처리를 추가하고 `manifest.json` 상태를 `strict-review-pass-v3-combo-chain-strips`로 갱신.
- `public/yisun-action-preview.html`에 한손/두손 콤보 루프 미리보기를 추가하고, 균등 재생뿐 아니라 공격 타이밍과 느린 검수 모드로 동작 연결성을 확인할 수 있게 구성.
- 다른 게임 스프라이트를 직접 복제하지 않고, 2D 액션/격투 게임의 `startup/active/recovery`, 예비동작/타격/팔로스루, 히트 이펙트 위치 기준을 정리한 `docs/yisun-combo-animation-standards.md`를 추가.
- 다음 콤보 이미지 재생성은 위 기준안의 실루엣, 체중 이동, 히트 프레임, 프레임 타이밍 기준을 먼저 통과해야 함.
- Phaser 3 도입을 고려해 `manifest.json`에 `phaser.frameWidth`, `frameHeight`, `origin`, animation별 `textureKey`, `durations`, `phases`, `activeFrames`를 추가.
- 미리보기 HTML의 `공격 타이밍` 재생은 하드코딩된 값이 아니라 manifest의 Phaser duration 메타데이터를 우선 사용하도록 변경.
- 이전 9프레임 콤보 시트와 키 이름은 폐기하고, `scripts/process-yisun-combo-chain.py`를 12프레임 처리 기준으로 갱신.

## 현재 모험 런타임 풀
- 기존 80/200/50 V2 초안 풀은 `archive/adventure-draft-pool/`로 분리했다.
- 플레이 가능한 모험은 승인본 전용 파일 `src/data/adventureApprovedStories.js`만 import한다.
- 현재 사용자 승인까지 완료된 새 미연시 모험 이야기는 0개다.
- 현재 플레이 검토용으로 런타임에 연결된 후보는 2개다.
- 검토 후보는 `review-main-001` `검은 갓의 사발`과 `review-main-002` `달콤한 유혹의 숨은 값`이다.
- `review-main-001`은 저승사자 같은 검은 갓의 선비와 어두운 사발을 다루는 인연 만남형 단편이다.
- `review-main-002`는 토끼전/수궁가의 토끼와 자라 모티브를 쓰는 전래동화 각색형 단편 세트다. 주인공은 토끼이며, 풀밭 유혹, 용궁 위기, 육지 마무리 선택으로 이어진다.
- `review-main-001`은 역사 근거보다 단편 완성도, 재미, 인과성, 논리성을 우선해 다시 다듬었다. 현재 중심 장면은 비 내리는 고갯길 주막에서 주모가 보지 못하는 검은 갓의 선비가 검은 사발, 빈 사발과 국밥, 검은 장부, 문고리를 앞에 두고, 주인공이 세 가지 행동 중 하나를 고르는 선택이다. 선택 A는 `검은 사발을 엎어 장부 글씨를 덮기`, 선택 B는 `빈 사발에 국밥을 덜어 내밀기`, 선택 C는 `문고리를 잡고 주막 밖으로 물러서기`로 고정했다.
- 어린 학생도 읽는 모험이라는 기준을 반영해 `낮은 술상 소리처럼`, `길 하나는 늦춰`, `문턱은 넘은 게요`처럼 뜻이 흐릿한 문구를 제거하고, 피/잔혹한 폭력/노골적인 죽음 묘사 없이 기묘한 손님과 사발 앞의 예절 중심으로 긴장을 유지하도록 조정했다.
- 주모의 초반 경고에서 `검은 갓 쓴 손님이 합석을 청하면`처럼 다음 장면을 그대로 알려 주는 직접 예고를 제거하고, `밤에 자기 이름을 함부로 말하지 말라`는 단편식 불안 신호로 바꿨다.
- 검은 갓의 선비 대사 뒤 주인공 속생각이 대사를 풀이하듯 반복하지 않도록, 빈 사발과 국밥, 문고리, 소매 속 손만 보이게 했고, 검은 사발과 검은 물방울은 다음 글상자에서 압박으로 드러나게 했다. 선택 직전에는 `등 뒤 문고리에도 하얀 서리가 붙어 있었다`, `내 시선과 손은 세 곳 사이에서 멈췄다`로 실제 행동 선택의 순간과 시선 이동을 더 분명히 남겼다.
- 최신 수리에서는 `검은 갓과 보이지 않는 손님, 서리가 번진 상판이 한 줄로 이어졌다`처럼 추상적으로 판단을 설명하던 문장을 `검은 갓을 다시 보았다`, `주모는 여전히 맞은편 손님을 알아차리지 못했다`, `상판의 서리까지 보이자`로 바꾸고, 장부 장면도 `선비가 내 앞에 멈춘 검은 사발 옆에 장부를 펼치자`, `붓끝이 한쪽 칸 위에 가만히 멈췄다`, `옆에는 먹물이 번진 이름 같은 글씨가 있었다`로 구체화했다. 선택 C 결과도 `진흙이 짚신에 무겁게 달라붙었다`, `잠시 뒤 흙길은 주막 마당의 질척한 흙으로 바뀌었다`로 정리해 반복 표현을 줄였다.
- 추가 연결성 점검에서는 속생각의 물건 나열을 `상 한쪽의 빈 사발부터 눈에 들어왔다`, `국밥에서는 김이 조금씩 사라졌다`, `등 뒤 문고리는 젖어 차가웠다`로 바꿨다. 선택 B 결과는 먹물이 번진 뒤 장부 종이가 움직이고 그 순간 맞은편 자리가 비도록 나누었고, 선택 C 결과도 흙길이 주막 마당 흙으로 바뀐 뒤 문틈 사이 검은 사발을 보게 하도록 문장을 분리했다.
- 첫 후보의 `storyCraft.sentenceBridgeAudit`를 추가해 14개 글상자 각각이 앞 문장의 이유, 시선 이동, 압박, 감정 반응으로 어떻게 이어지는지 기록했다. 검증 스크립트는 이 감사 기록이 글상자 수와 맞고 검토용 읽기본에도 동기화되어 있는지 확인한다.
- `npm run smoke:adventure`를 추가해 첫 에피소드 1편의 실제 앱 흐름을 데스크톱/모바일에서 반복 검증할 수 있게 했다. 이 검증은 `review-candidate`와 `approved` 상태 모두를 대상으로 글상자 순서, 세 선택 결과, 선택별 마지막 장면, 선택지 세로 배치, 임시 이미지 표시를 확인한다.
- `docs/adventure-first-episode-approval-audit.md`를 추가해 목표 요구사항별 자동 증거와 사용자 판단으로 남은 항목을 분리했다.
- `docs/adventure-first-episode-review-copy.md`를 추가해 첫 에피소드의 글상자, 선택지, 양쪽 결과, 선택별 마지막 장면을 코드 없이 읽고 검토할 수 있게 했다.
- `npm run validate:adventure`가 첫 에피소드 검토용 읽기본과 승인 감사 문서가 현재 런타임 후보 데이터와 동기화되어 있는지도 확인하도록 보강했다.
- 이전 `approved-main-001` 도깨비감투 파일럿은 사용자 플레이 피드백에서 줄거리와 선택지가 약하다고 판정되어 승인본에서 제거했다.
- 새 이야기는 `docs/adventure-v2-criteria.md`, `docs/adventure-vn-production-plan.md`, `docs/adventure-vn-story-standards.md` 기준을 한 편씩 통과한 뒤에만 런타임 연결을 검토한다.
- 승인본은 `episodeCategory`를 먼저 정하며, 전래동화 각색형은 원전, 모티프, 주인공 개입 지점, 원래 흐름, 바뀐 흐름, 지켜야 할 핵심을 기록해야 한다.
