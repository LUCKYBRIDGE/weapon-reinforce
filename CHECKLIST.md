# Weapon Reinforce 1.2.0 검증 체크리스트

이 문서는 `1.2.0` 릴리스 후보의 검증 기준을 다룬다. 토끼·자라 장편 모험과 0.13 이전 랜덤 조우는 과거 이력이며 런타임·배포 대상이 아니다. 자동 게이트가 모두 통과해도 아래 수동 릴리스 게이트가 남아 있으면 `v1.2.0` 태그를 만들지 않는다.

## 자동 출시 게이트

- [x] `npm run validate:weapons`: 7단계 역사 트리, 강화 수치, 출처, 무기별 전투 개성과 화면 배치를 검사한다.
- [x] `npm run validate:quizzes`: 9팩 762문항, 정답 관계, SVG 506개와 참조 경로·위험 태그를 검사한다.
- [x] `npm run validate:saves`: 손상된 로컬 숫자·객체·배열·경로·통계를 안전한 값으로 복구한다.
- [x] `npm run validate:save-file`: JSON 저장 형식, 체크섬, 1MB 상한, 허용 키, 교체와 실패 롤백을 검사한다.
- [x] `npm run validate:economy`: 가상 전리품 6종·드롭 표 7종·준비물 3종, 결정적 드롭, 교환·장착·소비와 거래·정산 중복 방지를 검사한다.
- [x] `npm run validate:expedition`: 3지역·적 7·NPC 6·사건 6·역사층 7, 유형 우선 추첨, 역할·공격 예고, 자동전투, 선택형 지원 조우, 무기 전투 피드백, v2→v3 이관과 정산을 검사한다.
- [x] `npm run validate:balance`: 단계별 7깊이 탐사가 유한하게 끝나는지 고정 시드로 검사한다. 수치는 시스템 완성 뒤 별도 조정한다.
- [x] `npm run lint`: React/ESLint 오류가 없다.
- [x] `npm run build`: GitHub Pages base path를 포함한 프로덕션 빌드가 생성된다.
- [x] `npm run validate:runtime`: 허용 자산, 용량 상한, 퀴즈 카탈로그가 참조한 516개 파일과 실제 공개 파일의 일치, 분리된 토끼·자라 런타임 자산 0개를 검사한다.
- [x] `npm run validate:release`: package/lock 버전, README 콘텐츠 수, 1.2 릴리스 노트, 수동 게이트 목록의 정합성을 검사한다.
- [x] `npm run validate:all`: 위 검사를 배포 워크플로와 같은 한 명령으로 재실행할 수 있다.
- [x] `npm run validate:publish`: 수동 게이트 완료와 RC→Stable 문서 전환을 요구하는 정식 발행 전용 검증을 제공한다.
- [x] `.github/workflows/release.yml`: 최신 master·버전·수동 완료 확인·태그 중복을 검사한 뒤에만 GitHub Release를 발행한다.
- [x] `validate:release`: 현재 문서가 RC이면 `validate:publish`가 실제로 실패하고 Stable이면 성공하는지 subprocess로 자가검증한다.

## 저장과 복구

- [x] 강화 단계, 엽전, 퀴즈 통계, 도감, 괴작, 탐사 통계가 `localStorage`에 자동 저장된다.
- [x] 진행 중인 탐사의 HP, 깊이, 현재 조우, 단계, 임시 명성·기록 조각·가상 재료, 난수, 결정된 적 공격, 남은 효과와 사용 준비물이 체크포인트에 포함된다.
- [x] 탐사를 새로고침해도 같은 상태와 다음 조우를 이어가며 결과를 다시 뽑지 않는다.
- [x] 귀환·사망 정산은 같은 정산 ID를 두 번 적용해도 엽전과 통계가 한 번만 바뀐다.
- [x] 탐사 창고·준비물·장착 상태는 `weaponExpeditionEconomyV1`에 자동 저장되고 JSON 저장 파일 허용 목록에 포함된다.
- [x] 버전 2 진행은 버전 3으로 이관하며 미래 버전, 잘못된 깊이/조우, 살아 있는 적의 결정 상태는 거부한다.
- [x] 저장 파일은 버전·생성 시각·체크섬을 포함하고 게임 소유 키만 내보낸다.
- [x] 손상, 변조, 미지원 버전, 허용되지 않은 키, 1MB 초과 파일은 기존 저장을 바꾸지 않고 거부한다.
- [x] 저장 교체 중 쓰기 실패를 주입해 기존 로컬 진행이 정확히 롤백되는지 검사한다.
- [x] 로컬 저장이 차단되거나 용량이 부족하면 조용히 무시하지 않고 파일 백업 안내를 화면에 표시한다.
- [x] 실제 브라우저에서 진행 중 JSON을 내려받고, 생성 파일의 체크섬·현재 깊이·조우·선택 상태를 다시 읽어 확인한다.
- [x] 저장 파일 불러오기는 실제 탐사 객체를 사용해 `내보내기 → 검증 → 전체 교체 → 탐사 복구`를 자동 통합 검증한다.
- [ ] OS 파일 선택 창에서 같은 파일을 고르는 마지막 UI 왕복은 인앱 검증 도구가 로컬 파일 주입을 지원하지 않아 일반 브라우저에서 배포 전 수동 확인한다.

## 핵심 플레이 루프

- [x] 새 게임은 +1 K2 소총, 0냥에서 시작하고 퀴즈를 풀어 엽전을 번다.
- [x] 강화 판정 직전 터치·클릭·Enter/Space로 타이밍 바를 멈출 수 있다.
- [x] 강화 단계가 오를수록 비용이 오르고 성공률이 내려가며 GOOD/PERFECT가 확률을 보정한다.
- [x] 실패하면 +1 K2로 돌아가고, 12종 괴작 중 하나가 확률적으로만 나온다.
- [x] 괴작 발견/보유/판매와 영구·보유 조건 칭호, 일회성 보상이 구분된다.
- [x] 마지막 괴작 판매로 보유 조건 칭호가 비활성화될 때 경고하고, 중복은 한 개를 남겨 일괄 판매할 수 있다.
- [x] 평생 최고 성공 단계의 -2까지만 복원 무기고에서 비싸게 다시 살 수 있다.
- [x] 탐사는 7개 노드를 따라 진행하고 첫 조우는 적, 마지막 조우는 최종 적이다.
- [x] 중간 조우는 먼저 적 60%·NPC 23%·사건 17% 유형을 뽑고 해당 유형 안에서 개별 후보를 고르며 최근 2개 ID 반복을 피한다.
- [x] 적 2연속 뒤에는 NPC/사건을 보장해 억울한 장기 적 연속을 막는다.
- [x] 적 조우는 `내 공격 → 상대 공격 예고 → 상대 공격`을 자동 반복하고 쓰러진 적은 반격하지 않는다.
- [x] 적 7종은 역할·방어·체력/공격 보정·첫 강공/주기 강공이 실제 계산에 서로 다르게 반영된다.
- [x] NPC와 사건은 회복·다음 공격·다음 방어·전리품 조사 보정을 서로 다르게 주며 한 번만 적용된다.
- [x] 적/NPC/사건 전리품은 임시 가방에 쌓이고 안전 귀환 때 창고로 옮겨진다.
- [x] 사망은 임시 명성·조각·가상 재료를 잃고 최대 200냥 안에서 수리비 또는 강탈 손실을 적용하되 강화 단계는 보존한다.
- [x] 탐사 준비소에서 가상 전리품과 퀴즈 엽전으로 준비물을 만들고 하나를 장착·소비하며, 전리품 자체는 판매할 수 없다.

## 재미·화면·입력

- [x] 현재 무기 7종은 서로 다른 공격명, 치명타·방어·회복 특성과 1인칭 공격 자세를 가진다.
- [x] 전장 왼쪽에 무기를 든 상반신 뒷모습, 공격 순간에는 1인칭 무기와 손 연출이 나타난다.
- [x] 턴 배지는 지금 실행 중인 플레이어/적 행동과 강공격 예고를 표시한다.
- [x] 1배/2배 속도 변경은 저장된 난수를 건드리지 않는다.
- [x] 탐사 중 강화·퀴즈·도감·복원·저장 교체 등 충돌 가능한 조작을 잠근다.
- [x] 데스크톱에서 탐사 시작·교대전투·사망·귀환·기록 복원·파일 백업·새로고침 복구를 포인터로 확인한다.
- [x] 탐사 모달은 제목·설명·초기 포커스·Tab 순환을 제공하며 진행 중 Escape 종료를 막고 정산 뒤에만 닫을 수 있다.
- [x] 무기 도감의 해금 카드는 키보드 버튼으로 선택할 수 있고 무기·괴작 도감은 대화상자로 식별된다.
- [x] 날짜가 바뀐 상태에서 진행 중 탐사를 복구해도 일보와 탐사 모달이 겹치지 않는다.
- [x] 768px급 태블릿에서 헤더, 대장간, 전장, 경로, 모달 스크롤과 조작 영역을 확인한다.
- [x] 390px급 모바일에서 가로 넘침 없이 대장간·HP·전리품 칩·조우 특징·준비소 카드·선택 버튼·저장·백업이 표시되는지 확인한다.
- [x] 개발 서버와 프로덕션 미리보기에서 브라우저 console error/warn 0개, 깨진 이미지 0개를 확인한다.

## 역사·교육 정보

- [x] 단일 트리는 `K2 소총 → 정조대 월도 → 선조대 장도·쌍수도 → 조선 군영 환도 → 백제 칠지도 → 한국식 동검 → 비파형동검` 순서다.
- [x] 오래될수록 강해진다는 규칙은 실제 무기 우열이 아니라 대장간의 `역사 공명` 설정으로 분리한다.
- [x] 각 역사층은 현재 무기 데이터의 시대·용도·고증·공식 자료와 직접 연결된다.
- [x] 카드에는 초등학생이 읽기 쉬운 짧은 사실과 생각 질문을 제공한다.
- [x] 기록 카드는 해당 층을 먼저 목격한 뒤 기록 조각을 써야 전체 내용을 복원할 수 있다.
- [x] 적은 역사 실재 인물이나 집단으로 단정하지 않는 가상 존재이며, 역사 정보와 전투 판타지를 구분한다.
- [x] +7 무기로 7층에서 안전 귀환해야 시간 균열 완주 기록이 열린다.

## 1.2 변경 검증

- [x] 1.2-01: 탐사 적 7종, NPC 6종, 사건 6종으로 확장한다.
- [x] 1.2-01: 모든 지역에 NPC 2종 이상, 사건 2종 이상이 존재한다.
- [x] 1.2-01: 청동 안개 5~6층 일반 적 후보가 2종 이상이다.
- [x] 1.2-01: 같은 지역의 NPC/사건 이미지가 한 종류로 고정되지 않는다.
- [x] 1.2-01: 같은 지역 지원 조우의 효과 조합이 완전히 중복되지 않는다.
- [x] 1.2-01: 200개 시드에서 2~6층 조우가 모두 알려진 ID와 올바른 지역으로 선택된다.
- [x] 1.2-01: 기존 60/23/17 유형 비율과 적 2연속 뒤 지원 조우 보장이 유지된다.
- [x] 1.2-02: 선택형 NPC 2종·사건 2종에 각각 2택 선택을 제공하고 선택 결과가 서로 다르다.
- [x] 1.2-02: `npc-choice` / `event-choice` 상태는 선택 전 보상을 지급하지 않고 JSON 직렬화·복구 뒤 같은 선택지를 유지한다.
- [x] 1.2-02: 알 수 없는 선택 ID와 선택 결과 중복 적용을 자동 검증한다.
- [x] 1.2-02: 390px 미디어쿼리에서 두 선택 버튼을 1열로 배치하고 키보드 첫 포커스·Tab 순환을 유지한다.
- [ ] 1.2-02: 실제 390px 브라우저에서 터치 선택과 가로 넘침을 배포 전 수동 재확인한다.
- [x] 1.2-03: 최종 적 HP 배율만 135% → 120%로 조정하고 공격력·방어·강공 주기는 유지한다.
- [x] 1.2-03: +6 무준비 최종층 생존률이 5% 이하인지 자동 검증한다.
- [x] 1.2-03: +7 무준비 최종층 생존률 20~30%, 여행 붕대·공명 손질 천 30~45% 목표 구간을 자동 검증한다.
- [x] 1.2-03: 동일 시드에서 전투 능력이 없는 균열 안전 주머니가 무준비 생존률을 바꾸지 않는지 검증한다.
- [x] 1.2-04: 무기 7종에 서로 다른 게임 전투 태그와 공격명이 존재하는지 자동 검증한다.
- [x] 1.2-04: 치명타 배율·기본 방어·적중 회복 수치는 변경하지 않고 기존 combatProfile을 그대로 사용한다.
- [x] 1.2-04: 치명타·실제 피해 경감·적중 회복·지원 공격 보너스를 구조화된 lastAction 피드백으로 기록한다.
- [x] 1.2-04: 구조화된 치명타·회복 피드백이 JSON 직렬화·복구 뒤 유지되는지 검증한다.
- [x] 1.2-04: 전투 특성 패널에 '게임 전투 효과'를 명시해 역사적 실전 성능과 구분한다.
- [x] 1.2-04: 기존 무기별 1인칭 모션에 치명타 플래시·방어 경감·회복 강조 연출을 추가한다.
- [ ] 1.2-04: 실제 데스크톱·390px 브라우저에서 전투 피드백 배지가 대사창/HP를 가리지 않는지 배포 전 수동 재확인한다.
- [x] 1.2-05: 런타임과 시뮬레이터가 대성공 +2 3%·+3 1%·가짜 대강화 4%, 복원 상한, 타이밍 판정 폭을 같은 공용 규칙에서 사용한다.
- [x] 1.2-05: 대성공·복원 무기고·MISS/GOOD/PERFECT 혼합을 포함한 +1→+7 완주 20,000회 시뮬레이션을 실행한다.
- [x] 1.2-05: 복원 없음 기준팩 예상 정답 수가 MISS ≤180, 타이밍 창 비례 ≤160, GOOD/PERFECT 혼합 ≤135, PERFECT ≤115인지 자동 검증한다.
- [x] 1.2-05: 최고 가능 단계 즉시 복원이 평균 강화 시도를 50% 이상 줄이면서 총 엽전 소모는 1.5~2.5배가 되는 시간-비용 trade-off를 자동 검증한다.
- [x] 1.2-05: 9개 퀴즈팩의 실제 보상 범위를 사용해 기초/중간/고난도 보상팩별 예상 정답 수를 출력한다.
- [x] 1.2-05: 계측 결과 핵심 진행이 과도하지 않아 강화 비용·성공률·타이밍 보너스·퀴즈 보상·복원 가격은 1.2에서 변경하지 않는다.
- [x] 1.2-06: package.json과 package-lock.json 버전을 `1.2.0`으로 맞춘다.
- [x] 1.2-06: README의 적/NPC/사건 수와 1.2 선택형 조우·게임 전투 효과 설명을 현재 데이터와 맞춘다.
- [x] 1.2-06: `docs/releases/v1.2.0.md`에 1.2 변경점·밸런스·저장 호환·수동 게이트를 기록한다.
- [x] 1.2-06: 알려진 수동 3건 외의 미완료 체크가 있으면 실패하는 `validate:release`를 전체 출시 게이트에 포함한다.
- [x] 1.2-06: 아래 수동 3건이 모두 완료되기 전에는 `v1.2.0` 태그·GitHub Release를 만들지 않는 릴리스 조건을 고정한다.

## 다음 배포 전 수동 재검증 순서

1. `npm ci && npm run validate:all`
2. 새 브라우저 저장소에서 퀴즈 정답·오답, 강화 타이밍 입력, 강화 중 조작 잠금 확인
3. 탐사 시작 후 자동전투 중 새로고침, 결정 화면 새로고침, 같은 상태·조우 재개 확인
4. 7노드에서 적·NPC·사건, 계속 탐사, 안전 귀환과 영구 정산 확인
5. 사망 후 임시 보상 소멸, 엽전 손실 상한, 강화 단계 보존 확인
6. 시간 기록관에서 목격 전/후 카드, 조각 사용, 명성 칭호와 완주 기록 확인
7. JSON 저장 파일 내려받기와 교체 불러오기, 변조 파일 거부 확인
8. 데스크톱·태블릿·390px 모바일에서 포인터·키보드·터치와 가로 넘침 확인
9. 프로덕션 미리보기 `/weapon-reinforce/`에서 모든 이미지·퀴즈·새로고침 경로와 콘솔 확인
